<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>监控室内空气质量</title>
    <link href="/2022/12/05/%E7%9B%91%E6%8E%A7%E5%AE%A4%E5%86%85%E7%A9%BA%E6%B0%94%E8%B4%A8%E9%87%8F/"/>
    <url>/2022/12/05/%E7%9B%91%E6%8E%A7%E5%AE%A4%E5%86%85%E7%A9%BA%E6%B0%94%E8%B4%A8%E9%87%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="1-背景"><a href="#1-背景" class="headerlink" title="1. 背景"></a>1. 背景</h1><p>爱否出品的新风消费报告<sup id="fnref:1" class="footnote-ref"><a href="#fn:1" rel="footnote"><span class="hint--top hint--rounded" aria-label="[新风消费者报告 | 爱否出品\_哔哩哔哩\_bilibili](https://www.bilibili.com/video/BV1A4411u7gg/)">[1]</span></a></sup>里提到了一个概念：室内空气的四个终极目标是恒温、恒湿、恒净、恒氧。笔者对此深感认同，可惜笔者生活的北京并没有足够宜人的气候<sup id="fnref:2" class="footnote-ref"><a href="#fn:2" rel="footnote"><span class="hint--top hint--rounded" aria-label="[模板:北京市气候数据 - 维基百科，自由的百科全书](https://zh.wikipedia.org/wiki/Template:%E5%8C%97%E4%BA%AC%E5%B8%82%E6%B0%94%E5%80%99%E6%95%B0%E6%8D%AE)">[2]</span></a></sup>：</p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221205220501060.webp"></p><p>空气质量指数<sup id="fnref:3" class="footnote-ref"><a href="#fn:3" rel="footnote"><span class="hint--top hint--rounded" aria-label="[北京空气质量指数AQI\_PM2.5月统计历史数据\_中国空气质量在线监测分析平台历史数据](https://www.aqistudy.cn/historydata/monthdata.php?city=%E5%8C%97%E4%BA%AC)">[3]</span></a></sup>也堪忧：</p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221205215834953.webp"></p><p>更糟糕的是，为了保证室内空气含氧量，笔者的窗户常年是半开的。这就导致室内空气很容易受室外影响，不仅容易积灰，而且往往要等空气质量显著糟糕时笔者才会想到要关窗。</p><p>为了能更好地评估室内空气质量，并指导改进的方向，笔者决定搭建一套监控室内空气质量的解决方案。需要监控的指标包括：温度&#x2F;湿度&#x2F;二氧化碳&#x2F;PM2.5。</p><h1 id="2-硬件部分"><a href="#2-硬件部分" class="headerlink" title="2. 硬件部分"></a>2. 硬件部分</h1><p>首先是物美价廉的米家蓝牙温湿度计2，只要29即可实现温湿度测量，并可在APP端查看历史数据。然而实际体验并不好，打开APP后很难直接加载成功数据（笔者甚至要重启蓝牙才行），查看历史数据功能也不能自定义时间范围。不清楚使用蓝牙网关后体验能否得到提升。</p><table><thead><tr><th><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/c44018f98ca3bc62.webp"></th><th><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206092203598.webp" alt="image-20221206092203598" style="zoom:50%;" /></th><th><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206092129994.webp" alt="image-20221206092129994" style="zoom:50%;" /></th></tr></thead></table><p>然后是功能相对完善的青萍空气检测仪Lite、淘宝某品牌空气检测仪，在500元的价位上都能做到温度&#x2F;湿度&#x2F;二氧化碳&#x2F;PM2.5的检测。</p><table><thead><tr><th><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/O1CN017VZrO42DFEU0obiX7_!!2201232258579.webp" alt="img" style="zoom:50%;" /></th><th><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206093206207.webp" alt="image-20221206093206207" style="zoom:50%;" /></th></tr></thead></table><p>青萍这款接入了米家生态，能很好和其余设备联动，外形也更为简约。某品牌这款优点是自定义程度高，和店家咨询后得知其能通过USB将监控数据发送主机，并提供了一份源码<sup id="fnref:4" class="footnote-ref"><a href="#fn:4" rel="footnote"><span class="hint--top hint--rounded" aria-label="[JoeyZheng/Lewei365B35：一个简单的应用程序，从365 B35获取数据，然后发送到Lewei50平台，用于网页和微信的数据监控和跟踪。](https://github.com/JoeyZheng/Lewei365B35)">[4]</span></a></sup>作为参考。笔者考虑到使用米家APP的体验不佳，且有较多自定义需求，所以购入了后者。</p><p>后者到手后笔者发现了一些小问题：</p><ul><li>由于需要通过USB发送检测数据，所以检测仪不能离主机太远，而摆放的位置可能会影响检测准确性</li><li>由于需要始终连接USB，各部件的发热（包括内置电池）会影响温度检测准确性</li><li>USB发送的检测数据中不包括PM10，商家解释这是因为国内的PM10传感器普遍不准确</li></ul><p>好在这些问题笔者都能接受。有了数据源后，下面就开始正式施工。</p><h1 id="3-软件部分"><a href="#3-软件部分" class="headerlink" title="3. 软件部分"></a>3. 软件部分</h1><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/architecture.webp" alt="prometheus架构图"></p><p>参考Prometheus的架构图，笔者列出了自己这套监控方案的架构图：定期抓取空气检测数据并上报、Grafana做展示、告警则用Telegram触达。</p><pre><code class=" mermaid">flowchart LR    A[空气检测仪]--USB--&gt;B[PC]    B-.push.-&gt;C[Pushgateway]    D[Promethues]-.pull.-&gt;C    D--&gt;E[Alertmanager]    E--&gt;G[Telegram]    F[Grafana]--&gt;D    style C stroke-dasharray:5    style E stroke-dasharray:5</code></pre><h2 id="3-1-上报检测数据"><a href="#3-1-上报检测数据" class="headerlink" title="3.1 上报检测数据"></a>3.1 上报检测数据</h2><p>在<a href="https://github.com/prometheus/pushgateway/releases">releases页</a>下载最新Pushgateway后用<a href="http://supervisord.org/index.html">Supervisor</a>等工具运行，即可在9091端口上提供API服务。</p><p>将检测仪连接到PC后，PC会自动识别到该设备（win10&#x2F;ubuntu20.04都无需额外安装驱动，好评）。用波特率57600连接后，笔者发现检测仪将PM2.5&#x2F;CO2&#x2F;温度&#x2F;湿度等数据以CSV格式周期性发送至PC。</p><p>于是可以编写如下脚本，用于解析检测仪提供的数据并将其发送至Pushgateway：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> time<br><span class="hljs-keyword">from</span> datetime <span class="hljs-keyword">import</span> datetime<br><span class="hljs-keyword">import</span> serial<br><span class="hljs-keyword">import</span> socket<br><span class="hljs-keyword">import</span> requests<br><br>PORT = <span class="hljs-string">&#x27;/dev/ttyUSB0&#x27;</span><br>BRATE = <span class="hljs-number">57600</span><br>HOST = socket.gethostname()<br>alias = &#123;<span class="hljs-number">0</span>: <span class="hljs-string">&#x27;0.3um&#x27;</span>, <span class="hljs-number">1</span>: <span class="hljs-string">&#x27;pm2.5&#x27;</span>, <span class="hljs-number">2</span>: <span class="hljs-string">&#x27;hcho&#x27;</span>, <span class="hljs-number">3</span>: <span class="hljs-string">&#x27;co2&#x27;</span>, <span class="hljs-number">4</span>: <span class="hljs-string">&#x27;temp&#x27;</span>, <span class="hljs-number">5</span>: <span class="hljs-string">&#x27;humidity&#x27;</span>&#125;<br><br>session = requests.Session()<br>device = serial.Serial(port=PORT, baudrate=BRATE, timeout=<span class="hljs-number">2</span>)<br><span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:<br>    time.sleep(<span class="hljs-number">0.1</span>)<br>    <span class="hljs-keyword">if</span> device.in_waiting &gt; <span class="hljs-number">0</span>:<br>        text = device.readline().decode(<span class="hljs-string">&#x27;utf-8&#x27;</span>).strip()  <span class="hljs-comment"># type: <span class="hljs-built_in">str</span></span><br>        lines = [<span class="hljs-string">&#x27;# TYPE b36_air_monitor gauge&#x27;</span>]<br>        <span class="hljs-keyword">for</span> i, val <span class="hljs-keyword">in</span> <span class="hljs-built_in">enumerate</span>(text.split(<span class="hljs-string">&#x27;,&#x27;</span>)):<br>            name = alias.get(i)<br>            <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> name:<br>                <span class="hljs-keyword">continue</span><br>            lines.append(<br>                <span class="hljs-string">f&#x27;b36_air_monitor&#123;&#123;instance=&quot;<span class="hljs-subst">&#123;PORT&#125;</span>&quot;,alias=&quot;<span class="hljs-subst">&#123;name&#125;</span>&quot;,index=&quot;<span class="hljs-subst">&#123;i&#125;</span>&quot;&#125;&#125; <span class="hljs-subst">&#123;val&#125;</span>&#x27;</span>)<br>        lines.append(<span class="hljs-string">&#x27;&#x27;</span>)<br>        url = <span class="hljs-string">f&#x27;http://127.0.0.1:9091/metrics/job/<span class="hljs-subst">&#123;HOST&#125;</span>&#x27;</span>  <span class="hljs-comment"># Pushgateway地址</span><br>        <span class="hljs-keyword">try</span>:<br>            session.post(url, data=<span class="hljs-string">&#x27;\n&#x27;</span>.join(lines))<br>        <span class="hljs-keyword">except</span> Exception <span class="hljs-keyword">as</span> ex:<br>            <span class="hljs-built_in">print</span>(datetime.now())<br>            <span class="hljs-built_in">print</span>(<span class="hljs-built_in">str</span>(ex))<br></code></pre></td></tr></table></figure><p>运行后可读取Pushgateway的输出进行验证（由于笔者购买的检测仪没有搭配甲醛模块，所以hcho的值始终为0）：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">$ </span><span class="language-bash">curl 127.0.0.1:9091/metrics</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">TYPE b36_air_monitor gauge</span><br>b36_air_monitor&#123;alias=&quot;0.3um&quot;,index=&quot;0&quot;,instance=&quot;/dev/ttyUSB0&quot;,job=&quot;nas&quot;&#125; 250<br>b36_air_monitor&#123;alias=&quot;co2&quot;,index=&quot;3&quot;,instance=&quot;/dev/ttyUSB0&quot;,job=&quot;nas&quot;&#125; 687<br>b36_air_monitor&#123;alias=&quot;hcho&quot;,index=&quot;2&quot;,instance=&quot;/dev/ttyUSB0&quot;,job=&quot;nas&quot;&#125; 0<br>b36_air_monitor&#123;alias=&quot;humidity&quot;,index=&quot;5&quot;,instance=&quot;/dev/ttyUSB0&quot;,job=&quot;nas&quot;&#125; 28<br>b36_air_monitor&#123;alias=&quot;pm2.5&quot;,index=&quot;1&quot;,instance=&quot;/dev/ttyUSB0&quot;,job=&quot;nas&quot;&#125; 1<br>b36_air_monitor&#123;alias=&quot;temp&quot;,index=&quot;4&quot;,instance=&quot;/dev/ttyUSB0&quot;,job=&quot;nas&quot;&#125; 26<br></code></pre></td></tr></table></figure><h2 id="3-2-接入Prometheus"><a href="#3-2-接入Prometheus" class="headerlink" title="3.2 接入Prometheus"></a>3.2 接入Prometheus</h2><p>数据上报到Pushgateway后，下一步是由Prometheus进行抓取。在<code>scrape_configs</code>下增加配置即可：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">scrape_configs:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-attr">job_name:</span> <span class="hljs-string">&quot;pushgateway&quot;</span><br>    <span class="hljs-attr">honor_labels:</span> <span class="hljs-literal">true</span><br>    <span class="hljs-attr">static_configs:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">targets:</span> [<span class="hljs-string">&quot;localhost:9091&quot;</span>]<br></code></pre></td></tr></table></figure><p>重启Prometheus后可在Web页面验证：</p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206184333163.webp"></p><h2 id="3-3-配置告警"><a href="#3-3-配置告警" class="headerlink" title="3.3 配置告警"></a>3.3 配置告警</h2><p>从笔者查阅的资料来看，PM2.5浓度小于35μg&#x2F;m3、CO2浓度小于1000ppm是比较舒适的室内空气标准<sup id="fnref:5" class="footnote-ref"><a href="#fn:5" rel="footnote"><span class="hint--top hint--rounded" aria-label="[室內空氣品質標準 – 浦羅特官方討論區](https://purotechbogger.wordpress.com/air/)">[5]</span></a></sup>：</p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206190207027.webp"></p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/e59c96e789873737381.webp"></p><p>于是笔者基于这两个数值为抓取到的数据配置了告警。首先是在<a href="https://github.com/prometheus/alertmanager/releases">releases页</a>下载最新Alertmanager并用<a href="http://supervisord.org/index.html">Supervisor</a>工具运行，配置文件如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">route:</span><br>  <span class="hljs-attr">group_by:</span> [<span class="hljs-string">&#x27;alertname&#x27;</span>]<br>  <span class="hljs-attr">receiver:</span> <span class="hljs-string">&#x27;tg&#x27;</span><br><span class="hljs-attr">receivers:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">&#x27;tg&#x27;</span><br>    <span class="hljs-attr">telegram_configs:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-attr">api_url:</span> <span class="hljs-string">&quot;https://api.telegram.org&quot;</span><br>      <span class="hljs-attr">bot_token:</span> <span class="hljs-string">&quot;xx:xx&quot;</span><br>      <span class="hljs-attr">chat_id:</span> <span class="hljs-string">xxx</span><br>      <span class="hljs-attr">parse_mode:</span> <span class="hljs-string">&quot;HTML&quot;</span><br>      <span class="hljs-attr">http_config:</span><br>        <span class="hljs-attr">proxy_url:</span> <span class="hljs-string">&quot;xxx&quot;</span><br></code></pre></td></tr></table></figure><p>然后是在Promethues的配置中加入告警规则：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># prometheus.yml</span><br><span class="hljs-attr">rule_files:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;prometheus_alert.yml&quot;</span><br></code></pre></td></tr></table></figure><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># prometheus_alert.yml</span><br><span class="hljs-attr">groups:</span><br><span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">air_monitor</span><br>  <span class="hljs-attr">rules:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-attr">alert:</span> <span class="hljs-string">HighCO2</span><br>    <span class="hljs-attr">expr:</span> <span class="hljs-string">avg(b36_air_monitor&#123;job=&quot;nas&quot;,instance=&quot;/dev/ttyUSB0&quot;,alias=&quot;co2&quot;&#125;)</span> <span class="hljs-string">&gt;</span> <span class="hljs-number">1000</span><br>    <span class="hljs-attr">for:</span> <span class="hljs-string">10m</span><br>    <span class="hljs-attr">annotations:</span><br>      <span class="hljs-attr">summary:</span> <span class="hljs-string">&quot;室内CO2浓度超过1000&quot;</span><br><br>  <span class="hljs-bullet">-</span> <span class="hljs-attr">alert:</span> <span class="hljs-string">HighPM2.5</span><br>    <span class="hljs-attr">expr:</span> <span class="hljs-string">avg(b36_air_monitor&#123;job=&quot;nas&quot;,instance=&quot;/dev/ttyUSB0&quot;,alias=&quot;pm2.5&quot;&#125;)</span> <span class="hljs-string">&gt;</span> <span class="hljs-number">35</span><br>    <span class="hljs-attr">for:</span> <span class="hljs-string">10m</span><br>    <span class="hljs-attr">annotations:</span><br>      <span class="hljs-attr">summary:</span> <span class="hljs-string">&quot;室内PM2.5浓度超过35&quot;</span><br></code></pre></td></tr></table></figure><p>当过去10分钟PM2.5浓度&#x2F;CO2浓度超过设定值时，Promethues会通过Alertmanager向笔者的Telegram发送告警。</p><h1 id="4-效果展示"><a href="#4-效果展示" class="headerlink" title="4. 效果展示"></a>4. 效果展示</h1><p>最后来看看效果：</p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206191154479.webp" alt="Grafana监控看板"></p><p><img src="https://picutres-1251933829.cos.ap-beijing.myqcloud.com/image-20221206191249249.webp" alt="Telegram告警"></p><p>笔者表示很满意！</p><p>PS：其实从上图的监控可以看出，笔者的室内PM2.5&#x2F;CO2浓度都比较低，这是因为笔者在写下这篇文章时已经通过一些手段大幅改善空气状况了，笔者会在后续的文章进行分享~</p><h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><section class="footnotes"><div class="footnote-list"><ol><li><span id="fn:1" class="footnote-text"><span><a href="https://www.bilibili.com/video/BV1A4411u7gg/">新风消费者报告 | 爱否出品_哔哩哔哩_bilibili</a><a href="#fnref:1" rev="footnote" class="footnote-backref"> ↩</a></span></span></li><li><span id="fn:2" class="footnote-text"><span><a href="https://zh.wikipedia.org/wiki/Template:%E5%8C%97%E4%BA%AC%E5%B8%82%E6%B0%94%E5%80%99%E6%95%B0%E6%8D%AE">模板:北京市气候数据 - 维基百科，自由的百科全书</a><a href="#fnref:2" rev="footnote" class="footnote-backref"> ↩</a></span></span></li><li><span id="fn:3" class="footnote-text"><span><a href="https://www.aqistudy.cn/historydata/monthdata.php?city=%E5%8C%97%E4%BA%AC">北京空气质量指数AQI_PM2.5月统计历史数据_中国空气质量在线监测分析平台历史数据</a><a href="#fnref:3" rev="footnote" class="footnote-backref"> ↩</a></span></span></li><li><span id="fn:4" class="footnote-text"><span><a href="https://github.com/JoeyZheng/Lewei365B35">JoeyZheng&#x2F;Lewei365B35：一个简单的应用程序，从365 B35获取数据，然后发送到Lewei50平台，用于网页和微信的数据监控和跟踪。</a><a href="#fnref:4" rev="footnote" class="footnote-backref"> ↩</a></span></span></li><li><span id="fn:5" class="footnote-text"><span><a href="https://purotechbogger.wordpress.com/air/">室內空氣品質標準 – 浦羅特官方討論區</a><a href="#fnref:5" rev="footnote" class="footnote-backref"> ↩</a></span></span></li></ol></div></section>]]></content>
    
    
    
    <tags>
      
      <tag>监控</tag>
      
      <tag>生活</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>用Afterburner和Grafana监控PC性能</title>
    <link href="/2022/08/20/%E7%94%A8Afterburner%E5%92%8CGrafana%E7%9B%91%E6%8E%A7PC%E6%80%A7%E8%83%BD/"/>
    <url>/2022/08/20/%E7%94%A8Afterburner%E5%92%8CGrafana%E7%9B%91%E6%8E%A7PC%E6%80%A7%E8%83%BD/</url>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>监控Linux服务器性能已经有比较成熟的解决方案了：笔者用的是<code>node_exporter</code>+<code>Prometheus</code>+<code>Grafana</code>的经典三件套，整体效果令人满意：</p><table><thead><tr><th>整体架构</th><th>Grafana面板</th></tr></thead><tbody><tr><td><img src="/images/2022/8/20/43f3ee1d6e7cc42258812b94b31292d3.jpg"></td><td><img src="/images/2022/8/20/5315d9b41fb01e5f42efbcdb90c33b38.jpg"></td></tr></tbody></table><p>同时笔者在使用PC时偶尔还会使用<a href="https://www.msi.com/Landing/afterburner/graphics-cards">MSI Afterburner</a>（以下简称Afterburner）来观察PC性能：</p><p><img src="/images/2022/8/20/be56457ed0e1952818d51b2009053c09.jpg"></p><p>但Afterburner的监控功能并不易用（特别是在高分辨率下），如果能将监控数据在Grafana上呈现那就太棒了。<a href="https://github.com/RafhaanShah/grAfterburner">RafhaanShah&#x2F;grAfterburner</a>这个开源项目做到了这点，其使用的是方案是<a href="https://collectd.org/">collectd</a>+<a href="https://graphiteapp.org/">Graphite</a>+<a href="https://grafana.com/">Grafana</a>，整体用<code>docker-compose</code>启动。不过由于笔者已经有了另一套基于<code>Prometheus</code>的监控方案，所以笔者决定新造个轮子来复用现有的监控组件。</p><h1 id="获取数据"><a href="#获取数据" class="headerlink" title="获取数据"></a>获取数据</h1><p>Afterburner本体在<a href="https://www.msi.com/Landing/afterburner/graphics-cards">官网</a>下载并安装即可，但Afterburner不直接对外提供监控数据，而是通过一个叫<code>MSI Afterburner Remote Server</code>的小工具来做到。这个工具是为了在手机上监控PC性能而开发的，但因为种种原因官网已经不再提供其下载入口，最新版本也停留在2011年开发的V1.2。万幸的是我们可以从第三方网站下载到这个工具（<a href="https://drive.google.com/file/d/1zRZQ8_aEQcKwld3uavyT0aFi3b4sux-R/view">google driver</a>、<a href="https://github.com/wolf-joe/data-backup/tree/main/software/msi-afterburner">github</a>）。</p><p>运行Afterburner后，点击<code>MSI Afterburner Remote Server</code>的主程序，该工具会在后台运行，并在托盘区域内展示密码、端口（默认82）等信息：</p><p><img src="/images/2022/8/20/c9934a2d9cba41ce121e27231caa7980.jpg" alt="image-20220820162024028"></p><p>在浏览器中打开<code>127.0.0.1:82/mahm</code>，输入用户名<code>MSIAfterburner</code>和上面的密码即可看到对应的监控数据：</p><p><img src="/images/2022/8/20/b93b658e283490dbfd7b6b26845e07cc.jpg" alt="image-20220820162505895"></p><h1 id="转换数据格式"><a href="#转换数据格式" class="headerlink" title="转换数据格式"></a>转换数据格式</h1><p><code>MSI Afterburner Remote Server</code>提供的监控数据是xml格式的，要想适配<code>Prometheus</code>，得做一层转换才行。笔者选用Golang实现一个HTTP正向代理小工具来完成这层转换，逻辑很简单：接收<code>Prometheus</code>发出的数据采集请求、从<code>MSI Afterburner Remote Server</code>处获取监控数据、将监控数据以metrics格式进行返回。</p><!---@startumlparticipant Afterburner as dataparticipant proxyparticipant Prometheus as pmpm -> proxy: GET /metricsproxy -> data: GET /mahmdata --\> proxy: xmlproxy -> proxy: convertproxy --\> pm@enduml--><p><img src="/images/2022/8/20/d3ce4a5f1aeea651a6ab8534e7965d3d.jpg" alt="PlantUML diagram"></p><p>该工具已在<a href="https://github.com/wolf-joe/tools/tree/main/cmd/afterburner_exporter">GitHub</a>上开源，安装运行后可直接通过浏览器看到工具输出的结果：</p><pre><code class="shell">$ go install github.com/wolf-joe/tools/cmd/afterburner_exporter@latest$ afterburner_exporter -listen &quot;0.0.0.0:8090&quot; -target &quot;127.0.0.1:82&quot; -password &quot;17cc95b4017d496f82&quot;2022/08/20 18:13:08 listen on :8090</code></pre><p><img src="/images/2022/8/20/0701aeb276daaf249d1ac1ea26062661.jpg" alt="image-20220820185928484"></p><h1 id="接入Prometheus"><a href="#接入Prometheus" class="headerlink" title="接入Prometheus"></a>接入Prometheus</h1><p>修改Prometheus的配置文件，让其定时抓取上一章的metrics数据，重启后可看到服务正常接入：</p><pre><code class="yaml">scrape_configs:  ...  - job_name: &quot;desktop&quot;    static_configs:      - targets: [&quot;localhost:9102&quot;] # 这里将PC上的127.0.0.1:8090转发到了Prometheus服务器的:9102</code></pre><p><img src="/images/2022/8/20/815351595b418e04648bacc311c011b7.jpg" alt="image-20220820190351844"></p><h1 id="配置Grafana"><a href="#配置Grafana" class="headerlink" title="配置Grafana"></a>配置Grafana</h1><p>Prometheus抓取到数据之后，在Grafana上添加对应面板：</p><p><img src="/images/2022/8/20/4e0513124777c920c5a47b0f70b7a005.jpg" alt="image-20220821051812112"></p><p>最终效果如下：</p><p><img src="/images/2022/8/20/c6d98f87fc49e13258216134d189871a.jpg" alt="image-20220821051331042"></p><p>大功告成！</p>]]></content>
    
    
    
    <tags>
      
      <tag>Golang</tag>
      
      <tag>监控</tag>
      
      <tag>生活</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Go语言error四问</title>
    <link href="/2021/07/17/Go%E8%AF%AD%E8%A8%80error%E5%9B%9B%E9%97%AE/"/>
    <url>/2021/07/17/Go%E8%AF%AD%E8%A8%80error%E5%9B%9B%E9%97%AE/</url>
    
    <content type="html"><![CDATA[<h1 id="什么是error"><a href="#什么是error" class="headerlink" title="什么是error"></a>什么是error</h1><p>Go语言没有像Java&#x2F;Python一样提供<code>try</code>&amp;<code>catch</code>这种错误捕获方式，而是要求编码者显示地处理下游的传递的错误、显示地向上游抛出错误，也难怪总是有人吐槽Go在这方面的啰嗦。</p><pre><code class="go">var file *os.Filevar err errorfile, err = os.Open(&quot;something.txt&quot;)if err != nil &#123;    // 处理打开文件时的错误，比如panic    panic(file)&#125;// 处理文件fmt.Println(file.Stat())// 处理关闭文件时的错误，比如直接忽略_ = file.Close()</code></pre><p><img src="/images/2021/07/17/most_used_words.png" alt="words.png"></p><p>Go在标准库里提供了一种错误表现形式：<code>error</code>接口，定义很简单：</p><pre><code class="go">package builtin// The error built-in interface type is the conventional interface for// representing an error condition, with the nil value representing no error.type error interface &#123;    Error() string&#125;</code></pre><p>而Go提倡的组合机制让任何实现了<code>Error() string</code>方法的类型都可以视作<code>error</code>，这就带来了极大的灵活性。</p><pre><code class="go">type Error1 stringfunc (e Error1) Error() string &#123; return string(e) &#125;type Error2 struct&#123; msg string &#125;func (e *Error2) SetMsg(msg string) &#123; e.msg = msg &#125;func (e *Error2) Error() string     &#123; return e.msg &#125;var err1 error = Error1(&quot;hello&quot;) // Error1可以被视为Errorvar err2 error = &amp;Error2&#123;msg: &quot;world&quot;&#125; // Error2也可以被视为Error</code></pre><h1 id="如何创建error"><a href="#如何创建error" class="headerlink" title="如何创建error"></a>如何创建error</h1><p>Go原生提供了两种方式来创建一个<code>error</code></p><pre><code class="go">var err1 error = errors.New(&quot;error msg&quot;)var err2 error = fmt.Errorf(&quot;error msg: %s&quot;, &quot;hello&quot;)</code></pre><p>前者比较简单，单纯地创建了一个<code>string</code>的封装类型：</p><pre><code class="go">package errors// New returns an error that formats as the given text.// Each call to New returns a distinct error value even if the text is identical.func New(text string) error &#123; return &amp;errorString&#123;text&#125; &#125;// errorString is a trivial implementation of error.type errorString struct&#123; s string &#125;func (e *errorString) Error() string &#123; return e.s &#125;</code></pre><p>后者在示例代码中也很简单，可以看成<code>fmt.Sprintf</code>+<code>errors.New</code>。当然Go1.13做了点小改进，下文会提到。</p><pre><code class="go">// Go1.12源码package fmt// Errorf formats according to a format specifier and returns the string// as a value that satisfies error.func Errorf(format string, a ...interface&#123;&#125;) error &#123;    return errors.New(Sprintf(format, a...))&#125;</code></pre><p>但这两种方式还是太简陋了，毕竟携带的只是字符串信息。所以在实际应用场景中，更常见的做法是自定义错误类型：</p><pre><code class="go">type BizError struct &#123; // 自定义业务错误    Code int32 // 错误码    Msg  string // 错误提示&#125;func (e *BizError) Error() string &#123; return fmt.Sprintf(&quot;BizError&lt;%d&gt;: %s&quot;, e.Code, e.Msg) &#125;func NewBizErr(code int32, msg string) error &#123; return &amp;BizError&#123;Code: code, Msg: msg&#125; &#125;</code></pre><p>当然，为某些特定场景设计的公用错误（如标准库里的<code>io.EOF</code>）也会使用<code>errors.New</code>，这些错误的意思也足以用简短的字符串描述。</p><h1 id="如何向上游传递error"><a href="#如何向上游传递error" class="headerlink" title="如何向上游传递error"></a>如何向上游传递error</h1><p>由于Go中的错误一般是通过<code>error</code>接口以返回值的形式传递给上游的，且一般不会包含函数调用栈信息，所以在给上游传递<code>error</code>时就需要考虑附加上下文信息以便排查问题，特别是将下游的<code>error</code>传递给上游时。</p><pre><code class="go">func dial80() error &#123;    addr, timeout := &quot;127.0.0.1:80&quot;, time.Second        // do something    if ... &#123;        timeout = time.Second * 3    &#125;    conn, err := net.DialTimeout(&quot;tcp&quot;, addr, timeout)    if err != nil &#123;        return err    &#125;    _ = conn.Close()    return nil&#125;</code></pre><p>比如这个简单的函数，如果上游<strong>有必要</strong>感知出现错误时的上下文信息（比如超时配置），应该怎么处理呢？</p><p>一种简单的方法是将错误信息重新拼装一下，比如：</p><pre><code class="go">func dial80() error &#123;    ...    if err != nil &#123;        // 对error来说，%s会调用.Error()方法        return fmt.Errorf(&quot;dial80 timeout %s: %s&quot;, timeout, err)    &#125;    ...&#125;</code></pre><p>另一种方法是将<code>error</code>再包装一层：</p><pre><code class="go">type dial80Err struct &#123;    timeout time.Duration    err     error&#125;func (e *dial80Err) Error() string &#123; return fmt.Sprintf(&quot;dial80 timeout %s: %s&quot;, e.timeout, e.err) &#125;func dial80() error &#123;    ...    if err != nil &#123;        return &amp;dial80Err&#123;timeout: timeout, err: err&#125;    &#125;    ...&#125;</code></pre><p>但两种方法都并不完美。前者会丢失<code>error</code>中除<code>.Error()</code>返回值外的其它信息，后者用起来很繁琐，有没有更好的方法呢？当然是有的。Go1.13引入了<em>Error wrapping</em>的概念，通过扩展<code>fmt.Errorf</code>来包装<code>error</code>：</p><pre><code class="go">func dial80() error &#123;    ...    if err != nil &#123;        return fmt.Errorf(&quot;dial80 timeout %s: %w&quot;, timeout, err)    &#125;    ...&#125;</code></pre><p>注意其中<code>%w</code>占位符，<code>fmt.Errorf</code>会特殊处理这个占位符，如果存在这个占位符，<code>fmt.Errorf</code>会将其对应的<code>error</code>与错误信息一起放入一个新的结构体里：</p><pre><code class="go">// Go1.13源码package fmtfunc Errorf(format string, a ...interface&#123;&#125;) error &#123;    ...    if p.wrappedErr == nil &#123;        err = errors.New(s)    &#125; else &#123;        err = &amp;wrapError&#123;s, p.wrappedErr&#125;    &#125;    ...&#125;type wrapError struct &#123;    msg string    err error&#125;func (e *wrapError) Error() string &#123; return e.msg &#125;func (e *wrapError) Unwrap() error &#123; return e.err &#125;</code></pre><p>这种方法综合了前两种的优点，即使用便捷又不会丢失信息。所以在Go1.13之后，非常推荐使用<code>%w</code>占位符来为错误附加上下文信息并传递给上游。</p><h1 id="如何分析下游传递的error"><a href="#如何分析下游传递的error" class="headerlink" title="如何分析下游传递的error"></a>如何分析下游传递的error</h1><p>当我们得到下游传递的<code>error</code>且需要根据其类型、属性做进一步操作时，该怎么办呢？（当然，前提是下游不能使用<code>errors.New</code>或<code>fmt.Errorf</code>+<code>%s</code>来传递错误，笑）</p><p>由于下游可能传递<code>wrapError</code>给上游，所以上游并不能直接对<code>error</code>进行比较运算（<code>==</code>）、类型断言，而是要用<code>errors</code>包提供的API进行分析。</p><p>比如，用<code>errors.Is</code>替代<code>==</code>运算符来判断两个<code>error</code>是否相等：</p><pre><code class="go">myRead := func() ([]byte, error) &#123;    ...    if ... &#123;        return nil, fmt.Errorf(&quot;myRead: %w&quot;, io.EOF)    &#125;    ...&#125;buf, err := myRead()if errors.Is(err, io.EOF) &#123;    // do something&#125;</code></pre><p>又比如，用<code>errors.As</code>替代<code>.(type)</code>来做类型断言：</p><pre><code class="go">err := dial80()if err != nil &#123;    var opErr *net.OpError    // 判断err是否为超时错误    if ok := errors.As(err, &amp;opErr); ok &amp;&amp; opErr.Timeout() &#123;        // do something    &#125;    // do something&#125;</code></pre><h1 id="github-com-x2F-pkg-x2F-errors"><a href="#github-com-x2F-pkg-x2F-errors" class="headerlink" title="github.com&#x2F;pkg&#x2F;errors"></a>github.com&#x2F;pkg&#x2F;errors</h1><p>标准库中<code>errors.Is</code>和<code>errors.As</code>两个函数需要频繁使用性能较差的反射，<code>errors.As</code>使用起来也很繁琐，所以如果有条件的话可以使用功能更丰富的<a href="https://github.com/pkg/errors/">github.com&#x2F;pkg&#x2F;errors</a>包，例子如下：</p><pre><code class="go">// import github.com/pkg/errorsmyRead := func() ([]byte, error) &#123;    ...    if ... &#123;        // 效果类似 fmt.Errorf(&quot;myRead: %w&quot;, io.EOF)        return nil, errors.WithMessage(io.EOF, &quot;myRead&quot;)    &#125;    ...&#125;buf, err := myRead()if errors.Is(err, io.EOF) &#123;    // do something&#125;// 谨慎使用if errors.Cause(err) == io.EOF &#123;    // do something&#125;</code></pre><p>使用时在下游通过<code>errors.WithMessage</code>&#x2F;<code>errors.WithMessagef</code>包装错误，在上游通过<code>errors.Cause</code>获取被包装的原始错误。不过<code>errors.Cause</code>不能穿透被<code>fmt.Errorf</code>+<code>%w</code>包装的<code>wrapError</code>，所以使用时一定要谨慎。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><ol><li>Go中的<code>error</code>是一个只需实现<code>Error() string</code>方法的接口。</li><li>除非是公用错误，否则尽量使用自定义错误类型来生成错误，而不是使用<code>errors.New</code>和<code>fmt.Errorf</code>。</li><li>使用<code>fmt.Errorf</code>与<code>%w</code>来为错误附加上下文信息。</li><li>使用<code>errors.Is</code>、<code>errors.As</code>来分析错误。</li><li>如有条件，可使用更优秀的<a href="https://github.com/pkg/errors/">github.com&#x2F;pkg&#x2F;errors</a>包替代标准库的<code>errors</code>包和<code>fmt.Errorf</code>。</li></ol>]]></content>
    
    
    
    <tags>
      
      <tag>Golang</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>读《流畅的Python》：一等函数</title>
    <link href="/2020/07/05/fluent-python-chap-5/"/>
    <url>/2020/07/05/fluent-python-chap-5/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>一等对象（<code>first-class objects</code>）是指拥有如下特性的程序实体：</p><ul><li>在运行时创建</li><li>能赋值给变量或数据结构中的元素</li><li>能作为参数传给函数</li><li>能作为函数的返回结果</li></ul><p>Python中的函数拥有这几个特性，所以被称作<strong>一等函数</strong>（<code>functions as first-class objects</code>，简称<code>first-class functions</code>）。</p><h1 id="把函数视作对象"><a href="#把函数视作对象" class="headerlink" title="把函数视作对象"></a>把函数视作对象</h1><pre><code class="python">&gt;&gt;&gt; def foo(x):  # 在运行时创建...     &quot;&quot;&quot;return x * x&quot;&quot;&quot;...     return x * x... &gt;&gt;&gt; bar = foo  # 赋值给变量&gt;&gt;&gt; bar.__doc__&#39;return x * x&#39;&gt;&gt;&gt; print(bar)  # 作为参数传给函数&lt;function foo at ...&gt;&gt;&gt;&gt; def fooo(): return foo... &gt;&gt;&gt; fooo()(10)  # 作为函数的返回结果100</code></pre><h1 id="高阶函数"><a href="#高阶函数" class="headerlink" title="高阶函数"></a>高阶函数</h1><p>接受函数为参数，或把函数作为结果返回的函数是<strong>高阶函数</strong>，比如上文提到的<code>print()</code>和<code>fooo()</code>。再比如接收函数作为<code>key</code>参数的<code>sorted()</code>：</p><pre><code class="python">&gt;&gt;&gt; sorted([-2, -1, 0, 1, 2], key=abs)[0, -1, 1, -2, 2]</code></pre><p>函数式编程语言一般会提供<code>map</code>、<code>filter</code>、<code>reduce</code>三个高阶函数。Python也提供这三个函数，但列表推导（<code>list comprehensions</code>）和生成器表达式（<code>generator expressions</code>）在完成和<code>map</code>、<code>filter</code>相同的功能时代码可读性更好：</p><pre><code class="python">&gt;&gt;&gt; list(map(foo, range(6)))[0, 1, 4, 9, 16, 25]&gt;&gt;&gt; [foo(x) for x in range(6)][0, 1, 4, 9, 16, 25]&gt;&gt;&gt;&gt;&gt;&gt; list(map(foo, filter(lambda x: x &gt; 2, range(6))))  # 用lambda关键字创造了一个匿名函数[9, 16, 25]&gt;&gt;&gt; [foo(x) for x in range(6) if x &gt; 2][9, 16, 25]</code></pre><p>如果是执行求和操作，也可以用内置的<code>sum</code>函数替代<code>reduce</code>，性能和可读性更好：</p><pre><code class="python">&gt;&gt;&gt; from functools import reduce  # Python3起，reduce不再是内置函数&gt;&gt;&gt; from operator import add  # add 相当于 lambda a, b: a + b&gt;&gt;&gt; reduce(add, range(100))4950&gt;&gt;&gt; sum(range(100))4950</code></pre><p>和<code>sum</code>和<code>reduce</code>的思路类似，<code>all</code>和<code>any</code>也是两个内置的归约函数：</p><ul><li><code>all(iterable)</code>：如果每个元素都为真，返回<code>True</code>。<code>all([])</code>返回<code>True</code>。</li><li><code>any(iterable)</code>：如任意一个元素为真，返回<code>True</code>。<code>any([])</code>返回<code>False</code>。</li></ul><h1 id="匿名函数"><a href="#匿名函数" class="headerlink" title="匿名函数"></a>匿名函数</h1><p>用<code>lambda</code>关键字创造的函数叫做<strong>匿名函数</strong>。匿名函数只能使用纯表达式，函数内不能赋值，也不能使用<code>while</code>、<code>try</code>等语句。匿名函数一般只作为参数传递给高阶函数，就像上一节的示例那样。</p><p>从<code>Pythonic</code>的角度来说，匿名函数最好不要超过一行，也不推荐将匿名函数赋值给其它变量。</p><h1 id="可调用对象"><a href="#可调用对象" class="headerlink" title="可调用对象"></a>可调用对象</h1><p>能被调用运算符（即<code>()</code>）应用的对象被称为<strong>可调用对象</strong>，这点可以通过内置的<code>callable</code>函数来判断。<a href="https://docs.python.org/3/reference/datamodel.html">Python数据模型</a>文档列出了7种可调用对象：</p><ul><li>用户定义的函数（<code>User-defined functions</code>），使用<code>def</code>语句和<code>lambda</code>表达式创建的函数。</li><li>方法（<code>Instance methods</code>），在类的定义体中定义的函数。</li><li>生成器函数（<code>Generator functions</code>），使用<code>yield</code>关键字的函数或方法。调用生成器函数会返回生成器对象。</li><li>内置函数（<code>Built-in functions</code>）、内置方法（<code>Built-in methods</code>），用C语言实现的函数&#x2F;方法，如<code>len()</code>、<code>alist.append()</code>。</li><li>类（<code>Classes</code>），调用类时会运行类的<code>__new__</code>方法创建实例，然后运行<code>__init__</code>方法初始化实例。</li><li>类的实例（<code>Class Instances</code>），定义了<code>__call__</code>方法的类的实例。</li></ul><p>当然，《流畅的Python》基于Python3.4，后续的Python版本还引入了其它种类的可调用对象，如协程函数（<code>Coroutine functions</code>）和异步生成器函数（<code>Asynchronous generator functions</code>），详见前文的文档链接。</p><pre><code class="python">&gt;&gt;&gt; abs, str, 13(&lt;built-in function abs&gt;, &lt;class &#39;str&#39;&gt;, 13)&gt;&gt;&gt; [callable(obj) for obj in (abs, str, 13)][True, True, False]</code></pre><h1 id="函数内省"><a href="#函数内省" class="headerlink" title="函数内省"></a>函数内省</h1><p><strong>内省</strong>指程序在运行时检查对象类型的一种能力，在Python中，函数提供许多属性来实现内省。函数特有的属性主要有如下几种：</p><table><thead><tr><th>名称</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>__annotations__</code></td><td>dict</td><td>参数和返回值的注解</td></tr><tr><td><code>__call__</code></td><td>method-wrapper</td><td>实现<code>()</code>运算符</td></tr><tr><td><code>__closure__</code></td><td>tuple</td><td>函数闭包，即自由变量的绑定</td></tr><tr><td><code>__code__</code></td><td>code</td><td>编译成字节码的函数元数据和函数定义体</td></tr><tr><td><code>__defaults__</code></td><td>tuple</td><td>形参的默认值</td></tr><tr><td><code>__get__</code></td><td>method-wraper</td><td>实现<a href="https://docs.python.org/3/howto/descriptor.html">只读描述符协议</a></td></tr><tr><td><code>__globals__</code></td><td>dict</td><td>函数所在模块的全局变量</td></tr><tr><td><code>__kwdefaults__</code></td><td>dict</td><td>仅限关键字形参的默认值</td></tr><tr><td><code>__name__</code></td><td>str</td><td>函数名称</td></tr><tr><td><code>__qualname__</code></td><td>str</td><td>函数的<a href="https://www.python.org/dev/peps/pep-3155/">限定名称</a>，如<code>Random.choice</code></td></tr></tbody></table><h1 id="函数参数、获取关于参数的信息"><a href="#函数参数、获取关于参数的信息" class="headerlink" title="函数参数、获取关于参数的信息"></a>函数参数、获取关于参数的信息</h1><p>详见<a href="/2020/07/04/python-parameters-and-arguments/">上一篇文章</a></p><h1 id="函数注解"><a href="#函数注解" class="headerlink" title="函数注解"></a>函数注解</h1><p>Python3提供的新语法<code>函数注解</code>可以为函数声明的参数和返回值附加元数据，如：</p><pre><code class="python">def foo(num_a: &#39;int &gt; 0&#39;, num_b: str = &#39;123&#39;) -&gt; float:    return (num_a + int(num_b)) / 2</code></pre><p>函数中的参数可以在<code>:</code>之后增加注解表达式。如参数有默认值，则表达式放在参数和<code>=</code>号之间。函数末尾的<code>)</code>和<code>:</code>之间也可以放入表达式，用于注解返回值。注解不会做任何处理，只是储存在函数的<code>__annotations__</code>属性里：</p><pre><code class="python">&gt;&gt;&gt; foo.__annotations__&#123;&#39;num_a&#39;: &#39;int &gt; 0&#39;, &#39;num_b&#39;: &lt;class &#39;str&#39;&gt;, &#39;return&#39;: &lt;class &#39;float&#39;&gt;&#125;</code></pre><p>注解只是元数据，可以供IDE、框架、装饰器、静态代码分析工具等使用。标准库中只有上一节提到的<code>inspect.signature</code>会用到注解，如：</p><pre><code class="python">&gt;&gt;&gt; from inspect import signature&gt;&gt;&gt; sig = signature(foo)&gt;&gt;&gt; sig.return_annotation&lt;class &#39;float&#39;&gt;&gt;&gt;&gt; for param in sig.parameters.values():...     anno = str(param.annotation)...     print(f&#39;&#123;anno:&lt;13&#125; : &#123;param.name&#125; = &#123;param.default&#125;&#39;)... int &gt; 0       : num_a = &lt;class &#39;inspect._empty&#39;&gt;&lt;class &#39;str&#39;&gt; : num_b = 123</code></pre><h2 id="用注解实现参数校验"><a href="#用注解实现参数校验" class="headerlink" title="用注解实现参数校验"></a>用注解实现参数校验</h2><p>Python大牛<code>Raymond Hettinger</code>在一个<a href="https://stackoverflow.com/questions/3038033/what-are-good-uses-for-python3s-function-annotations">StackOverflow问答</a>里展示了一种方便的、基于注解的参数校验机制，摘录如下：</p><pre><code class="python">def validate(func, locals):    for var, test in func.__annotations__.items():        value = locals[var]        msg = &#39;Var: &#123;0&#125;\tValue: &#123;1&#125;\tTest: &#123;2.__name__&#125;&#39;.format(var, value, test)        assert test(value), msgdef is_int(x):    return isinstance(x, int)def between(lo, hi):    def _between(x):        return lo &lt;= x &lt;= hi    return _betweendef f(x: between(3, 10), y: is_int):    validate(f, locals())    print(x, y)</code></pre><pre><code class="python">&gt;&gt;&gt; f(3, 1)3 1&gt;&gt;&gt; f(0, 31.1)Traceback (most recent call last):  ...AssertionError: Var: xValue: 0Test: _between</code></pre><p>其基本思想是用<code>Callable</code>对象充当函数参数的注解，然后在函数被调用时调用<code>Callable</code>对象来校验参数。换句话说，上面的函数<code>f</code>可以改写成如下形式：</p><pre><code class="python">def f(x, y):    assert 3 &lt;= x &lt;= 10, f&#39;Var: xValue: &#123;x&#125;Test: _between&#39;    assert isinstance(y, int), f&#39;Var: yValue: &#123;y&#125;Test: is_int&#39;    print(x, y)</code></pre><p>由此可见前文参数校验机制的精妙。</p><h1 id="支持函数式编程的模块"><a href="#支持函数式编程的模块" class="headerlink" title="支持函数式编程的模块"></a>支持函数式编程的模块</h1><p>在<code>operator</code>和<code>functools</code>等模块的支持下，Python也可以很方便地实现函数式编程风格。</p><h2 id="operator模块"><a href="#operator模块" class="headerlink" title="operator模块"></a>operator模块</h2><p><code>operator</code>模块为许多算数运算符提供了对应的函数，如<code>add</code>（对应<code>a + b</code>）、<code>mul</code>（对应<code>a * b</code>）等等。比如计算<code>1~n</code>的和与<code>n</code>的阶乘：</p><pre><code class="python">&gt;&gt;&gt; from functools import reduce&gt;&gt;&gt; from operator import add, mul&gt;&gt;&gt; n = 10&gt;&gt;&gt; reduce(add, range(1, n+1))  # 1 + 2 + ... + 1055&gt;&gt;&gt; reduce(mul, range(1, n+1))  # 1 * 2 * ... * 103628800</code></pre><p><code>operator</code>模块还提供了<code>itemgetter</code>和<code>attrgetter</code>两个函数，可以从序列中提取元素或读取对象的属性。比如：</p><pre><code class="python">&gt;&gt;&gt; records = [(1, &#39;orange&#39;), (2, &#39;apple&#39;), (3, &#39;juice&#39;)]&gt;&gt;&gt; from operator import itemgetter, attrgetter&gt;&gt;&gt; sorted(records, key=itemgetter(1))  # itemgetter(1) 等效于 lambda x: x[1][(2, &#39;apple&#39;), (3, &#39;juice&#39;), (1, &#39;orange&#39;)]&gt;&gt;&gt;&gt;&gt;&gt; attrgetter(&#39;append&#39;)(records)&lt;built-in method append of list object at ...&gt;</code></pre><p>最后介绍<code>methodcaller</code>函数，这个函数创建的函数会调用对象上的指定方法，如：</p><pre><code class="python">&gt;&gt;&gt; from operator import methodcaller&gt;&gt;&gt; records[(1, &#39;orange&#39;), (2, &#39;apple&#39;), (3, &#39;juice&#39;)]&gt;&gt;&gt; append_lemon = methodcaller(&#39;append&#39;, (4, &#39;lemon&#39;))&gt;&gt;&gt; append_lemon(records)  # 等效于 records.append((4, &#39;lemon&#39;))&gt;&gt;&gt; records[(1, &#39;orange&#39;), (2, &#39;apple&#39;), (3, &#39;juice&#39;), (4, &#39;lemon&#39;)]</code></pre><h2 id="用functools-partial冻结参数"><a href="#用functools-partial冻结参数" class="headerlink" title="用functools.partial冻结参数"></a>用functools.partial冻结参数</h2><p>除了前文已经多次提到的<code>reduce</code>，<code>functools</code>模块还提供了许多有用的函数，比如可以冻结函数参数的<code>partial</code>。上一节提到的<code>append_lemon</code>其实就起到了类似<code>partial</code>的作用，但<code>partial</code>的功能更强大。比如前文提到的自定义排序，可以用更通用的形式重写：</p><pre><code class="python">&gt;&gt;&gt; records = [(1, &#39;orange&#39;), (2, &#39;apple&#39;), (3, &#39;juice&#39;)]&gt;&gt;&gt; from operator import itemgetter, attrgetter&gt;&gt;&gt; sorted(records, key=itemgetter(1))  # itemgetter(1) 等效于 lambda x: x[1][(2, &#39;apple&#39;), (3, &#39;juice&#39;), (1, &#39;orange&#39;)]</code></pre><pre><code class="python">&gt;&gt;&gt; from functools import partial&gt;&gt;&gt; sort_by_second = partial(sorted, key=itemgetter(1))&gt;&gt;&gt; sort_by_second(records)[(2, &#39;apple&#39;), (3, &#39;juice&#39;), (1, &#39;orange&#39;)]</code></pre><p>在需要用相同（或类似）的参数反复调用某个函数时，使用<code>functools.partial</code>冻结参数可以有效降低工作量和bug出现的几率。<code>functools</code>还提供了<code>wraps</code>、<code>lru_cache</code>等比较实用的装饰器，这方面的内容可以参考笔者<a href="/2020/06/20/python-decorators-source-code/">先前的文章</a>。</p>]]></content>
    
    
    
    <tags>
      
      <tag>流畅的Python</tag>
      
      <tag>读书笔记</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Python中的函数参数与参数传递</title>
    <link href="/2020/07/04/python-parameters-and-arguments/"/>
    <url>/2020/07/04/python-parameters-and-arguments/</url>
    
    <content type="html"><![CDATA[<blockquote><p>注：本篇文章使用CPython3.6</p></blockquote><h1 id="定位参数、关键字参数"><a href="#定位参数、关键字参数" class="headerlink" title="定位参数、关键字参数"></a>定位参数、关键字参数</h1><p>在Python中，当定义一个函数时，函数接收的参数叫做为形式参数（<code>parameters</code>），以下简称<strong>形参</strong>；当调用一个函数时，调用者传递给该函数的值叫做实际参数（<code>arguments</code>），以下简称<strong>实参</strong>。</p><p>根据<a href="https://docs.python.org/3/library/inspect.html">inspect模块</a>的描述，Python的形参可以分成如下五类：</p><ul><li><em>POSITIONAL_OR_KEYWORD</em>，默认类型，可通过定位&#x2F;关键字实参传递；</li><li><em>VAR_POSITIONAL</em>，定位形参元祖，如<code>*args</code>，捕获剩下的定位实参；</li><li><em>KEYWORD_ONLY</em>，在<code>*</code>或<code>*args</code>之后的形参，只能通过关键字实参传递；</li><li><em>VAR_KEYWORD</em>，关键字形参字典，如<code>**kwargs</code>，捕获剩下的关键字实参；</li><li><em>POSITIONAL_ONLY</em>，只能通过定位实参传递，Python语法暂不支持，只有一些C函数（如<code>divmod</code>）使用。</li></ul><p>比如定义如下函数：</p><pre><code class="python">def foo(a, *args):    print(a, args)</code></pre><p>其中形参<code>a</code>属于<em>POSITIONAL_OR_KEYWORD</em>，可通过定位&#x2F;关键字实参传递：</p><pre><code class="python">&gt;&gt;&gt; foo(1)1 ()&gt;&gt;&gt; foo(a=1)1 ()</code></pre><p>满足形参<code>a</code>之后，剩余的定位实参将被<code>*args</code>以元组的形式捕获：</p><pre><code class="python">&gt;&gt;&gt; foo(1, 2, 3)1 (2, 3)</code></pre><p>再比如定义如下函数：</p><pre><code class="python">def foo(a, *args, b, **kwargs):    print(a, args, b, kwargs)</code></pre><p>形参<code>b</code>属于<em>KEYWORD_ONLY</em>，因为它在<code>*args</code>之后定义：</p><pre><code class="python">&gt;&gt;&gt; foo(1, b=2)1 () 2 &#123;&#125;</code></pre><p>满足形参<code>b</code>之后，剩余的关键字实参将被<code>**kwargs</code>以字典的形式捕获：</p><pre><code class="python">&gt;&gt;&gt; foo(1, b=2, c=3)1 () 2 &#123;&#39;c&#39;: 3&#125;</code></pre><p>如果想定义<em>KEYWORD_ONLY</em>形参，但不想使用<em>VAR_POSITIONAL</em>形参（即<code>*args</code>），则可以在定义函数时单独的<code>*</code>号：</p><pre><code class="python">&gt;&gt;&gt; def foo(a, *, b):...     print(a, b)...&gt;&gt;&gt; foo(1, b=2)1 2&gt;&gt;&gt; foo(1, 2)Traceback (most recent call last):  File &quot;&lt;stdin&gt;&quot;, line 1, in &lt;module&gt;TypeError: foo() takes 1 positional argument but 2 were given</code></pre><h1 id="参数默认值"><a href="#参数默认值" class="headerlink" title="参数默认值"></a>参数默认值</h1><p>在定义函数时，我们可以给形参指定默认值，比如：</p><pre><code class="python">&gt;&gt;&gt; def foo(a=1, *args, b=2, **kwargs):...     print(a, args, b, kwargs)... &gt;&gt;&gt; foo()1 () 2 &#123;&#125;</code></pre><p>需要注意的是，形参的默认值存储在函数对象的<code>__defaults__</code>和<code>__kwdefaults__</code>属性里，而不是每次调用函数时动态生成，所以最好不要用可变对象充当形参的默认值。下面的例子就是反面教材：</p><pre><code class="python">&gt;&gt;&gt; def foo(param=[]):...     param.append(1)...     print(id(param), param)... &gt;&gt;&gt; &gt;&gt;&gt; print(id(foo.__defaults__[0]), foo.__defaults__[0])140009169940232 []&gt;&gt;&gt; foo()140009169940232 [1]&gt;&gt;&gt; foo()140009169940232 [1, 1]</code></pre><h1 id="获取关于参数的信息"><a href="#获取关于参数的信息" class="headerlink" title="获取关于参数的信息"></a>获取关于参数的信息</h1><p><strong>内省</strong>指程序在运行时检查对象类型的一种能力，本节介绍的内容就属于函数内省的范围。假设有如下函数：</p><pre><code class="python">def foo(a=1, *args, b=2, **kwargs):    c = a    print(c, args, b, kwargs)</code></pre><p>就像上一节中提到的，<code>foo</code>函数有<code>__defaults__</code>、<code>__kwdefaults__</code>属性，用于记录定位参数和关键字参数的默认值；有<code>__code__</code>属性，存储函数编译后的字节码信息，其中就包括参数的名称。通过这些属性，我们可以获取关于函数参数的信息：</p><pre><code class="python">&gt;&gt;&gt; foo.__defaults__(1,)&gt;&gt;&gt; foo.__kwdefaults__&#123;&#39;b&#39;: 2&#125;&gt;&gt;&gt; foo.__code__.co_varnames  # 参数&amp;局部变量名称(&#39;a&#39;, &#39;b&#39;, &#39;args&#39;, &#39;kwargs&#39;, &#39;c&#39;)&gt;&gt;&gt; foo.__code__.co_argcount  # 定位参数数量1&gt;&gt;&gt; foo.__code__.co_kwonlyargcount  # 仅限关键字参数数量1</code></pre><p>但这样还是太原始、太不方便了。幸好，我们有更好的选择：Python内置的<code>inspect</code>模块。下面这个例子就提取了<code>foo</code>函数的签名，然后获取函数的参数信息：</p><pre><code class="python">&gt;&gt;&gt; from inspect import signature&gt;&gt;&gt; sig = signature(foo)&gt;&gt;&gt; sig&lt;Signature (a=1, *args, b=2, **kwargs)&gt;&gt;&gt;&gt; for name, param in sig.parameters.items():...     print(f&#39;&#123;str(param.kind):&lt;21&#125; : &#123;param.name:&lt;6&#125; = &#123;param.default&#125;&#39;)... POSITIONAL_OR_KEYWORD : a      = 1VAR_POSITIONAL        : args   = &lt;class &#39;inspect._empty&#39;&gt;KEYWORD_ONLY          : b      = 2VAR_KEYWORD           : kwargs = &lt;class &#39;inspect._empty&#39;&gt;</code></pre><p>同时，<code>inspect.Signature</code>对象还有一个<code>bind</code>方法，该方法可以将一些对象绑定到函数的形参上，就像Python解释器在调用函数时做的那样。通过这种方法，框架可以在真正执行函数前验证参数，就像下面这个例子：</p><pre><code class="python">&gt;&gt;&gt; bound = sig.bind(1, 2, 3, c=3)&gt;&gt;&gt; for name, value in bound.arguments.items():...     print(f&#39;&#123;name:&lt;6&#125; = &#123;value&#125;&#39;)... a      = 1args   = (2, 3)kwargs = &#123;&#39;c&#39;: 3&#125;&gt;&gt;&gt; bound = sig.bind(1, 2, 3, a=4)Traceback (most recent call last):  File ...TypeError: multiple values for argument &#39;a&#39;</code></pre><h1 id="函数参数传递"><a href="#函数参数传递" class="headerlink" title="函数参数传递"></a>函数参数传递</h1><p>说起函数参数传递，可能就有人想起了<code>引用传递</code>、<code>值传递</code>……忘掉这两个概念，来看看下面两个例子：</p><pre><code class="python">def foo1(param: list):    param += [4, 5]arg1 = [1, 2, 3]foo1(arg1)print(arg1)  # 输出[1, 2, 3, 4, 5]</code></pre><p>内存中有一个<code>list</code>对象（<code>[1, 2, 3]</code>），该对象有两个别名：<code>arg1</code>和<code>param</code>。由于<code>list</code>对象是可变的（<code>mutable</code>），所以可以通过<code>param</code>这个别名修改这个<code>list</code>对象的内容。</p><p><img src="/images/2020/07/04/list.webp" alt="tuple.webp"></p><pre><code class="python">def foo2(param: tuple):    param += (4, 5)arg2 = (1, 2, 3)foo2(arg2)print(arg2)  # 输出(1, 2, 3)</code></pre><p>内存中有一个<code>tuple</code>对象（<code>(1, 2, 3)</code>），该对象也有两个别名：<code>arg2</code>和<code>param</code>。但由于<code>tuple</code>对象是不可变的<code>(immutable)</code>，当执行<code>param += (4, 5)</code>时，解释器创建了一个新的<code>tuple</code>对象（<code>(1, 2, 3, 4, 5)</code>），并让<code>param</code>指向这个新的对象，而原来的对象没有被改变。</p><p><img src="/images/2020/07/04/tuple.webp" alt="tuple.webp"></p><p>在Python中，参数传递本质上是为已有的对象取了一个函数作用域级别的别名。如果该对象是可变的，那么就可以在函数内修改该对象，这种修改也可以被其它的别名所感知。弄清楚对象、别名的关系，就不会对<code>值传递</code>、<code>引用传递</code>这种说法感到困惑了。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Python</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Python部分内置函数装饰器源码浅析</title>
    <link href="/2020/06/20/python-decorators-source-code/"/>
    <url>/2020/06/20/python-decorators-source-code/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>这是这个博客里第一篇带有<code>源码</code>Tag的文章。在过去的一个月，笔者深刻地认识到自己在技术深度方面有很大的不足，所以在未来的学习道路上，笔者会尽量多地阅读开源软件的源代码，并将自己的理解以文章的形式表达出来。如有错漏，请各位读者不吝指教。</p><blockquote><p>注：本篇文章所用环境为CPython3.6</p></blockquote><h1 id="functools-wraps"><a href="#functools-wraps" class="headerlink" title="functools.wraps"></a>functools.wraps</h1><p><code>wraps</code>这个内置函数装饰器在笔者的<a href="/2020/06/19/%E8%AF%BB%E3%80%8A%E6%B5%81%E7%95%85%E7%9A%84Python%E3%80%8B%EF%BC%9A%E5%87%BD%E6%95%B0%E8%A3%85%E9%A5%B0%E5%99%A8%E4%B8%8E%E9%97%AD%E5%8C%85/#functool-wraps">上一篇文章</a>里有过介绍，功能很单纯（拷贝<code>__name__</code>、<code>__doc__</code>等属性），源码也很短（不到50行）。</p><h2 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h2><pre><code class="python">def deco(func: Callable) -&gt; Callable:    def inner(*args, **kwargs):        &quot;&quot;&quot;inner doc&quot;&quot;&quot;        print(f&#39;inner(&#123;args&#125;, &#123;kwargs&#125;)&#39;)    return inner@decodef target(name: str):    &quot;&quot;&quot;target doc&quot;&quot;&quot;    print(f&#39;target(name=&#123;name&#125;)&#39;)</code></pre><p>在不用<code>wraps</code>装饰器的情况下，<code>target</code>的<code>__name__</code>和<code>__doc__</code>属性都来自<code>deco()</code>里的<code>inner</code>：</p><pre><code class="python">print(&#39;__doc__  :&#39;, target.__doc__)   # __doc__  : inner docprint(&#39;__name__ :&#39;, target.__name__)  # __name__ : innertarget(name=&#39;bob&#39;)                    # inner((), &#123;&#39;name&#39;: &#39;bob&#39;&#125;)</code></pre><p>这一点也不奇怪，因为此时<code>target</code>是<code>deco()</code>的返回值，其<code>__name__</code>等属性自然也不会是来自被装饰的原始<code>target</code>。但从调用者的角度来说，这是很让人费解的。所以Python内置了<code>wraps</code>装饰器，可以拷贝被装饰函数的部分属性，让装饰后的函数<code>看起来</code>和装饰前一样：</p><pre><code class="python">def deco(func: Callable) -&gt; Callable:    @functools.wraps(func)    def inner(*args, **kwargs):        &quot;&quot;&quot;inner doc&quot;&quot;&quot;        print(f&#39;inner(&#123;args&#125;, &#123;kwargs&#125;)&#39;)    return inner</code></pre><pre><code class="python">print(&#39;__doc__  :&#39;, target.__doc__)   # __doc__  : target docprint(&#39;__name__ :&#39;, target.__name__)  # __name__ : targettarget(name=&#39;bob&#39;)                    # inner((), &#123;&#39;name&#39;: &#39;bob&#39;&#125;)</code></pre><h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h2><p><code>wraps</code>装饰器的入口函数在<code>functools.py</code>的第<a href="https://github.com/python/cpython/blob/3.6/Lib/functools.py#L74">74行</a>，函数很短小：</p><pre><code class="python">def wraps(wrapped,          assigned = WRAPPER_ASSIGNMENTS,          updated = WRAPPER_UPDATES):    &quot;&quot;&quot; ... 省略    &quot;&quot;&quot;    return partial(update_wrapper, wrapped=wrapped,                   assigned=assigned, updated=updated)</code></pre><p><a href="/2020/06/19/%E8%AF%BB%E3%80%8A%E6%B5%81%E7%95%85%E7%9A%84Python%E3%80%8B%EF%BC%9A%E5%87%BD%E6%95%B0%E8%A3%85%E9%A5%B0%E5%99%A8%E4%B8%8E%E9%97%AD%E5%8C%85/#%E5%B8%A6%E5%8F%82%E6%95%B0%E7%9A%84%E5%87%BD%E6%95%B0%E8%A3%85%E9%A5%B0%E5%99%A8">上一篇文章</a>我介绍过，带参数的函数装饰器会返回一个函数，用返回的函数来对目标函数进行装饰。于是，上面提到的<code>deco()</code>函数就可以转换为如下形式：</p><pre><code class="python">from functools import partial, update_wrapper, WRAPPER_ASSIGNMENTS, WRAPPER_UPDATESdef deco(func: Callable) -&gt; Callable:    # @functools.wraps(func)    def inner(*args, **kwargs):        &quot;&quot;&quot;inner doc&quot;&quot;&quot;        print(f&#39;inner(&#123;args&#125;, &#123;kwargs&#125;)&#39;)        # 关键的两行    tmp = partial(update_wrapper, wrapped=func,                  assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES)    inner = tmp(inner)    return inner</code></pre><p>由于<code>functools.partial</code>的功能只是固定参数，所以以上提到的关键的两行可以转换为如下形式：</p><pre><code class="python">inner = update_wrapper(inner, wrapped=func, assigned=WRAPPER_ASSIGNMENTS,                       updated=WRAPPER_UPDATES)</code></pre><p>再让我们把目光聚集在<code>update_wrapper()</code>函数的源码上（<code>functools.py</code><a href="https://github.com/python/cpython/blob/3.6/Lib/functools.py#L44">第44行</a>）：</p><pre><code class="python">WRAPPER_ASSIGNMENTS = (&#39;__module__&#39;, &#39;__name__&#39;, &#39;__qualname__&#39;, &#39;__doc__&#39;,                       &#39;__annotations__&#39;)WRAPPER_UPDATES = (&#39;__dict__&#39;,)def update_wrapper(wrapper,                   wrapped,                   assigned = WRAPPER_ASSIGNMENTS,                   updated = WRAPPER_UPDATES):    &quot;&quot;&quot; ... 省略    &quot;&quot;&quot;    for attr in assigned:        try:            value = getattr(wrapped, attr)        except AttributeError:            pass        else:            setattr(wrapper, attr, value)    for attr in updated:        getattr(wrapper, attr).update(getattr(wrapped, attr, &#123;&#125;))    # Issue #17482: set __wrapped__ last so we don&#39;t inadvertently copy it    # from the wrapped function when updating __dict__    wrapper.__wrapped__ = wrapped    # Return the wrapper so this can be used as a decorator via partial()    return wrapper</code></pre><p>结合前两段代码，<code>functools.wraps</code>所做的事情就比较清晰了：通过<code>getattr()</code>从被装饰的函数中提取<code>__module__</code>、<code>__name__</code>等属性，再通过<code>setattr()</code>将提取到的属性赋值给装饰后的函数。也就是说，<code>deco()</code>装饰器最后可以转换为如下形式：</p><pre><code class="python">def deco(func: Callable) -&gt; Callable:    # @functools.wraps(func)    def inner(*args, **kwargs):        &quot;&quot;&quot;inner doc&quot;&quot;&quot;        print(f&#39;inner(&#123;args&#125;, &#123;kwargs&#125;)&#39;)    # 节省空间，不考虑AttributeError    setattr(inner, &#39;__module__&#39;, getattr(func, &#39;__module__&#39;))     # ... 省略    getattr(inner, &#39;__dict__&#39;).update(getattr(func, &#39;__dict__&#39;, &#123;&#125;))    inner.__wrapped__ = func    return inner</code></pre><p>不考虑注释，<code>functools.wraps</code>的代码总共只有23行，可以说是相当短小了🤣</p><h1 id="functools-lru-cache"><a href="#functools-lru-cache" class="headerlink" title="functools.lru_cache"></a>functools.lru_cache</h1><p><code>lru_cache</code>是个非常实用的装饰器，这个装饰器可以将函数的结果缓存起来，避免传入相同的参数时重复计算。<code>lru</code>代表这个装饰器在缓存结果时采用<code>最近最少使用</code>算法，在缓存的结果到达一定数量时抛弃那些很久没命中过的缓存。</p><p><code>lru_cache</code>接收两个参数：<code>maxsize</code>和<code>typed</code>。前者用于指定缓存结果数量上限，而后者用于判断缓存是是否考虑参数类型。比如<code>func(1)</code>和<code>func(1.0)</code>在默认情况下会共用缓存，但当<code>typed</code>为<code>True</code>时，这两个函数就会分开缓存。</p><h2 id="使用示例-1"><a href="#使用示例-1" class="headerlink" title="使用示例"></a>使用示例</h2><p>递归计算<code>fibonacci</code>数列这种需要多次重复计算的场景特别适合使用<code>lru_cache</code>装饰器。比如下面这个实现：</p><pre><code class="python">def fibonacci(k: int) -&gt; int:    return k if k &lt; 2 else fibonacci(k - 1) + fibonacci(k - 2)</code></pre><p>当计算<code>fibonacci(6)</code>时，总共需要调用25次<code>fibonacci()</code>函数，其中8次是<code>fibonacci(1)</code>。但使用<code>lru_cache</code>装饰器后，只需要调用7次<code>fibonacci()</code>函数即可得出计算结果，而且只需要在原函数上加上一行代码即可实现：</p><pre><code class="python">@functools.lru_cache()def fibonacci(k: int) -&gt; int:    return k if k &lt; 2 else fibonacci(k - 1) + fibonacci(k - 2)</code></pre><p>比较有意思的是，使用了<code>lru_cache</code>装饰器的函数还会被”附赠”两个方法：<code>cache_info</code>和<code>cache_clear</code>，分别用于展示缓存情况和清空缓存：</p><pre><code class="python">&gt;&gt;&gt; fibonacci(6)8&gt;&gt;&gt; fibonacci.cache_info()CacheInfo(hits=4, misses=7, maxsize=128, currsize=7)&gt;&gt;&gt; fibonacci.cache_clear()&gt;&gt;&gt; fibonacci.cache_info()CacheInfo(hits=0, misses=0, maxsize=128, currsize=0)</code></pre><h2 id="源码-1"><a href="#源码-1" class="headerlink" title="源码"></a>源码</h2><p><code>functool.lru_cache</code>在CPython里有两种实现，一种基于C语言（<code>_functool._lru_cache_wrapper</code>），另一种基于Python（<code>functool._lru_cache_wraper</code>）。后者只有在前者无法导入时才会启用，但本篇文章还是聚焦于后者的源码。</p><p><code>lru_cache</code>装饰器的入口函数在<code>functools.py</code>第<a href="https://github.com/python/cpython/blob/3.6/Lib/functools.py#L448">448行</a>：</p><pre><code class="python">_CacheInfo = namedtuple(&quot;CacheInfo&quot;, [&quot;hits&quot;, &quot;misses&quot;, &quot;maxsize&quot;, &quot;currsize&quot;])# ... 略def lru_cache(maxsize=128, typed=False):    &quot;&quot;&quot; ... 略    &quot;&quot;&quot;    if maxsize is not None and not isinstance(maxsize, int):        raise TypeError(&#39;Expected maxsize to be an integer or None&#39;)    def decorating_function(user_function):        wrapper = _lru_cache_wrapper(user_function, maxsize, typed, _CacheInfo)        return update_wrapper(wrapper, user_function)    return decorating_function</code></pre><p>和<code>wraps</code>一样，<code>lru_cache</code>也是一个带参数的装饰器。同时，<code>lru_cache</code>还用到了上一节提到的<code>update_wrapper()</code>函数，自动将被装饰函数的属性拷贝到新函数中。这段代码的关键是<code>wrapper = _lru_cache_wrapper(...)</code>，那就让我们来看看<code>_lru_cache_wrapper</code>函数的源码结构（<code>functools.py</code>第<a href="https://github.com/python/cpython/blob/3.6/Lib/functools.py#L485">485行</a>）：</p><pre><code class="python">def _lru_cache_wrapper(user_function, maxsize, typed, _CacheInfo):    # 初始化变量，省略    if maxsize == 0:        def wrapper(*args, **kwds):            # No caching -- just a statistics update after a successful call            # ... 省略    elif maxsize is None:        def wrapper(*args, **kwds):            # Simple caching without ordering or size limit            # ... 省略    else:        def wrapper(*args, **kwds):            # Size limited caching that tracks accesses by recency            # ... 省略    def cache_info():        &quot;&quot;&quot;Report cache statistics&quot;&quot;&quot;        # ... 省略    def cache_clear():        &quot;&quot;&quot;Clear the cache and cache statistics&quot;&quot;&quot;        # ... 省略    wrapper.cache_info = cache_info    wrapper.cache_clear = cache_clear    return wrapper</code></pre><p>所以，当某个函数使用了<code>lru_cache</code>装饰器后，调用这个函数时被执行的实际上是源码里定义的各个<code>wrapper</code>，<code>cache_info()</code>和<code>cache_clear()</code>这两个方法也是在源码里为<code>wrapper</code>加上的。接下来，就让我们具体分析源码里定义的各个模块。</p><h3 id="maxsize为0"><a href="#maxsize为0" class="headerlink" title="maxsize为0"></a>maxsize为0</h3><p>当<code>maxsize</code>为0时，<code>lru_cache</code>不会缓存任何结果，除了调用原始函数并返回结果外，只增加<code>misses</code>（缓存未命中）计数器。</p><pre><code class="python">def _lru_cache_wrapper(user_function, maxsize, typed, _CacheInfo):    # ...    hits = misses = 0    # ...    if maxsize == 0:        def wrapper(*args, **kwds):            # No caching -- just a statistics update after a successful call            nonlocal misses            result = user_function(*args, **kwds)            misses += 1            return result    # ...</code></pre><h3 id="maxsize为None"><a href="#maxsize为None" class="headerlink" title="maxsize为None"></a>maxsize为None</h3><p>当<code>maxsize</code>为<code>None</code>时，<code>lru_cache</code>会无限制地缓存结果，<code>lru</code>算法并不会生效。</p><pre><code class="python">def _lru_cache_wrapper(user_function, maxsize, typed, _CacheInfo):    # Constants shared by all lru cache instances:    sentinel = object()          # unique object used to signal cache misses    make_key = _make_key         # build a key from the function arguments    cache = &#123;&#125;    hits = misses = 0    cache_get = cache.get    # bound method to lookup a key or return None    # ...    elif maxsize is None:        def wrapper(*args, **kwds):            # Simple caching without ordering or size limit            nonlocal hits, misses            key = make_key(args, kwds, typed)            result = cache_get(key, sentinel)            if result is not sentinel:                hits += 1                return result            result = user_function(*args, **kwds)            cache[key] = result            misses += 1            return result    # ...</code></pre><p>可以看到，<code>lru_cache</code>首先会通过函数的参数生成<code>key</code>值（<code>make_key</code>），再用<code>key</code>值去缓存池（<code>dict</code>）里查找结果。如果命中缓存，则返回缓存并增加<code>hits</code>计数器；否则调用函数返回结果，将结果加入缓存池并增加<code>misses</code>（缓存未命中）计数器。</p><h3 id="make-key"><a href="#make-key" class="headerlink" title="_make_key"></a>_make_key</h3><p>再让我们将目光转到<code>_make_key</code>上来。这个函数会通过函数的参数、<code>typed</code>的值来为函数调用生成<code>key</code>值，用于在缓存池里查找或缓存结果。其源码如下：</p><pre><code class="python">class _HashedSeq(list):    &quot;&quot;&quot; ... 省略    &quot;&quot;&quot;    __slots__ = &#39;hashvalue&#39;    def __init__(self, tup, hash=hash):        self[:] = tup        self.hashvalue = hash(tup)    def __hash__(self):        return self.hashvaluedef _make_key(args, kwds, typed,             kwd_mark = (object(),),             fasttypes = &#123;int, str, frozenset, type(None)&#125;,             tuple=tuple, type=type, len=len):    &quot;&quot;&quot; ... 省略    &quot;&quot;&quot;    key = args    if kwds:        key += kwd_mark        for item in kwds.items():            key += item    if typed:        key += tuple(type(v) for v in args)        if kwds:            key += tuple(type(v) for v in kwds.values())    elif len(key) == 1 and type(key[0]) in fasttypes:        return key[0]    return _HashedSeq(key)</code></pre><p>从这段源码可以看出，当函数的参数只有一个、<code>typed</code>为<code>False</code>、且类型为<code>int</code>、<code>str</code>等时，<code>_make_key</code>会直接将该参数作为<code>key</code>值返回。否则，<code>_make_key</code>会生成一个<code>_HashedSeq</code>来存储函数的参数，当<code>typed</code>为<code>True</code>时还会存储参数的类型，且<code>*args</code>和<code>**kwargs</code>这两种参数之间会插入一个<code>object</code>用作分隔。下面这几个例子能帮助理解这段源码：</p><pre><code class="python">&gt;&gt;&gt; from functools import _make_key&gt;&gt;&gt; _make_key((1, &#39;123&#39;), &#123;&#39;name&#39;: &#39;bob&#39;&#125;, False)[1, &#39;123&#39;, &lt;object object at ...&gt;, &#39;name&#39;, &#39;bob&#39;]&gt;&gt;&gt; _make_key((1, &#39;123&#39;), &#123;&#39;name&#39;: &#39;bob&#39;&#125;, True)[1, &#39;123&#39;, &lt;object object at ...&gt;, &#39;name&#39;, &#39;bob&#39;, &lt;class &#39;int&#39;&gt;, &lt;class &#39;str&#39;&gt;, &lt;class &#39;str&#39;&gt;]&gt;&gt;&gt; _make_key((1,), &#123;&#125;, False)1&gt;&gt;&gt; _make_key((1,), &#123;&#125;, True)[1, &lt;class &#39;int&#39;&gt;]</code></pre><h4 id="谁错了？"><a href="#谁错了？" class="headerlink" title="谁错了？"></a>谁错了？</h4><p>理解了这段源码之后，又会出现新的问题：不管<code>typed</code>为<code>True</code>还是<code>False</code>，对于单参数<code>1</code>和<code>1.0</code>，<code>_make_key()</code>得到的结果的哈希值都不一致：</p><pre><code class="python">&gt;&gt;&gt; from functools import _make_key&gt;&gt;&gt; hash(_make_key((1,), &#123;&#125;, False))    # 11&gt;&gt;&gt; hash(_make_key((1.0,), &#123;&#125;, False))  # [1.0]3430019387558&gt;&gt;&gt; hash(_make_key((1,), &#123;&#125;, True))     # [1, &lt;class &#39;int&#39;&gt;]3713082220284583106&gt;&gt;&gt; hash(_make_key((1.0,), &#123;&#125;, True))   # [1.0, &lt;class &#39;float&#39;&gt;]3713082219329796056</code></pre><p>但此时如果使用<code>lru_cache</code>，就会发现<code>lru_cache</code>是将单参数<code>1</code>和<code>1.0</code>一起缓存的，和本文一开始对<code>lru_cache</code>的描述一致：</p><pre><code class="python">@functools.lru_cache()def test(val):    print(val)test(1)    # 输出1test(1.0)  # 已缓存，不输出</code></pre><p>所以这到底是哪里出了问题？答案就是前文提到的C语言实现和Python实现的区别。C语言实现的<code>_lru_cache_wrapper()</code>并不使用Python实现的<code>_make_key()</code>，而CPython默认使用的是前者，所以会造成这里提到的问题。</p><p>此时如果将修改CPython内置的<code>functools.py</code>，只使用Python实现的<code>_lru_cache_wrapper()</code>，就会发现结果是符合预期的。<code>1</code>和<code>1.0</code>通过<code>_make_key()</code>得到的哈希值不同，自然也就会分开缓存：</p><pre><code class="python">try:    # 不使用C语言实现的版本    # from _functools import _lru_cache_wrapper    passexcept ImportError:    pass</code></pre><pre><code class="python">@functools.lru_cache()def test(val):    print(val)test(1)    # 输出1test(1.0)  # 输出1.0</code></pre><h3 id="maxsize为正整数"><a href="#maxsize为正整数" class="headerlink" title="maxsize为正整数"></a>maxsize为正整数</h3><p>当<code>maxsize</code>为正整数时，<code>lru_cache</code>不能无限制地缓存函数结果，必须用<code>lru</code>算法清除缓存。遗憾的是，由于受限于篇幅，且笔者并没有弄懂这部分的代码（这才是重点吧……），所以这部分源码就先按下不表。</p><h3 id="cache-info和cache-clear"><a href="#cache-info和cache-clear" class="headerlink" title="cache_info和cache_clear"></a>cache_info和cache_clear</h3><pre><code class="python">def _lru_cache_wrapper(user_function, maxsize, typed, _CacheInfo):    # ...    hits = misses = 0    full = False    cache_len = cache.__len__  # get cache size without calling len()    lock = RLock()           # because linkedlist updates aren&#39;t threadsafe    root = []                # root of the circular doubly linked list    root[:] = [root, root, None, None]     # initialize by pointing to self    # ...    def cache_info():        &quot;&quot;&quot;Report cache statistics&quot;&quot;&quot;        with lock:            return _CacheInfo(hits, misses, maxsize, cache_len())    def cache_clear():        &quot;&quot;&quot;Clear the cache and cache statistics&quot;&quot;&quot;        nonlocal hits, misses, full        with lock:            cache.clear()            root[:] = [root, root, None, None]            hits = misses = 0            full = False</code></pre><p>这两个函数的作用比较单纯，分别是返回当前缓存状态和清空缓存。源码也简单易懂，这里就不过多介绍。其中<code>root</code>、<code>lock</code>、<code>full</code>等变量是只有在<code>maxsize</code>为正整数，或者说启用<code>lru</code>算法时才需要的，本文介绍的、针对其它情况的源码不需要这几个变量。</p><h1 id="尾声"><a href="#尾声" class="headerlink" title="尾声"></a>尾声</h1><p>本来笔者还想分析<code>singledispatch</code>装饰器的源码的，但一个<code>lru_cache</code>就把笔者折腾得够呛，这还是跳过了最复杂的<code>lru</code>算法的情况下……路漫漫其修远兮啊。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>源码</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>读《流畅的Python》：函数装饰器与闭包</title>
    <link href="/2020/06/19/fluent-python-chap-7/"/>
    <url>/2020/06/19/fluent-python-chap-7/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2020/06/19/fluent_python.webp"></p><p>读书笔记这种内容，放在<code>Evernote</code>等笔记软件里似乎要比放在博客里更合适。但其一是笔者还没有找到适合我的笔记同步方案，其二是博客相比笔记软件更能锻炼自身的表达能力（或许吧），其三是将读书笔记放在博客里可以避免在求职的时候被面试官吐槽博客内容太少（笑），所以还是决定将这篇文章放在博客里。</p><p>但不管怎样，读书笔记终归是读书笔记，对其他人的作用还是有限，如有读者觉得碍眼烦请无视。</p><blockquote><p>注：本篇文章使用CPython3.6</p></blockquote><h1 id="函数装饰器"><a href="#函数装饰器" class="headerlink" title="函数装饰器"></a>函数装饰器</h1><p>函数装饰器是可调用的对象，其参数是另一个函数（被装饰的函数）。下面是一个简单的装饰器：</p><pre><code class="python">def deco(func: Callable) -&gt; Callable:    def inner():        print(&#39;running inner()&#39;)    return inner</code></pre><p>用法也很容易理解，下面两段代码效果一样：</p><pre><code class="python">@decodef target():    print(&#39;running target()&#39;)</code></pre><pre><code class="python">def target():    print(&#39;running target()&#39;)target = deco(target)</code></pre><p>从上面的例子就可以看出，装饰器只是一个语法糖。即使没有这个语法糖，也只是在进行类似的操作时需要更繁琐的代码，但不会造成功能上的缺失。</p><p>既然上面两段代码效果一样，那装饰器何时运行就很明显了：<strong>在被装饰的函数定义之后（通常是函数被导入时）立即运行</strong>。毕竟，真正被导入的不是<code>target()</code>，而是<code>deco()</code>的返回值。而要获得<code>deco()</code>的返回值，就必须让<code>deco()</code>先运行。</p><h1 id="变量作用域"><a href="#变量作用域" class="headerlink" title="变量作用域"></a>变量作用域</h1><p>下面两段代码有什么区别？</p><pre><code class="python">b = 9def f1():    print(b)f1()</code></pre><pre><code class="python">b = 9def f2():    print(b)  # UnboundLocalError: local variable &#39;b&#39; referenced before assignment    b = 3f2()</code></pre><p>答案是后一段代码在执行<code>print(b)</code>时会抛出<code>UnboundLocalError</code>。因为在<code>f2()</code>中变量<code>b</code>被赋值了（<code>b = 3</code>），于是Python在编译<code>f2()</code>的定义体时将<code>b</code>视为局部变量，而不是像<code>f1()</code>那样在全局变量里查找<code>b</code>。如果想让<code>f2()</code>也将<code>b</code>视为全局变量，就需要使用<code>global</code>关键字：</p><pre><code class="python">b = 9def f1():    global b    print(b)    b = 3f1()  # 正常运行</code></pre><h1 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h1><p>为什么会突然提到变量作用域这个概念呢？我们知道，Python中的局部变量在离开作用域会后被GC（垃圾回收）机制销毁，比如：</p><pre><code class="python">&gt;&gt;&gt; import weakref&gt;&gt;&gt; def foo():...     s = set(range(3))...     weakref.finalize(s, lambda: print(&#39;s has gone&#39;))... &gt;&gt;&gt; foo()s has gone</code></pre><p>很明显，<code>foo()</code>运行完成后，运行期间创建的变量<code>s</code>被销毁了。但如果我们想在<code>foo()</code>内部再定义一个函数，并让这个函数能访问变量<code>s</code>，那<code>s</code>就不会被销毁：</p><pre><code class="python">&gt;&gt;&gt; import weakref&gt;&gt;&gt; def foo():...     s = set(range(3))...     weakref.finalize(s, lambda: print(&#39;s has gone&#39;))......     def bar():...         print(s)...     return bar... &gt;&gt;&gt; bar = foo()&gt;&gt;&gt; bar()&#123;0, 1, 2&#125;</code></pre><p>像<code>bar()</code>这种<strong>延伸了作用域的函数</strong>叫做<strong>闭包</strong>，它能访问不在定义体内定义的非全局变量。从这点可以看出，只有嵌套函数才有闭包这个概念。</p><p>变量<code>s</code>在<code>foo()</code>运行函数后已经是孤魂野鬼，被称作自由变量（<code>free variable</code>）。但<code>bar()</code>要能够访问变量<code>s</code>，就需要持有变量<code>s</code>的引用，这点可以从<code>__code__</code>属性（编译后的函数定义体）和<code>__closure__</code>属性中可以看出：</p><pre><code class="python">&gt;&gt;&gt; bar.__code__.co_varnames  # 局部变量()&gt;&gt;&gt; bar.__code__.co_freevars  # 自由变量(&#39;s&#39;,)&gt;&gt;&gt; bar.__closure__(&lt;cell at ...: set object at ...&gt;,)&gt;&gt;&gt; bar.__closure__[0].cell_contents&#123;0, 1, 2&#125;</code></pre><h2 id="危险地带：被删除的自由变量"><a href="#危险地带：被删除的自由变量" class="headerlink" title="危险地带：被删除的自由变量"></a>危险地带：被删除的自由变量</h2><p>Python在编译<code>foo()</code>时发现<code>bar()</code>需要访问变量<code>s</code>，于是将<code>s</code>视为自由变量，即使离开作用域也不会销毁。但Python不是静态语言，对象的实际状态需要等到运行时才能确定。看下面这个例子：</p><pre><code class="python">def foo():    s = set(range(3))    weakref.finalize(s, lambda: print(&#39;s has gone&#39;))    del s         # 1. 输出s has gone    def bar():        print(s)  # 4. NameError: free variable &#39;s&#39; referenced before assignment in enclosing scope    return barbar = foo()print(bar.__code__.co_freevars)  # 2. 输出(&#39;s&#39;,)print(bar.__closure__[0])        # 3. 输出&lt;cell at ...: empty&gt;bar()</code></pre><p>这段代码的执行顺序和输出结果已经以注释的形式附加。Python在编译完<code>bar()</code>之后知道应该将<code>s</code>视为自由变量，但实际上<code>bar()</code>在运行时变量<code>s</code>已经被删除了……这个例子告诉我们，Python的动态特性是把双刃剑，更直白点就是：不要作死.</p><h2 id="nonlocal关键字"><a href="#nonlocal关键字" class="headerlink" title="nonlocal关键字"></a>nonlocal关键字</h2><p>再来看看这样一个返回值为<code>统计平均值的函数</code>的函数：</p><pre><code class="python">def make_averager():    nums = []    def averager(num: int) -&gt; float:        nums.append(num)        return sum(nums) / len(nums)    return averageraverager = make_averager()print(averager(10))  # 10.0print(averager(5))  # 7.5</code></pre><p>嗯，运行很完美。优化一下这个函数，让它不需要将所有的数字都保存起来：</p><pre><code class="python">def make_averager():    total, count = 0, 0    def averager(num: int) -&gt; float:        total += num  # UnboundLocalError        count += 1        return total / count    return averager</code></pre><p>和上一节的第二个例子类似，<code>averager()</code>在运行时会抛出<code>UnboundLocalError</code>异常。但这次不能用<code>global</code>关键字来解决这个异常了：因为<code>total</code>和<code>num</code>压根就不是全局变量，而是作用域仅限<code>make_averager()</code>的局部变量。那怎样让<code>averager()</code>能修改<code>total</code>和<code>num</code>呢？答案是<code>nonlocal</code>关键字。和<code>global</code>关键字类似，<code>nonlocal</code>会告知Python：<strong>这个变量在更外层的作用域</strong>。如代码所示：</p><pre><code class="python">def make_averager():    total, count = 0, 0    def averager(num: int) -&gt; float:        nonlocal total, count        total += num        count += 1        return total / count  # 正常运行    return averager</code></pre><p>但<code>nonlocal</code>关键字并不能取代<code>global</code>关键字，因为局部变量（<code>locals()</code>）和全局变量（<code>globals()</code>）是互相独立的，比如如下的代码会抛出<code>SyntaxError</code>，即使<code>f2()</code>并没有被实际执行：</p><pre><code class="python">b = 9def f2():    nonlocal b    print(b)    b = 3</code></pre><p>同时<code>nonlocal</code>关键字的声明是不限深度的，也就是说Python会一直向更外层的作用域寻找这个变量，直到找到为止。如代码所示：</p><pre><code class="python">def make_averager():    total, count = 0, 0    def averager(num: int) -&gt; float:        def deeper():            nonlocal total, count            total += num            count += 1            return total / count  # 正常运行        return deeper()    return averager</code></pre><h1 id="计时函数装饰器"><a href="#计时函数装饰器" class="headerlink" title="计时函数装饰器"></a>计时函数装饰器</h1><p>了解了函数装饰器、闭包等概念后，就可以编写一个稍微具有一点实用性的函数装饰器了：</p><pre><code class="python">def clock(func: Callable) -&gt; Callable:    def inner(*args, **kwargs):        &quot;&quot;&quot;inner doc&quot;&quot;&quot;        t0 = time.perf_counter()        result = func(*args, **kwargs)        elapsed = time.perf_counter() - t0        fmt = &#39;[&#123;elapsed:.4f&#125;s] &#123;func.__name__&#125;(&#123;args&#125;, &#123;kwargs&#125;): &#123;result&#125;&#39;        print(fmt.format(**locals()))        return result    return inner</code></pre><p>同样的，<code>inner</code>函数属于闭包函数，<code>func</code>属于自由变量。使用方法和效果如下：</p><pre><code class="python">@clockdef snooze(second: float):    &quot;&quot;&quot;snooze doc&quot;&quot;&quot;    time.sleep(second)snooze(0.1)  # 输出：[0.1001s] snooze((0.1,), &#123;&#125;): None</code></pre><h2 id="functool-wraps"><a href="#functool-wraps" class="headerlink" title="functool.wraps"></a>functool.wraps</h2><p>上面这个装饰器的缺点是无法继承被装饰函数的属性（如<code>__name__</code>、<code>__doc__</code>）：</p><pre><code class="python">print(snooze.__name__)  # innerprint(snooze.__doc__)  # inner doc</code></pre><p>这个时候就需要请出Python内置的装饰器<code>functool.wraps</code>了，这个装饰器会自动复制被装饰函数的属性到返回值函数里（换句话说就是让<code>inner()</code>按目标函数<code>snnoze()</code>的样子整个容）：</p><pre><code class="python">def clock(func: Callable) -&gt; Callable:    @functools.wraps(func)    def inner(*args, **kwargs):        &quot;&quot;&quot;inner doc&quot;&quot;&quot;        # ... 略    return inner@clockdef snooze(second: float):    &quot;&quot;&quot;snooze doc&quot;&quot;&quot;    time.sleep(secondprint(snooze.__name__)  # snoozeprint(snooze.__doc__)  # snooze doc</code></pre><h1 id="带参数的函数装饰器"><a href="#带参数的函数装饰器" class="headerlink" title="带参数的函数装饰器"></a>带参数的函数装饰器</h1><p>上节提到的<code>functool.wraps</code>装饰器比较特别，这个装饰器可以传入参数。我们知道，对于不带参数的装饰器，下面两段代码是相等的：</p><pre><code class="python">@decodef target():    print(&#39;running target()&#39;)</code></pre><pre><code class="python">def target():    print(&#39;running target()&#39;)target = deco(target)</code></pre><p>而对于带参数的装饰器，下面两段代码也是相等的：</p><pre><code class="python">@deco(123)def target():    print(&#39;running target()&#39;)</code></pre><pre><code class="python">def target():    print(&#39;running target()&#39;)target = deco(123)(target)</code></pre><p>从上面的例子可以看出，装饰器带不带参数，决定了是用这个函数本身来当装饰器，还是用函数的返回值来当装饰器（可能有点绕）。用这个思路改进一下前面提到的函数计时装饰器：</p><pre><code class="python">DEFAULT_FMT = &#39;[&#123;elapsed:.4f&#125;s] &#123;func.__name__&#125;(&#123;args&#125;, &#123;kwargs&#125;): &#123;result&#125;&#39;def clock_plus(fmt=DEFAULT_FMT):    def clock(func: Callable) -&gt; Callable:        def inner(*args, **kwargs):            &quot;&quot;&quot;inner doc&quot;&quot;&quot;            t0 = time.perf_counter()            result = func(*args, **kwargs)            elapsed = time.perf_counter() - t0            print(fmt.format(**locals()))            return result        return inner    return clock</code></pre><p>用法和效果如下：</p><pre><code class="python">@clock_plus(fmt=&#39;&#123;elapsed:.4f&#125;&#39;)def snooze(second: float):    &quot;&quot;&quot;snooze doc&quot;&quot;&quot;    time.sleep(second)snooze(0.1)  # 输出：0.1001</code></pre><p>所以，带参数的函数装饰器只是在不带函数的装饰器外面裹了一层，并没有太多魔术操作。</p><h1 id="尾声"><a href="#尾声" class="headerlink" title="尾声"></a>尾声</h1><p>《流畅的Python》第七章（函数装饰器和闭包）的概念不算少，同时笔者也没能在提取关键信息的基础上控制好篇幅，再加上文章里还有笔者的一些个人理解，所以文章最后只能草草收尾，没有介绍叠放装饰器、<code>functools.lru_cache</code>等内容。</p><p>也许在读完这类书籍后，画一个思维导图，或单纯写一篇给自己看的笔记才是最好的选择，至少不会在博客里丢人（逃</p>]]></content>
    
    
    
    <tags>
      
      <tag>流畅的Python</tag>
      
      <tag>读书笔记</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>如何在Python中引起内存泄露</title>
    <link href="/2020/06/14/%E5%A6%82%E4%BD%95%E5%9C%A8Python%E4%B8%AD%E5%BC%95%E8%B5%B7%E5%86%85%E5%AD%98%E6%B3%84%E9%9C%B2/"/>
    <url>/2020/06/14/%E5%A6%82%E4%BD%95%E5%9C%A8Python%E4%B8%AD%E5%BC%95%E8%B5%B7%E5%86%85%E5%AD%98%E6%B3%84%E9%9C%B2/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2020/06/14/gc.jpg"></p><p>嗯，怎么看怎么像标题党写的标题……其实这篇文章只是笔者对Python中引用计数、弱引用的一些记录和思考，不涉及引用循环、分代回收等概念，先打个预防针。</p><blockquote><p>注：本篇文章基于CPython 3.6，可能不适用于其它CPython版本或其它类型的Python实现。</p></blockquote><h1 id="引用计数"><a href="#引用计数" class="headerlink" title="引用计数"></a>引用计数</h1><p><strong>引用计数</strong>（Reference counting）可以说是一个老生常谈的问题了，炒冷饭也没啥意思。引用一下《流畅的Python》里的描述：<em>“每个对象都会统计有多少引用指向自己。当引用计数归零时，对象立即就被销毁”</em>。引用计数在不存在<strong>引用循环</strong>（Reference cycle）时能够及时清理内存，这也是CPython最主要的垃圾回收算法。</p><pre><code class="python">&gt;&gt;&gt; import sys&gt;&gt;&gt; import weakref&gt;&gt;&gt; s1 = set(range(3))&gt;&gt;&gt; sys.getrefcount(s1)2&gt;&gt;&gt; s2 = s1&gt;&gt;&gt; s2 is s1True&gt;&gt;&gt; sys.getrefcount(s1)3&gt;&gt;&gt; weakref.finalize(s1, lambda: print(&quot;obj has gone&quot;))&lt;finalize object at ...; for &#39;set&#39; at ...&gt;&gt;&gt;&gt; del s2&gt;&gt;&gt; sys.getrefcount(s1)2&gt;&gt;&gt; del s1obj has gone</code></pre><p>从上面的例子可以看出，<code>set(range(3))</code>这个对象总共有两个引用：<code>s1</code>和<code>s2</code>，只有当这两个引用都无效（<code>del s2</code>、<code>del s1</code>）之后，这个对象才会被销毁（或者说占用的内存空间被回收）。</p><p>需要注意的是，<code>sys.getrefcount()</code>返回的值会比真实引用计数值高出1，因为参数传递会（临时）增加一个引用，详见<a href="https://docs.python.org/3.6/library/sys.html#sys.getrefcount">官方文档</a>。同时，笔者用到了<code>weakref.finalize()</code>方法来确认对象是否被销毁，该方法会在第一个参数指向的对象被销毁时调用第二个参数（一个<code>Callable</code>对象）。</p><h1 id="如何引起内存泄露"><a href="#如何引起内存泄露" class="headerlink" title="如何引起内存泄露"></a>如何引起内存泄露</h1><p>前面提到，引用计数在不存在引用循环时能及时清理内存，但这并不能代表程序员就能高枕无忧了。毕竟，被遗忘的引用也是引用。考虑下面这个可以追踪当前实例的类：</p><pre><code class="python">class MyOBJ(object):    __instances = set()    def __init__(self, name: str):        self.name = name        self.__instances.add(self)    def __str__(self) -&gt; str:        return f&quot;MyOBJ(&#39;&#123;self.name&#125;&#39;)&quot;    @classmethod    def instances(cls):        return cls.__instances</code></pre><pre><code class="python">&gt;&gt;&gt; obj1 = MyOBJ(&quot;obj1&quot;)&gt;&gt;&gt; weakref.finalize(obj1, lambda: print(&quot;obj1 has gone&quot;))&lt;finalize object at ...; for &#39;MyOBJ&#39; at ...&gt;&gt;&gt;&gt; del obj1&gt;&gt;&gt; len(MyOBJ.instances())1</code></pre><p>可以看到，即使通过<code>del obj1</code>将对象的引用删除，但由于<code>MyOBJ</code>这个类的<code>__instances</code>属性里始终持有<code>obj1</code>对应对象的另一个引用，所以这个对象始终无法被销毁。如果不能将<code>__instances</code>里的引用也删除，就会引起内存泄露。</p><pre><code class="python">&gt;&gt;&gt; MyOBJ.instances().pop()MyOBJ(&#39;obj1&#39;)obj1 has gone</code></pre><h1 id="弱引用（weakref）"><a href="#弱引用（weakref）" class="headerlink" title="弱引用（weakref）"></a>弱引用（weakref）</h1><p>可能从第一个例子里就有读者奇怪：<code>weakref.finalize()</code>会在对象被销毁时执行动作，按理说也应该持有该对象的一个引用才对，这不就产生矛盾了吗？实际上<code>weakref</code>这个模块名就给出了说明：<strong>弱引用</strong>（weakref）指不增加对象引用计数的引用。于是，<code>weakref.finalize()</code>自然也就不会妨碍对象被销毁了。</p><p>同样的，上一节提到的内存泄露问题也可以通过<code>weakref</code>模块提供的功能解决。只需要将<code>MyOBJ</code>里<code>__instances</code>的类型改为<code>WeakSet</code>即可：</p><pre><code class="python">class MyOBJ(object):    __instances = weakref.WeakSet()        # ... 省略</code></pre><pre><code class="python">&gt;&gt;&gt; obj1 = MyOBJ(&quot;obj1&quot;)&gt;&gt;&gt; weakref.finalize(obj1, lambda: print(&quot;obj1 has gone&quot;))&lt;finalize object at ...; for &#39;MyOBJ&#39; at ...&gt;&gt;&gt;&gt; len(MyOBJ.instances())1&gt;&gt;&gt; del obj1obj1 has gone&gt;&gt;&gt; len(MyOBJ.instances())0</code></pre><p>在引入了<code>WeakSet</code>后，<code>__instances</code>属性持有的引用都是弱引用，当实际的对象被垃圾回收机制销毁时，<code>__instances</code>属性里的引用也会跟着被删除。类似的数据结构还有<code>WeakKeyDictionary</code>、<code>WeakValueDictionary</code>，它们会将对象的引用分别当成字典的键和值，更具体的说明可以参考<a href="https://docs.python.org/3.6/library/weakref.html">官方文档</a>。</p><h1 id="延伸"><a href="#延伸" class="headerlink" title="延伸"></a>延伸</h1><p>实际上本文只涉及Python内存管理中最基本的一些概念。关于引用循环、分代回收等概念，推荐阅读以下内容：</p><ul><li><a href="https://github.com/python/cpython/blob/3.6/Modules/gcmodule.c#L899">cpython&#x2F;gcmodule.c at 3.6 · python&#x2F;cpython</a></li><li><a href="https://pythoninternal.wordpress.com/2014/08/04/the-garbage-collector/">The Garbage Collector | Yet Another Python Internals Blog</a></li><li><a href="https://www.quora.com/How-does-garbage-collection-in-Python-work-What-are-the-pros-and-cons">How does garbage collection in Python work? What are the pros and cons? - Quora</a></li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>GC</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>如何高效地对100万个UUID进行前缀匹配查询</title>
    <link href="/2020/06/13/%E5%A6%82%E4%BD%95%E9%AB%98%E6%95%88%E5%9C%B0%E5%AF%B9100%E4%B8%87%E4%B8%AAUUID%E8%BF%9B%E8%A1%8C%E5%89%8D%E7%BC%80%E5%8C%B9%E9%85%8D%E6%9F%A5%E8%AF%A2/"/>
    <url>/2020/06/13/%E5%A6%82%E4%BD%95%E9%AB%98%E6%95%88%E5%9C%B0%E5%AF%B9100%E4%B8%87%E4%B8%AAUUID%E8%BF%9B%E8%A1%8C%E5%89%8D%E7%BC%80%E5%8C%B9%E9%85%8D%E6%9F%A5%E8%AF%A2/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2020/06/13/redis-logo.png"></p><p>在《Redis实战》这本书的第六章中，作者介绍了如何使用Redis的ZSET来实现通讯录自动补全（即前缀匹配查询）。这部分内容激发了我的兴趣，于是我借助这个机会抛出问题：如何高效地对100万个UUID进行前缀匹配，并尝试用这篇文章来解答。</p><h1 id="数据准备"><a href="#数据准备" class="headerlink" title="数据准备"></a>数据准备</h1><p>首先是数据准备工作：生成100万个UUID、将其写入至Redis和MariaDB（后者可作为对比）。顺便一提，Redis和MariaDB均在笔者电脑上通过Docker部署，版本分别为Redis 6.0和MariaDB 10.4。</p><pre><code class="shell">$ # 生成100万个UUID$ for i in &#123;0..999999&#125;; do uuidgen; done &gt; uuids_1m.txt$ du uuids_1m.txt -h36Muuids_1m.txt$ head -n2 uuid_1m.txt25e2cc59-b117-4961-9bc6-a63c5b1770dbe9f833b9-630d-47cb-b2ae-69c6d1bc2bde</code></pre><pre><code class="shell">redis&gt; zcount uuids -inf +inf(integer) 1000000</code></pre><pre><code class="sql"># MariaDB表结构，在uuid字段加了索引create table uuids (    id int primary key auto_increment,    uuid char(36) not null,    key idx_uuid (uuid));</code></pre><pre><code class="sql">mariadb&gt; select count(*) from uuids;+----------+| count(*) |+----------+| 1000000  |+----------+mariadb&gt; select table_name, data_length, index_length from information_schema.tables where table_name=&#39;uuids&#39;;+------------+-------------+--------------+| table_name | data_length | index_length |+------------+-------------+--------------+| uuids      | 64569344    | 78397440     |+------------+-------------+--------------+</code></pre><p>可以看到，在插入100万条UUID数据后，MariaDB数据表的大小超过了60MB，索引的大小超过了70MB。同时，数据准备完成后，Redis和MariaDB的内存占用从刚部署时的5M和80M上升至了120M和270M。</p><h1 id="Python遍历查询"><a href="#Python遍历查询" class="headerlink" title="Python遍历查询"></a>Python遍历查询</h1><p>数据准备完成后，下面要做的就是进行前缀匹配查询。最直接的方式应该是遍历查询了，实现起来也简单，但<code>O(n)</code>的时间复杂实在是让人不敢恭维，代码贴出来权当消遣。</p><p>为了更符合前缀匹配查询的定义，我在读取完UUID后还会对数据进行一次排序操作，而这个排序的时间消耗是不会被统计到后续的性能对比中的，这对下一节的二分查询同样适用。</p><pre><code class="python">uuids = []# .. 读取uuiduuids.sort()def traverse(prefix: str, limit=10):    start = 0    while start &lt; len(uuids) and not uuids[start].startswith(prefix):        start += 1    ans = []    for i in range(start, min(start + limit, len(uuids))):        if not uuids[i].startswith(prefix):            break        ans.append(uuids[i])    return ans</code></pre><h1 id="Python二分查询"><a href="#Python二分查询" class="headerlink" title="Python二分查询"></a>Python二分查询</h1><p>遍历查询最大的问题在于其<code>O(n)</code>的时间复杂度。但只要目标数据是有序的，那就到时间复杂度只有<code>O(log n)</code>的二分查询上场的时候了。对于前缀匹配查询来说，查询以<code>abcd</code>开头的字符串，本质上是查询比<code>abcczzzz...</code>大的字符串。而在代码中，我用<code>0xffff</code>这个边界符号来代替前文提到的那一串<code>zzzz....</code>，这对UUID这种不超过ASCII码范围的字符串来说已经足够了。</p><p>同时，代码里二分查找的结果（<code>start</code>）是第一个大于要找的字符串的元素的下标，这点需要注意。</p><pre><code class="python">def bin_search(prefix: str, limit=10):    target = prefix[:-1] + chr(ord(prefix[-1]) - 1) + chr(0xffff)    left, right = 0, len(uuids) - 1    while left &lt;= right:        mid = (left + right) // 2        if uuids[mid] == target:            start = left + 1            break        elif uuids[mid] &lt; target:            left = mid + 1        else:            right = mid - 1    else:        start = left    ans = []    for i in range(start, min(start + limit, len(uuids))):        if not uuids[i].startswith(prefix):            break        ans.append(uuids[i])    return ans</code></pre><h1 id="Redis-ZSET查询"><a href="#Redis-ZSET查询" class="headerlink" title="Redis+ZSET查询"></a>Redis+ZSET查询</h1><p>Redis+ZSET查询的原理和二分查询原理的类似。Redis中的ZSET默认会按照元素的分值进行排序，但如果两个元素的分值一致，那就按元素本身排序。换言之，在ZSET里存放多个分值一致的UUID，从中任意取出一段UUID，这些UUID必然是有序的。</p><p>而为了进行前缀匹配查询，我们需要在ZSET里插入两个边界元素，用来标示开始和结束。对于前缀<code>abcd</code>来说，这两个边界元素就是<code>abcc0xffff</code>和<code>abcd0xffff</code>，ZSET里这两个边界元素之间的数据就是我们想要查询的结果。</p><pre><code class="python">def redis_zset(prefix: str, limit=10):    identifier = str(uuid4())    start = prefix[:-1] + chr(ord(prefix[-1]) - 1)    start += chr(0xffff) + identifier    end = prefix + chr(0xffff) + identifier    conn.zadd(&quot;uuids&quot;, &#123;start: 0, end: 0&#125;)  # 所有UUID的分值都是0    si = conn.zrank(&quot;uuids&quot;, start) + 1    ei = conn.zrank(&quot;uuids&quot;, end) - 1    ei = min(si + limit - 1, ei)    pipe = conn.pipeline()    pipe.zrange(&quot;uuids&quot;, si, ei)    pipe.zrem(&quot;uuids&quot;, start, end)    zr = pipe.execute()[0]  # type: List[bytes]    ans = [uuid.decode(&quot;utf-8&quot;) for uuid in zr]    return [uuid for uuid in ans if chr(0xffff) not in uuid]</code></pre><p>可以看到，查询过程还是很复杂的：要先插入边界元素，再找出两个边界元素的位置，再用边界元素的位置获取查询结果。而且由于过程复杂，这种方法实际的性能表现并不能让人满意，这个之后再分析。</p><blockquote><p>这段代码有很大的隐患：没有考虑多个查询同时执行的情况。更完善的实现请参考《Redis实战》第六章：使用Redis构建应用程序组件</p></blockquote><h1 id="MariaDB-Like查询"><a href="#MariaDB-Like查询" class="headerlink" title="MariaDB+Like查询"></a>MariaDB+Like查询</h1><p>最后介绍一种简单粗暴的查询方法：MariaDB+Like查询，一条SQL解决。</p><pre><code class="python">def mysql_like(prefix: str, limit=10):    sql = &quot;select uuid from uuids where uuid like %s order by uuid limit %s&quot;    cursor.execute(sql, (prefix + &#39;%&#39;, limit))    return [uuid for uuid, in cursor.fetchall()]</code></pre><h1 id="验证-amp-性能测试"><a href="#验证-amp-性能测试" class="headerlink" title="验证&amp;性能测试"></a>验证&amp;性能测试</h1><p>用<code>5678e</code>这个前缀来验证四种查询方法的正确性。</p><pre><code class="python">[&#39;5678e37b-6e7c-4760-89aa-1dc4c2b547c5&#39;, &#39;5678e967-4d5d-4e26-8ed1-bd8819b5b62c&#39;]traverse(&#39;5678e&#39;): 0.108424s[&#39;5678e37b-6e7c-4760-89aa-1dc4c2b547c5&#39;, &#39;5678e967-4d5d-4e26-8ed1-bd8819b5b62c&#39;]bin_search(&#39;5678e&#39;): 0.000020s[&#39;5678e37b-6e7c-4760-89aa-1dc4c2b547c5&#39;, &#39;5678e967-4d5d-4e26-8ed1-bd8819b5b62c&#39;]redis_zset(&#39;5678e&#39;): 0.002244s[&#39;5678e37b-6e7c-4760-89aa-1dc4c2b547c5&#39;, &#39;5678e967-4d5d-4e26-8ed1-bd8819b5b62c&#39;]mysql_like(&#39;5678e&#39;): 0.000773s</code></pre><p>从耗时来看，<code>O(n)</code>的遍历查询妥妥垫底，<code>O(log n)</code>的二分查询秒杀全场，MariaDB的查询速度则远远甩开了Redis。当然，单次特定条件查询不能说明什么问题，下面来看看10000次随机前缀（长度为1~8的随机字符串）匹配查询所需要的时间：</p><p><img src="/images/2020/06/13/compare.png"></p><ul><li><p>不出所料，二分查询的速度是最快的，毕竟是在内存中以<code>O(log n)</code>的时间复杂度来查找数据。</p></li><li><p>MariaDB查找的速度比二分查找的速度慢上十倍，但性能依然出乎我的意料。InnoDB的B+树索引无疑是最大功臣，定位到特定前缀的首个UUID只需要<code>O(log n)</code>的时间复杂度，之后就只需要顺序读取数据。同时，100万个UUID的索引小到可以全部放入内存，这进一步提高了性能。</p></li><li><p>Redis这个以快著称的KV数据库反而最慢，但仔细想想也无可厚非。为了进行一次查询，我们需要执行如下操作：<code>ZADD</code>添加边界元素、<code>ZRANK</code>确定位置、<code>ZRANGE</code>提取结果、<code>ZREM</code>移除边界元素。而这些操作的时间复杂度都达到甚至超过了<code>O(log n)</code>，速度当然快不起来。只能说《Redis实战》这本书提供了这样一个思路，但在具体实现上还是要更慎重。</p></li></ul><h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>读完文章后可能会有读者大呼上当：100万的数据集也太小了吧？数据的插入和删除怎么解决？前缀树呢？好吧，这篇文章确实没有回答这些问题。直接在内存中进行二分查询性能确实非常好，但插入和删除数据的时间复杂度来到了<code>O(n)</code>，同时也对超过内存容量限制的数据集无能为力。但是我觉得，前者可以通过分离数据的修改和查询解决（在数据库中修改，缓存到内存后查询），后者可以通过水平扩展解决（每个进程&#x2F;机器缓存部分数据集），同时笔者水平有限，所以没有回答更深入的问题。</p><p>文章最后，衷心感谢黄健宏老师的著作《Redis设计与实现》和译作《Redis实战》。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Redis</tag>
      
      <tag>数据库</tag>
      
      <tag>算法</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>如何在Golang里实现一个高性能的TTLMap</title>
    <link href="/2020/05/24/%E5%A6%82%E4%BD%95%E5%9C%A8Golang%E9%87%8C%E5%AE%9E%E7%8E%B0%E4%B8%80%E4%B8%AA%E9%AB%98%E6%80%A7%E8%83%BD%E7%9A%84TTLMap/"/>
    <url>/2020/05/24/%E5%A6%82%E4%BD%95%E5%9C%A8Golang%E9%87%8C%E5%AE%9E%E7%8E%B0%E4%B8%80%E4%B8%AA%E9%AB%98%E6%80%A7%E8%83%BD%E7%9A%84TTLMap/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><code>TTLMap</code>是一个比较实用的数据结构，特别是在需要缓存数据的场景下。<code>TTLMap</code>的实现现不算复杂，但也有许多需要注意的地方，这也是这篇文章出现的原因。</p><p>在<code>TTLMap</code>里，移除失效的数据有三种策略：立即删除、惰性删除和定期删除策略。立即删除策略会在数据过期时立即将数据删除；惰性删除策略只在碰到过期键时才进行删除操作；定期删除策略则每隔一段时间，主动查找并删除过期键。第一种策略对CPU不友好，第二种策略容易造成内存泄露。<code>Redis</code>对设置了TTL的数据采取的是惰性删除和定期删除策略，而本篇文章会将重点放在性能上，所以只会采取定期删除策略。</p><blockquote><p>注：本篇文章使用<code>go1.13.5 linux/amd64</code></p></blockquote><h1 id="接口定义"><a href="#接口定义" class="headerlink" title="接口定义"></a>接口定义</h1><p>先来看看<code>TTLMap</code>的接口定义：</p><pre><code class="go">type TTLMap interface &#123;    Get(key string) (interface&#123;&#125;, bool)    Set(key string, val interface&#123;&#125;, ex time.Duration)    Len() int    clean(tick time.Duration)&#125;type item struct &#123;    value  interface&#123;&#125;    expire int64&#125;</code></pre><p><code>Get()</code>函数负责从<code>TTLMap</code>中获取数据，如数据不存在或已失效则第二个返回值为<code>false</code>，行为和内置的<code>map</code>保持一致。<code>Set()</code>函数负责将数据放入<code>TTLMap</code>，并接收一个<code>time.Duration</code>参数用来标示数据的生命周期。<code>Len()</code>函数返回<code>TTLMap</code>中数据的数量，不过为了性能考虑，一般不区分数据是否已经失效。<code>clean()</code>函数则负责实现上文提到的定期删除策略。</p><h1 id="初步实现（V0版本）"><a href="#初步实现（V0版本）" class="headerlink" title="初步实现（V0版本）"></a>初步实现（V0版本）</h1><pre><code class="go">type TTLMapV0 struct &#123;    items map[string]item    mux   *sync.Mutex&#125;func (tm *TTLMapV0) Get(key string) (interface&#123;&#125;, bool) &#123;    tm.mux.Lock()    defer tm.mux.Unlock()    if item, ok := tm.items[key]; !ok || time.Now().UnixNano() &gt; item.expire &#123;        return nil, false    &#125; else &#123;        return item.value, true    &#125;&#125;func (tm *TTLMapV0) Set(key string, val interface&#123;&#125;, ex time.Duration) &#123;    tm.mux.Lock()    tm.items[key] = item&#123;value: val, expire: time.Now().Add(ex).UnixNano()&#125;    tm.mux.Unlock()&#125;func (tm *TTLMapV0) Len() int &#123;    tm.mux.Lock()    defer tm.mux.Unlock()    return len(tm.items)&#125;func (tm *TTLMapV0) clean(tick time.Duration) &#123;    for range time.Tick(tick) &#123;        tm.mux.Lock()        for key, item := range tm.items &#123;            if time.Now().UnixNano() &gt;= item.expire &#123;                delete(tm.items, key)            &#125;        &#125;        tm.mux.Unlock()    &#125;&#125;func NewTTLMapV0(cleanTick time.Duration) *TTLMapV0 &#123;    tm := &amp;TTLMapV0&#123;items: map[string]item&#123;&#125;, mux: new(sync.Mutex)&#125;    go tm.clean(cleanTick)    return tm</code></pre><h1 id="防止内存泄露（V1版本）"><a href="#防止内存泄露（V1版本）" class="headerlink" title="防止内存泄露（V1版本）"></a>防止内存泄露（V1版本）</h1><p>上面的V0版本实现在功能上已经基本完整了，但由于<code>clean()</code>函数会一直运行，所以V0版本的<code>TTLMap</code>不会被Golang的垃圾回收机制回收，进而引起内存泄露。所以我们需要对V0版本做出一点改进，并将改进后的<code>TTLMap</code>包裹起来以启动自动回收机制：</p><pre><code class="go">type TTLMapV1 struct &#123;    *TTLMapV0    stop chan bool&#125;func (tm *TTLMapV1) clean(tick time.Duration) &#123;    for range time.Tick(tick) &#123;        select &#123;        case &lt;-tm.stop:            return        default:            tm.mux.Lock()            for key, item := range tm.items &#123;                if time.Now().UnixNano() &gt;= item.expire &#123;                    delete(tm.items, key)                &#125;            &#125;            tm.mux.Unlock()        &#125;    &#125;&#125;type TTLMapWrapper struct &#123;    TTLMap&#125;func NewTTLMapV1(cleanTick time.Duration) *TTLMapWrapper &#123;    tm0 := &amp;TTLMapV0&#123;items: map[string]item&#123;&#125;, mux: new(sync.Mutex)&#125;    tm1 := &amp;TTLMapV1&#123;TTLMapV0: tm0, stop: make(chan bool)&#125;    go tm1.clean(cleanTick)    tmw := &amp;TTLMapWrapper&#123;TTLMap: tm1&#125;    runtime.SetFinalizer(tmw, func(_ interface&#123;&#125;) &#123; tm1.stop &lt;- true &#125;)    return tmw&#125;</code></pre><h2 id="验证自动回收是否启用"><a href="#验证自动回收是否启用" class="headerlink" title="验证自动回收是否启用"></a>验证自动回收是否启用</h2><p>为了验证上文的改进是否有效，将代码改动如下：</p><pre><code class="go">func (tm *TTLMapV0) clean(tick time.Duration) &#123;    defer fmt.Println(&quot;v0.clean() stopped&quot;)    // ...&#125;// ...func NewTTLMapV0(cleanTick time.Duration) *TTLMapV0 &#123;    // ...    runtime.SetFinalizer(tm, func(_ interface&#123;&#125;) &#123; fmt.Println(&quot;v0 gone&quot;) &#125;)    return tm&#125;// ...func (tm *TTLMapV1) clean(tick time.Duration) &#123;    defer func() &#123; fmt.Println(&quot;v1.clean() stopped&quot;) &#125;()    // ...&#125;// ...func NewTTLMapV1(cleanTick time.Duration) *TTLMapWrapper &#123;    // ...    runtime.SetFinalizer(tm1, func(_ interface&#123;&#125;) &#123; fmt.Println(&quot;v1 gone&quot;) &#125;)    runtime.SetFinalizer(tm0, func(_ interface&#123;&#125;) &#123; fmt.Println(&quot;v1.v0 gone&quot;) &#125;)    return tmw&#125;</code></pre><p>然后编写测试代码并运行：</p><pre><code class="go">func TestTTLMap(t *testing.T) &#123;    func() &#123;        NewTTLMapV0(time.Millisecond * 100)        NewTTLMapV1(time.Millisecond * 100)    &#125;()    fmt.Println(&quot;--&gt; first gc&quot;)    runtime.GC()    time.Sleep(time.Millisecond * 200)    fmt.Println(&quot;--&gt; second gc&quot;)    runtime.GC()    fmt.Println(&quot;--&gt; third gc&quot;)    runtime.GC()&#125;</code></pre><pre><code class="shell">$ go test .=== RUN   TestTTLMap--&gt; first gcv1.clean() stopped--&gt; second gcv1 gone--&gt; third gcv1.v0 gone--- PASS: TestTTLMap (0.20s)</code></pre><p>可以看到，改进后的代码确实生效了，未被引用的<code>TTLMap</code>（V1版本）会被自动回收，不会出现内存泄露。</p><blockquote><p>PS：在接下来的版本将省略<code>TTLMapWrapper</code>的实现部分</p></blockquote><h1 id="使用读写锁提高性能（V2版本）"><a href="#使用读写锁提高性能（V2版本）" class="headerlink" title="使用读写锁提高性能（V2版本）"></a>使用读写锁提高性能（V2版本）</h1><p>在之前的版本里，<code>TTLMap</code>使用的并发控制锁都是普通的<code>sync.Mutex</code>，也就是说同一时刻只能有一个<code>Goruntime</code>访问该对象。但<code>TTLMap</code>应用的场景大多是读多写少，所以我们可以使用读写锁，即<code>sync.RWMutex</code>来获得更好性能：</p><pre><code class="go">type TTLMapV2 struct &#123;    items map[string]item    mux   *sync.RWMutex&#125;func (tm *TTLMapV2) Get(key string) (interface&#123;&#125;, bool) &#123;    tm.mux.RLock()    defer tm.mux.RUnlock()    if item, ok := tm.items[key]; !ok || time.Now().UnixNano() &gt; item.expire &#123;        return nil, false    &#125; else &#123;        return item.value, true    &#125;&#125;func (tm *TTLMapV2) Set(key string, val interface&#123;&#125;, ex time.Duration) &#123;    // ... 同V0版本&#125;func (tm *TTLMapV2) clean(tick time.Duration) &#123;    // ... 同V0版本&#125;func (tm *TTLMapV2) Len() int &#123;    tm.mux.RLock()    defer tm.mux.RUnlock()    return len(tm.items)&#125;func NewTTLMapV2(cleanTick time.Duration) *TTLMapV2 &#123;    tm := &amp;TTLMapV2&#123;map[string]item&#123;&#125;, new(sync.RWMutex)&#125;    go tm.clean(cleanTick)    return tm&#125;</code></pre><h1 id="使用不精确的时间提高性能（V3版本）"><a href="#使用不精确的时间提高性能（V3版本）" class="headerlink" title="使用不精确的时间提高性能（V3版本）"></a>使用不精确的时间提高性能（V3版本）</h1><p>在之前的版本里，每次调用<code>Get()</code>和<code>Set()</code>函数时都会调用<code>time.Now()</code>以获取精确时间。但在对时间的精确度要求不那么高的场合下，可以缓存<code>time.Now()</code>的结果来获得更好的性能：</p><pre><code class="go">type TTLMapV3 struct &#123;    items map[string]item    mux   *sync.RWMutex    now   int64&#125;func (tm *TTLMapV3) Get(key string) (interface&#123;&#125;, bool) &#123;    tm.mux.RLock()    defer tm.mux.RUnlock()    if item, ok := tm.items[key]; !ok || tm.now &gt; item.expire &#123;        return nil, false    &#125; else &#123;        return item.value, true    &#125;&#125;func (tm *TTLMapV3) Set(key string, val interface&#123;&#125;, ex time.Duration) &#123;    tm.mux.Lock()    tm.items[key] = item&#123;value: val, expire: tm.now + ex.Nanoseconds()&#125;    tm.mux.Unlock()&#125;func (tm *TTLMapV3) Len() int &#123;    // ... 同V2版本&#125;func (tm *TTLMapV3) clean(tick time.Duration) &#123;    for range time.Tick(tick) &#123;        tm.mux.Lock()        for key, item := range tm.items &#123;            if tm.now &gt;= item.expire &#123;                delete(tm.items, key)            &#125;        &#125;        tm.mux.Unlock()    &#125;&#125;func (tm *TTLMapV3) updateNow(tick time.Duration) &#123;    for range time.Tick(tick) &#123;        tm.mux.Lock()        tm.now = time.Now().UnixNano()        tm.mux.Unlock()    &#125;&#125;func NewTTLMapV3(cleanTick time.Duration) *TTLMapV3 &#123;    tm := &amp;TTLMapV3&#123;map[string]item&#123;&#125;,        new(sync.RWMutex), time.Now().UnixNano()&#125;    go tm.clean(cleanTick)    go tm.updateNow(time.Second)    return tm&#125;</code></pre><h1 id="性能测试-amp-结语"><a href="#性能测试-amp-结语" class="headerlink" title="性能测试&amp;结语"></a>性能测试&amp;结语</h1><p>编写测试代码用于测试以上版本的性能并运行：</p><pre><code class="go">func BenchmarkTTLMap(b *testing.B) &#123;    rand.Seed(time.Now().UnixNano())    key := RandStringBytesRmndr(20)    tasks := []struct &#123;        name string        ttl  TTLMap    &#125;&#123;        &#123;&quot;TTLMapV0&quot;, NewTTLMapV0(time.Minute)&#125;,        &#123;&quot;TTLMapV2&quot;, NewTTLMapV2(time.Minute)&#125;,        &#123;&quot;TTLMapV3&quot;, NewTTLMapV3(time.Minute)&#125;,    &#125;    for _, task := range tasks &#123;        b.Run(task.name, func(sb *testing.B) &#123;            sb.RunParallel(func(pb *testing.PB) &#123;                for pb.Next() &#123;                    task.ttl.Set(key, key, time.Minute)                    task.ttl.Get(key)                &#125;            &#125;)        &#125;)    &#125;&#125;</code></pre><pre><code class="shell">$ go test -bench .  goos: linuxgoarch: amd64BenchmarkTTLMap/TTLMapV0-8               3972699               301 ns/opBenchmarkTTLMap/TTLMapV2-8               4972598               241 ns/opBenchmarkTTLMap/TTLMapV3-8               9125235               132 ns/opPASS</code></pre><p>可以看出，V2相比V0性能提升了20%左右，而V3相比V0的性能提升幅度更是超过了56%。而从PPROF的分析结果来看，剩下的时间主要消耗在互斥锁上，应该很难有进一步优化的空间了。</p><p><img src="/images/2020/05/24/pprof.webp"></p><p>虽然理论上来说用<code>int</code>当<code>Map</code>的键而不是<code>string</code>时会有更好的性能表现，但是从实际测试来看，对于长度为20的<code>string</code>，<code>fnv32</code>等哈希算法的开销还是超过了其能带来的收益，更别提还要考虑潜在的哈希冲突。</p><p>最后要重申一点的是，V2和V3版本的实现省略了V1中提到的<code>TTLMapWrapper</code>部分，所以请不要直接使用V2和V3版本的代码😂</p><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul><li><a href="https://github.com/golang/go/issues/19780">proposal: a faster, better fnv32 implementation #19780</a></li><li><a href="https://my.oschina.net/solate/blog/3034188">go benchmark 性能测试</a></li><li><a href="https://zhuanlan.zhihu.com/p/76504936">使用runtime.SetFinalizer优雅关闭后台goroutine</a></li><li><a href="http://redisbook.com/preview/database/review.html">重点回顾-Redis设计与实现</a></li><li><a href="https://stackoverflow.com/questions/22892120/how-to-generate-a-random-string-of-a-fixed-length-in-go">How to generate a random string of a fixed length in Go?</a></li><li><a href="https://medium.com/justforfunc/analyzing-the-performance-of-go-functions-with-benchmarks-60b8162e61c6">Analyzing the performance of Go functions with benchmarks</a></li><li><a href="https://stackimpact.com/blog/practical-golang-benchmarks/#map-access">Practical Go Benchmarks</a></li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>Golang</tag>
      
      <tag>map</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Django多数据库历险记（番外）</title>
    <link href="/2020/05/02/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%89%EF%BC%89/"/>
    <url>/2020/05/02/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%89%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<h1 id="前情提要"><a href="#前情提要" class="headerlink" title="前情提要"></a>前情提要</h1><p>这篇文章基于前两篇文章<a href="/2020/04/23/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%80%EF%BC%89/">Django多数据库历险记（一）</a>、<a href="/2020/04/25/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%BA%8C%EF%BC%89">Django多数据库历险记（二）</a>，将继续讲述关于Django多数据库的历险记。<br>好吧其实这篇文章的内容已经和Django多数据库没有太大关系了……只是为了和前两篇文章的命名保持一致才取了这个标题。</p><h1 id="秘技：避免物理外键约束"><a href="#秘技：避免物理外键约束" class="headerlink" title="秘技：避免物理外键约束"></a>秘技：避免物理外键约束</h1><p><a href="https://docs.djangoproject.com/en/2.2/ref/models/fields/#django.db.models.ForeignKey.db_constraint"><img src="/images/2020/05/02/document.webp" alt="document.webp"></a></p><p>对于数据库层面的物理外键，国内互联网上的声音普遍一致，那就是不推荐使用（比如这个<a href="https://www.zhihu.com/question/39062169">知乎问题</a>），公司的DBA也持这个态度。既然如此，就应该想办法在执行<code>Migrate</code>操作时避免产生物理外键了。<br>幸运的时，从很早开始（不晚于<code>Django1.8</code>）Django就提供了直接的手段来避免产生物理外键：<code>db_constraint</code>属性。从Django DB模块的源码（<a href="https://github.com/django/django/blob/2.2.12/django/db/backends/base/schema.py">schema.py</a>）中可以看出，Django在创建Model、添加&#x2F;修改Field时都会通过<code>db_constraint</code>属性的值来决定是否加入物理外键约束。实验一下（省略内容见<a href="/2020/04/23/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%80%EF%BC%89/">上一篇文章</a>）：</p><pre><code class="python"># app_1/models.py# ... 略class ChildModel1(models.Model):    name = models.CharField(max_length=255)    parent1 = models.ForeignKey(&quot;Model1&quot;, on_delete=models.CASCADE, db_constraint=False)    parent2 = models.ForeignKey(&quot;app_2.Model2&quot;, on_delete=models.CASCADE, db_constraint=False)</code></pre><pre><code class="shell">$ python manage.py makemigrationsMigrations for &#39;app_1&#39;:  app_1/migrations/0002_childmodel1.py    - Create model ChildModel1$$ python manage.py migrate app_1 --database db_1Operations to perform:  Apply all migrations: app_1Running migrations:  Applying app_2.0001_initial... OK  Applying app_1.0002_childmodel1... OK</code></pre><pre><code class="sql">mariadb root@127.0.0.1:db_1&gt; select * from django_migrations+----+-------+------------------+----------------------------+| id | app   | name             | applied                    |+----+-------+------------------+----------------------------+| 1  | app_1 | 0001_initial     | 2020-xx-xx xx:xx:xx.xxxxxx || 3  | app_2 | 0001_initial     | 2020-xx-xx xx:xx:xx.xxxxxx || 3  | app_1 | 0002_childmodel1 | 2020-xx-xx xx:xx:xx.xxxxxx |+----+-------+------------------+----------------------------+mariadb root@127.0.0.1:db_1&gt; show tables+-------------------+| Tables_in_db_1    |+-------------------+| app_1_childmodel1 || app_1_model1      || django_migrations |+-------------------+mariadb root@127.0.0.1:db_1&gt; show create table app_1_model1CREATE TABLE `app_1_childmodel1` (  `id` int(11) NOT NULL AUTO_INCREMENT,  `name` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,  `parent1_id` int(11) NOT NULL,  `parent2_id` int(11) NOT NULL,  PRIMARY KEY (`id`),  KEY `app_1_childmodel1_parent1_id_8dfbb0b1` (`parent1_id`),  KEY `app_1_childmodel1_parent2_id_db0fdb2e` (`parent2_id`)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci</code></pre><h1 id="秘技：migrations转SQL语句"><a href="#秘技：migrations转SQL语句" class="headerlink" title="秘技：migrations转SQL语句"></a>秘技：<code>migrations</code>转SQL语句</h1><p><a href="https://docs.djangoproject.com/en/2.2/ref/django-admin/#django-admin-sqlmigrate"><img src="/images/2020/05/02/sqlmigrate.webp" alt="sqlmigrate.webp"></a></p><p>在某些特殊场景下（比如处于安全考量），Django自带的<code>migrate</code>命令可能不被允许运行。如果此时还想使用Django的数据迁移功能，则可以使用<code>sqlmigrate</code>命令来将创建好的迁移方案转换为SQL语句。比如：</p><pre><code class="shell">$ python manage.py sqlmigrate app_1 0001_initial --database db_1BEGIN;---- Create model Model1--CREATE TABLE `app_1_model1` (`id` integer AUTO_INCREMENT NOT NULL PRIMARY KEY, `name` varchar(255) NOT NULL);COMMIT;</code></pre><p>不过这个命令并一次只能转换一条迁移方案，所以我们可以参考魔改一下：</p><pre><code class="python"># my_sqlmigrate.py#!/usr/bin/env pythonimport osimport djangofrom django.conf import settingsfrom django.core.management import execute_from_command_line, CommandParserfrom django.db import DEFAULT_DB_ALIASfrom django.db.migrations.loader import MigrationLoaderos.environ.setdefault(&#39;DJANGO_SETTINGS_MODULE&#39;, &#39;multi_db.settings&#39;)django.setup()parser = CommandParser()parser.add_argument(    &#39;app_label&#39;, nargs=&#39;?&#39;,    help=&#39;App label of an application to synchronize the state.&#39;,)app_label = parser.parse_args().app_labelloader = MigrationLoader(None)  # 获取所有migrationdb = settings.DB_ROUTING.get(app_label, DEFAULT_DB_ALIAS)# 遍历app下所有migrationmigrations = sorted(name for app, name in loader.disk_migrations if app == app_label)for migration in migrations:    argv = [&quot;manage.py&quot;, &quot;sqlmigrate&quot;, &quot;--database&quot;, db, app_label, migration]    print(&quot;\n-- python &quot; + &quot; &quot;.join(argv))    execute_from_command_line(argv)print(&quot;-- my_sqlmigrate complete&quot;)</code></pre><p>现在可以一次性转换所有迁移方案了：</p><pre><code class="shell">$ python my_sqlmigrate.py app_1-- python manage.py sqlmigrate --database db_1 app_1 0001_initialBEGIN;---- Create model Model1--CREATE TABLE `app_1_model1` (`id` integer AUTO_INCREMENT NOT NULL PRIMARY KEY, `name` varchar(255) NOT NULL);COMMIT;-- python manage.py sqlmigrate --database db_1 app_1 0002_childmodel1BEGIN;---- Create model ChildModel1--CREATE TABLE `app_1_childmodel1` (`id` integer AUTO_INCREMENT NOT NULL PRIMARY KEY, `name` varchar(255) NOT NULL, `parent1_id` integer NOT NULL, `parent2_id` integer NOT NULL);CREATE INDEX `app_1_childmodel1_parent1_id_8dfbb0b1` ON `app_1_childmodel1` (`parent1_id`);CREATE INDEX `app_1_childmodel1_parent2_id_db0fdb2e` ON `app_1_childmodel1` (`parent2_id`);COMMIT;-- my_sqlmigrate complete</code></pre><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>文章的实用性可能谈不上有多高，只能说聊以自慰。虽然我在第一篇文章的开头说<code>对多数据库的支持……坑无处不在</code>，但在深入了解之后，我还是被Django完善的架构给折服了：不愧是最优秀的Python Web框架，我还是too young, too simple。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>Python</tag>
      
      <tag>Django</tag>
      
      <tag>多数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Django多数据库历险记（二）</title>
    <link href="/2020/04/25/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%BA%8C%EF%BC%89/"/>
    <url>/2020/04/25/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%BA%8C%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<h1 id="前情提要"><a href="#前情提要" class="headerlink" title="前情提要"></a>前情提要</h1><p>这篇文章基于上一篇文章<a href="/2020/04/23/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%80%EF%BC%89/">Django多数据库历险记（一）</a>，将继续讲述关于Django多数据库的历险记。</p><p>在上一篇文章中，我创建了一个Django项目：<code>multi_db</code>，在这个项目里指定了两个app：<code>app_1</code>和<code>app_2</code>，每个app下各自创建了一个<code>Model</code>：<code>Model1</code>和<code>Model2</code>，并为这两个app各自分配了独立的数据库<code>db_1</code>和<code>db_2</code>。历险继续～</p><h1 id="第三关：TestCase"><a href="#第三关：TestCase" class="headerlink" title="第三关：TestCase"></a>第三关：TestCase</h1><p>在<code>multi_db</code>这个Django项目能够运行起来之后，下一步让我们来运行一些TestCase（对，即使这个项目里一个视图函数都还没有，<del>毕竟是TDD😂</del>）。编辑TestCase文件并运行：</p><pre><code class="python"># tests/test_multi_db.pyfrom django.test import TestCasefrom app_1.models import Model1from app_2.models import Model2class TestDB1(TestCase):    databases = [&quot;db_1&quot;]    def test_query(self):        count = Model1.objects.count()        self.assertEqual(count, 0)class TestDB2(TestCase):    databases = [&quot;db_2&quot;]    def test_query(self):        count = Model2.objects.count()        self.assertEqual(count, 0)</code></pre><pre><code class="shell">$ time python manage.py test tests/Creating test database for alias &#39;db_1&#39;...Creating test database for alias &#39;db_2&#39;...System check identified no issues (0 silenced)...----------------------------------------------------------------------Ran 2 tests in 0.006sOKDestroying test database for alias &#39;db_1&#39;...Destroying test database for alias &#39;db_2&#39;...python manage.py test tests/  1.02s user 0.04s system 14% cpu 7.288 total</code></pre><p>很好，两个TestCase都测试通过（毕竟没有比这更简单的TestCase了）。一切都看起来非常美好，除了这惊人的命令耗时：为什么创建&amp;销毁两个测试数据库会消耗7秒以上的时间！如果你看过上一篇文章，那么你可能已经隐约猜到原因了。没错，幕后黑手还是<code>migrate</code>命令。</p><p>从Django测试模块源码（<a href="https://github.com/django/django/blob/2.2.12/django/test/runner.py#L614">runner.py#L614</a>）中可以看出，对于TestCase里所有的<code>databases</code>（比如上面的<code>db_1</code>、<code>db_2</code>），Django都会创建对应的测试数据库（<code>test_db_1</code>、<code>test_db_2</code>），并为每个测试数据库执行一次<code>migrate</code>命令（<a href="https://github.com/django/django/blob/2.2.12/django/db/backends/base/creation.py#L67">creation.py#L67</a>）。在上一篇文章中，我们就已经知道<code>migrate</code>命令会一股脑地将所有迁移方案都应用到数据库，这里也不例外。用<code>test</code>命令的<code>--keepdb</code>选项验证一下：</p><pre><code class="shell">$ python manage.py test tests/ --keepdb# ... 略</code></pre><pre><code class="sql">mariadb root@127.0.0.1:test_db_1&gt; show tables+----------------------------+| Tables_in_test_db_1        |+----------------------------+| app_1_model1               || app_2_model2               || auth_group                 |# ... 略</code></pre><pre><code class="sql">mariadb root@127.0.0.1:test_db_2&gt; show tables+----------------------------+| Tables_in_test_db_2        |+----------------------------+| app_1_model1               || app_2_model2               || auth_group                 |# ... 略</code></pre><p>那么，有没有办法在对<code>db_1</code>进行测试时，只在<code>test_db_1</code>这个测试数据库上创建只属于<code>app_1</code>的<code>Model</code>呢？答案是肯定的。</p><h2 id="allow-migrate"><a href="#allow-migrate" class="headerlink" title="allow_migrate"></a>allow_migrate</h2><p>在上一篇文章的<code>DBRouter</code>数据库路由类中，我将<code>allow_migrate()</code>方法的返回值固定为<code>True</code>，意味着Django可以在任何数据库上、对任何app进行数据库迁移。这当然是一种有点不负责任的做法，所以，只要严格限制在<code>db_1</code>上只能进行<code>app_1</code>的迁移、在<code>db_2</code>上只能进行<code>app_2</code>的迁移，就可以达成我上文提到的需求了。</p><pre><code class="python"> # multi_db/db_routers.pyfrom django.conf import settingsfrom django.db import DEFAULT_DB_ALIASclass DBRouter:    # ... 略    def allow_migrate(self, db, app_label, model_name=None, **hints):        return db == settings.DB_ROUTING.get(app_label, DEFAULT_DB_ALIAS)</code></pre><h2 id="大功告成"><a href="#大功告成" class="headerlink" title="大功告成"></a>大功告成</h2><p>再次执行<code>test</code>命令，可以很明显地感受到效果：</p><pre><code class="shell">$ time python manage.py test tests/Creating test database for alias &#39;db_1&#39;...Creating test database for alias &#39;db_2&#39;...# ... 略python manage.py test tests/  0.74s user 0.02s system 57% cpu 1.320 total</code></pre><p>总耗时由7秒下降至1秒。再用<code>--keepdb</code>选项验证一下：</p><pre><code>mariadb root@127.0.0.1:test_db_1&gt; show tables                                   +---------------------+| Tables_in_test_db_1 |+---------------------+| app_1_model1        || django_migrations   |+---------------------+</code></pre><p>完美。</p><h1 id="第四关：跨库外键约束"><a href="#第四关：跨库外键约束" class="headerlink" title="第四关：跨库外键约束"></a>第四关：跨库外键约束</h1><p><img src="/images/2020/04/27/document.webp" alt="document.webp"></p><p>必须要说的是，Django（至少在文档里）并不支持跨数据库的外键约束。当然，这里指的是物理约束，即数据库层面的<code>CONSTRAINT</code>字段约束。但在实际业务中，由Django管理的、逻辑上的跨库外键约束却并不少见，这给程序员带来了巨大的便利——但代价是什么呢？代价往往是抛弃Django的数据迁移机制。</p><h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>在<code>app_1</code>里创建一个新Model（省略内容见<a href="/2020/04/23/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%80%EF%BC%89/">上一篇文章</a>），并生成对应迁移方案：</p><pre><code class="python"># app_1/models.py# ... 略class ChildModel1(models.Model):    name = models.CharField(max_length=255)    parent1 = models.ForeignKey(&quot;Model1&quot;, on_delete=models.CASCADE)    parent2 = models.ForeignKey(&quot;app_2.Model2&quot;, on_delete=models.CASCADE)</code></pre><pre><code class="shell">$ python manage.py makemigrations               Migrations for &#39;app_1&#39;:  app_1/migrations/0002_childmodel1.py    - Create model ChildModel1</code></pre><h2 id="迁移"><a href="#迁移" class="headerlink" title="迁移"></a>迁移</h2><pre><code class="shell">$ python manage.py migrate app_1 --database db_1Operations to perform:  Apply all migrations: app_1Running migrations:  Applying app_2.0001_initial... OK  Applying app_1.0002_childmodel1...# ... 略django.db.utils.OperationalError: (1005, &#39;Can\&#39;t create table `db_1`.`app_1_childmodel1` (errno: 150 &quot;Foreign key constraint is incorrectly formed&quot;)&#39;)</code></pre><p>果不其然，报错了。Django想要在<code>db_1</code>上应用<code>app_2</code>的迁移方案<code>0001_initial</code>来为<code>ChildModel1</code>的迁移铺路，但在上一关的<code>allow_migrate()</code>中我已经严格限制了数据库和app的对应关系，所以实际上<code>app_2.0001_initial</code>被跳过了（虽然还是在迁移进度里留下了成功标记）。</p><p>于是到了物理约束部分，Django想要在<code>db1</code>上创建从<code>ChildModel1</code>到<code>Model2</code>的<code>CONSTRAINT</code>约束就不可能成功：因为<code>app_2.0001_inital</code>这个前提并没有实际应用。打开数据库验证一下：</p><pre><code class="sql">mariadb root@127.0.0.1:db_1&gt; show tables                                        +-------------------+| Tables_in_db_1    |+-------------------+| app_1_childmodel1 || app_1_model1      || django_migrations |+-------------------+mariadb root@127.0.0.1:db_1&gt; select * from django_migrations+----+-------+------------------+----------------------------+| id | app   | name             | applied                    |+----+-------+------------------+----------------------------+| 1  | app_1 | 0001_initial     | 2020-04-27 xx:xx:xx.xxxxxx || 2  | app_2 | 0001_initial     | 2020-04-27 xx:xx:xx.xxxxxx || 3  | app_1 | 0002_childmodel1 | 2020-04-27 xx:xx:xx.xxxxxx |+----+-------+------------------+----------------------------+mariadb root@127.0.0.1:db_1&gt; show create table app_1_childmodel1CREATE TABLE `app_1_childmodel1` (  `id` int(11) NOT NULL AUTO_INCREMENT,  `name` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,  `parent1_id` int(11) NOT NULL,  `parent2_id` int(11) NOT NULL,  PRIMARY KEY (`id`),  KEY `xxx` (`parent1_id`),  CONSTRAINT `xxx` FOREIGN KEY (`parent1_id`) REFERENCES `app_1_model1` (`id`)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci</code></pre><h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>当然，即使Django在应用迁移方案的时候报错了，但缺失的只有一个数据库层面的约束字段，并不影响逻辑层面的外键约束。所以，我们可以直接在Django里使用这个Model了：</p><pre><code class="python"># app_1/views.pyfrom django.http import JsonResponsefrom app_1.models import ChildModel1, Model1from app_2.models import Model2def test(request):    child = ChildModel1.objects.create(        name=&quot;carl&quot;, parent1=Model1.objects.create(name=&quot;alice&quot;),        parent2=Model2.objects.create(name=&quot;bob&quot;),    )    return JsonResponse(&#123;        &quot;name&quot;: child.name, &quot;parent1&quot;: child.parent1.name,        &quot;parent2&quot;: child.parent2.name    &#125;)</code></pre><pre><code class="python"># multi_db/urls.py# ... 略from app_1.views import testurlpatterns = [    path(&#39;admin/&#39;, admin.site.urls),    path(&#39;&#39;, test)]</code></pre><pre><code class="shell">$ python manage.py runserver 8000Performing system checks...Watching for file changes with StatReloaderSystem check identified no issues (0 silenced).April 27, 2020 - xx:xx:xx# 新termial$ curl 127.0.0.1:8000&#123;&quot;name&quot;: &quot;carl&quot;, &quot;parent1&quot;: &quot;alice&quot;, &quot;parent2&quot;: &quot;bob&quot;&#125;</code></pre><h2 id="TestCase"><a href="#TestCase" class="headerlink" title="TestCase"></a>TestCase</h2><p>在上一节中可以这样蒙混过关，是因为<code>migrate</code>命令的失败并不会影响<code>runserver</code>命令的执行，但在TestCase里这样就行不通了。在第三关中提到，Django在进行TestCase前会对测试数据库执行<code>migrate</code>命令，而<code>migrate</code>的失败会导致TestCase直接结束。</p><p>解决方案主要有取消<code>allow_migrate()</code>对app&amp;数据库对应关系的限制、在运行TestCase时使用sqlite3（默认禁用外键约束）作为测试数据库。但这些都不能从根本上解决问题，这也就是为什么我在本关开头提到，跨库外键的代价往往是抛弃Django的数据迁移机制。</p><p>要想从根本上解决这个问题，就只有避免出现物理上的外键约束，只由Django进行逻辑上的外键约束管理。这个话题，就留到下篇文章再讲吧。（我怎么感觉我要被打了……）</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>Python</tag>
      
      <tag>Django</tag>
      
      <tag>多数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Django多数据库历险记（一）</title>
    <link href="/2020/04/23/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%80%EF%BC%89/"/>
    <url>/2020/04/23/Django%E5%A4%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8E%86%E9%99%A9%E8%AE%B0%EF%BC%88%E4%B8%80%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>毫无疑问，Django是最优秀的Python Web框架之一，然而其对多数据库的支持却让我内心十分复杂。在数据库迁移、跨库外键、单元测试等方面，坑无处不在。于是就有了这篇文章，关于Django多数据库的历险记。</p><blockquote><p>注：本篇文章使用<code>Python 3.6</code>+<code>Django 2.2</code></p></blockquote><h1 id="准备出发"><a href="#准备出发" class="headerlink" title="准备出发"></a>准备出发</h1><ol><li><p>创建Django项目<code>multi_db</code>和两个app：<code>app_1</code>和<code>app_2</code></p><pre><code class="shell">$ django-admin startproject multi_db$ cd multi_db/$ python manage.py startapp app_1$ python manage.py startapp app_2</code></pre></li><li><p>在<code>app_1</code>和<code>app_2</code>里分别添加各自的<code>Model</code></p><pre><code class="python"># app_1/models.pyfrom django.db import modelsclass Model1(models.Model):    name = models.CharField(max_length=255)    class Meta:        app_label = &quot;app_1&quot;</code></pre><pre><code class="python"># app_2/models.pyfrom django.db import modelsclass Model2(models.Model):    name = models.CharField(max_length=255)    class Meta:        app_label = &quot;app_2&quot;</code></pre></li><li><p>为两个app指定不同的数据库并为其编写路由文件（省略部分内容）</p><pre><code class="python"># multi_db/settings.py# ...INSTALLED_APPS = [    # ...    &#39;app_1&#39;,    &#39;app_2&#39;,]# ...DATABASES = &#123;    &#39;default&#39;: &#123;        &#39;ENGINE&#39;: &#39;django.db.backends.sqlite3&#39;,        &#39;NAME&#39;: os.path.join(BASE_DIR, &#39;db.sqlite3&#39;),    &#125;,    &#39;db_1&#39;: &#123;        &#39;ENGINE&#39;: &#39;django.db.backends.mysql&#39;, &#39;NAME&#39;: &#39;db_1&#39;,        &#39;HOST&#39;: &#39;127.0.0.1&#39;, &#39;PORT&#39;: &#39;6603&#39;, &#39;USER&#39;: &#39;root&#39;,        &#39;PASSWORD&#39;: &#39;password&#39;, &#39;CHARSET&#39;: &#39;utf-8&#39;, &#39;TEST&#39;: &#123;&#39;DEPENDENCIES&#39;: []&#125;,    &#125;,    &#39;db_2&#39;: &#123;        &#39;ENGINE&#39;: &#39;django.db.backends.mysql&#39;, &#39;NAME&#39;: &#39;db_2&#39;,        &#39;HOST&#39;: &#39;127.0.0.1&#39;, &#39;PORT&#39;: &#39;6603&#39;, &#39;USER&#39;: &#39;root&#39;,        &#39;PASSWORD&#39;: &#39;password&#39;, &#39;CHARSET&#39;: &#39;utf-8&#39;, &#39;TEST&#39;: &#123;&#39;DEPENDENCIES&#39;: []&#125;,    &#125;,&#125;DB_ROUTING = &#123;&#39;app_1&#39;: &#39;db_1&#39;, &#39;app_2&#39;: &#39;db_2&#39;&#125;DATABASE_ROUTERS = [&#39;multi_db.db_routers.DBRouter&#39;]# ...</code></pre><pre><code class="python"># multi_db/db_routers.pyfrom django.conf import settingsclass DBRouter:    def db_for_read(self, model, **hints):        return settings.DB_ROUTING.get(model._meta.app_label)    def db_for_write(self, model, **hints):        return settings.DB_ROUTING.get(model._meta.app_label)    def allow_relation(self, obj1, obj2, **hints):        return True    def allow_migrate(self, db, app_label, model_name=None, **hints):        return True</code></pre></li></ol><h1 id="第一关：数据迁移"><a href="#第一关：数据迁移" class="headerlink" title="第一关：数据迁移"></a>第一关：数据迁移</h1><p>Django里数据库迁移分两部分，<code>makemigrations</code>和<code>migrate</code>。前者负责在Model出现变动时生成迁移方案文件，后者则负责在数据库中应用前者的迁移方案。于是，麻烦来了。</p><h2 id="makemigrations"><a href="#makemigrations" class="headerlink" title="makemigrations"></a>makemigrations</h2><p><code>makemigrations</code>命令不涉及数据库操作，所以用起来比较简单。使用时可以指定app以检测部分Model，也可以不指定app以检测全部Model。</p><pre><code class="shell">$ python manage.py makemigrationsMigrations for &#39;app_1&#39;:  app_1/migrations/0001_initial.py    - Create model Model1Migrations for &#39;app_2&#39;:  app_2/migrations/0001_initial.py    - Create model Model2</code></pre><p>OK，两个app的迁移方案文件就生成好了。Django内置的一些app（比如<code>django.contrib.auth</code>）已经<a href="https://github.com/django/django/blob/2.2.12/django/contrib/auth/migrations">自带迁移方案</a>，所以这里没有生成迁移方案文件。</p><h2 id="migrate"><a href="#migrate" class="headerlink" title="migrate"></a>migrate</h2><p><code>migrate</code>命令和<code>makemigrations</code>一样，可以指定app。这里我们不指定app，直接应用所有迁移方案。</p><pre><code class="shell">$ multi_db python manage.py migrateOperations to perform:  Apply all migrations: admin, app_1, app_2, auth, contenttypes, sessionsRunning migrations:  Applying contenttypes.0001_initial... OK  Applying auth.0001_initial... OK  Applying admin.0001_initial... OK  # ...  Applying app_1.0001_initial... OK  Applying app_2.0001_initial... OK  # ...  Applying sessions.0001_initial... OK</code></pre><p>很好，包括内置app、自定义app的所有迁移方案全部完成。按照前面的配置，默认的<code>db.sqlite3</code>里应该出现Django内置app的Model、<code>db_1</code>里应该出现<code>app_1</code>的Model、<code>db2</code>里应该出现<code>app_2</code>的Model，对吧？</p><p>现实是残酷的。从<code>migrate</code>模块的源码（<a href="https://github.com/django/django/blob/2.2.12/django/core/management/commands/migrate.py#L81">migrate.py#L81</a>）可以看出，<code>migrate</code>在应用迁移方案时根本不会考虑<code>DATABASE_ROUTERS</code>里的<code>db_for_read</code>和<code>db_for_write</code>，只会对参数<code>--database</code>指定的数据库（如不指定则为默认数据库）进行操作——也就是说，这里所有的Model都被一股脑写入到了默认的<code>db.sqlite3</code>中：</p><pre><code class="shell">$ sqlite3 db.sqlite3 .tablesapp_1_model1                auth_user_groupsapp_2_model2                auth_user_user_permissionsauth_group                  django_admin_logauth_group_permissions      django_content_typeauth_permission             django_migrationsauth_user                   django_session</code></pre><p>很明显，这不是我想要的。如何补救呢？措施有两种。</p><h3 id="补救措施1"><a href="#补救措施1" class="headerlink" title="补救措施1"></a>补救措施1</h3><p>第一种补救措施是在生成自定义app的迁移方案之前抢先完成对内置app的迁移方案，然后再进行自定义app的迁移。这种措施需要删除已经生成好的迁移方案文件。当然，这个补救措施有点耍滑头了。</p><pre><code class="shell">$ rm db.sqlite3$ rm app_?/migrations/0001_initial.py$ # 迁移内置app$ python manage.py migrateOperations to perform:  Apply all migrations: admin, auth, contenttypes, sessionsRunning migrations:  Applying contenttypes.0001_initial... OK  # ...$ # 重新生成自定义app的model的迁移方案$ python manage.py makemigrationsMigrations for &#39;app_1&#39;:  app_1/migrations/0001_initial.py    - Create model Model1Migrations for &#39;app_2&#39;:  app_2/migrations/0001_initial.py    - Create model Model2$ # 迁移时指定数据库$ python manage.py migrate app_1 --database db_1Operations to perform:  Apply all migrations: app_1Running migrations:  Applying app_1.0001_initial... OK$ python manage.py migrate app_2 --database db_2Operations to perform:  Apply all migrations: app_2Running migrations:  Applying app_2.0001_initial... OK</code></pre><h3 id="补救措施2"><a href="#补救措施2" class="headerlink" title="补救措施2"></a>补救措施2</h3><p>第二种补救措施不必删除已经生成好的自定义app的迁移方案文件，而是单独迁移内置app。虽然<code>migrate</code>命令一次只能迁移一个app，但所幸需要用到的内置app一般不会太多。</p><pre><code class="shell">$ rm db.sqlite3$ python manage.py migrate adminOperations to perform:  Apply all migrations: adminRunning migrations:  Applying contenttypes.0001_initial... OK  # ...$ python manage.py migrate auth# ...$ python manage.py migrate contenttypes# ...$ python manage.py migrate sessions# ...$ # 单独迁移完内置app后再迁移自定义app$ python manage.py migrate app_1 --database db_1# ...$ python manage.py migrate app_2 --database db_2# ...</code></pre><h2 id="自定义migrate"><a href="#自定义migrate" class="headerlink" title="自定义migrate"></a>自定义migrate</h2><p>前文提到的两个补救措施终究只是一时的权宜之计，用起来也很麻烦。反正只是多运行几次<code>migrate</code>命令，为什么不交给python来做呢？编辑<code>my_migrate.py</code>如下：</p><pre><code class="python"># my_migrate.py#!/usr/bin/env pythonimport osimport djangofrom django.apps import apps, AppConfigfrom django.conf import settingsfrom django.core.management import execute_from_command_line, CommandErrorfrom django.db import DEFAULT_DB_ALIASos.environ.setdefault(&#39;DJANGO_SETTINGS_MODULE&#39;, &#39;multi_db.settings&#39;)django.setup()for app_config in list(apps.get_app_configs()):  # type: AppConfig    app_label = app_config.label  # type: str    db = settings.DB_ROUTING.get(app_config.label, DEFAULT_DB_ALIAS)    argv = [&quot;manage.py&quot;, &quot;migrate&quot;, &quot;--traceback&quot;, &quot;--database&quot;, db, app_label]    print(&quot;python &quot; + &quot; &quot;.join(argv))    try:        execute_from_command_line(argv)    except CommandError as e:        print(e)    finally:        print()print(&quot;migrate complete&quot;)</code></pre><p>一股熟悉的味道……没错，看起来就像是<code>manage.py</code>的魔改版本。代码在<code>migrate</code>某app之前会去配置文件里的<code>DB_ROUTING</code>字典查找该app对应的数据库，再用这个数据库来<code>migrate</code>这个app。运行效果如下：</p><pre><code class="shell">$ python my_migrate.pypython manage.py migrate --traceback --database default adminOperations to perform:  Apply all migrations: adminRunning migrations:  Applying contenttypes.0001_initial... OK  # ... 略python manage.py migrate --traceback --database db_1 app_1# ... 略</code></pre><h2 id="大功告成"><a href="#大功告成" class="headerlink" title="大功告成"></a>大功告成</h2><p>于是，这个Django项目的数据库迁移工作终于完成了，太不容易了……验证一下：</p><pre><code class="shell">$ sqlite3 db.sqlite3 .tablesauth_group                  auth_user_user_permissionsauth_group_permissions      django_admin_logauth_permission             django_content_typeauth_user                   django_migrationsauth_user_groups            django_session</code></pre><pre><code class="sql">mariadb root@127.0.0.1:db_1&gt; show tables+-------------------+| Tables_in_db_1    |+-------------------+| app_1_model1      || django_migrations |+-------------------+</code></pre><pre><code class="sql">mariadb root@127.0.0.1:db_2&gt; show tables+-------------------+| Tables_in_db_2    |+-------------------+| app_2_model2      || django_migrations |+-------------------+</code></pre><h1 id="第二关：运行"><a href="#第二关：运行" class="headerlink" title="第二关：运行"></a>第二关：运行</h1><p>数据库迁移（终于）完成后，该运行这个Django项目了：</p><pre><code class="shell">$ python manage.py runserverWatching for file changes with StatReloaderPerforming system checks...System check identified no issues (0 silenced).You have 2 unapplied migration(s). Your project may not work properly until you apply the migrations for app(s): app_1, app_2.Run &#39;python manage.py migrate&#39; to apply them.April 23, 2020 - xx:xx:xx</code></pre><p>等等，为什么我在上一步已经完成了全部迁移工作，Django还会提示<code>app_1</code>和<code>app_2</code>未迁移？虽然这并不影响运行，但鲜红的提示总是令人不爽。</p><p>从<code>apply_migration()</code>的源码（<a href="https://github.com/django/django/blob/2.2.12/django/db/migrations/executor.py#L246">executor.py#L246</a>）中可以看出，在<code>migrate</code>命令过程中，每执行完一条迁移任务，Django都会在同数据库的<code>django_migrations</code>表中写入一条迁移完成的标记，用于记录迁移进度。实际上，<code>db_1</code>和<code>db_2</code>的<code>django_migrations</code>也是有对应标记的。</p><p>但从模块<code>check_migrations()</code>的源码（<a href="https://github.com/django/django/blob/2.2.12/django/core/management/base.py#L453">base.py#L453</a>）中可以看出，在运行<code>runserver</code>命令时，Django只会到默认数据库的<code>django_migrations</code>表中查找迁移进度，<code>db_1</code>和<code>db_2</code>的迁移进度就被无视了……那么，怎样才能让在运行<code>runserver</code>时检查非默认数据库的迁移进度呢？</p><h2 id="自定义check-migrations"><a href="#自定义check-migrations" class="headerlink" title="自定义check_migrations"></a>自定义check_migrations</h2><p>简单来说，就是修改<code>runserver</code>里<code>check_migrations()</code>的行为。先来看看原始的<code>check_migrations()</code>代码：</p><pre><code class="python">def check_migrations(self):    from django.db.migrations.executor import MigrationExecutor    try:        executor = MigrationExecutor(connections[DEFAULT_DB_ALIAS])    except ImproperlyConfigured:        # No databases are configured (or the dummy one)        return    plan = executor.migration_plan(executor.loader.graph.leaf_nodes())    if plan:        # 提示未migrate的app，省略        self.stdout.write(self.style.NOTICE(&quot;Run &#39;python manage.py migrate&#39; to apply them.\n&quot;))</code></pre><p>总体逻辑并不复杂，关键在于<code>executor</code>对象的<code>migration_plan()</code>方法。我要做的就是引入<code>db_1</code>和<code>db_2</code>的<code>executor</code>，让每个数据库只检查自己的迁移进度。下面是我自己实现的<code>check_migrations()</code>方法：</p><pre><code class="python"># multi_db/my_runserver.pyfrom collections import defaultdictfrom django.conf import settingsfrom django.core.management.commands import runserverfrom django.db import connections, DEFAULT_DB_ALIASfrom django.db.migrations.executor import MigrationExecutorclass MyCommand(runserver.Command):    def check_migrations(self):        # 为每个数据库连接创建一个executor并放到字典里        executors = &#123;&#125;        for alias in connections:            executors[alias] = MigrationExecutor(connections[alias])        default_executor = executors[DEFAULT_DB_ALIAS]        # 获取迁移任务列表（nodes）        # nodes是一个tuple的list，每个tuple的第一个元素是app的名称        nodes = default_executor.loader.graph.leaf_nodes()        # 按app名称将node分类        node_map = defaultdict(list)        for node in nodes:  # type: tuple            alias = settings.DB_ROUTING.get(node[0], DEFAULT_DB_ALIAS)            node_map[alias].append(node)        # 让每个executor检查各自的迁移任务是否完成        plan = []        for alias, executor in executors.items():            plan.extend(executor.migration_plan(node_map[alias]))        if plan:            # 提示未migrate的app，同原函数，省略            self.stdout.write(self.style.NOTICE(&quot;Run &#39;python manage.py migrate&#39; to apply them.\n&quot;))</code></pre><p>下一步就是让<code>manager.py</code>使用我这个<code>check_migrations()</code>而不是Django内置的<code>check_migrations()</code>。在<code>manager.py</code>的文件头修改一下：</p><pre><code class="python">#!/usr/bin/env python&quot;&quot;&quot;Django&#39;s command-line utility for administrative tasks.&quot;&quot;&quot;import osimport sysfrom multi_db.my_runserver import MyCommandfrom django.core.management.commands import runserverrunserver.Command = MyCommand# ... 略</code></pre><h2 id="大功告成-1"><a href="#大功告成-1" class="headerlink" title="大功告成"></a>大功告成</h2><p>运行看看效果：</p><pre><code class="shell">$ python manage.py runserverWatching for file changes with StatReloaderPerforming system checks...System check identified no issues (0 silenced).April 23, 2020 - xx:xx:xx</code></pre><p>终于不提示还有<code>unapplied migration</code>了……我哭了，你呢？</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>Python</tag>
      
      <tag>Django</tag>
      
      <tag>多数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Django视图函数性能分析（续）</title>
    <link href="/2020/04/19/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E7%BB%AD%EF%BC%89/"/>
    <url>/2020/04/19/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E7%BB%AD%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2020/04/19/django.png" alt="django.png"></p><p>在<a href="/2019/03/11/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90/">以前的文章</a>中，我介绍了两种分析Django视图函数性能的工具：<code>Django Debug Toolbar</code>和<code>cProfile中间件</code>。在实际开发过程中，前者的SQL执行分析是相当实用的功能。</p><p>但美中不足的是，<code>Django Debug Toolbar</code>只能在浏览器里分析响应类型为HTML的视图函数，这就导致响应类型为JSON的视图函数必须要借助<code>Django Rest Framework</code>框架中的<code>HTMLRenderer</code>等手段修改自身的响应类型，否则无法使用该工具。</p><p>那有没有更直接的手段，来为JSON视图函数分析SQL执行情况呢？这就是文本要探究的问题。</p><blockquote><p>注：本文所使用的运行环境为<code>Python 3.6</code>+<code>Django 2.2</code></p></blockquote><h1 id="Django中的SQL执行"><a href="#Django中的SQL执行" class="headerlink" title="Django中的SQL执行"></a>Django中的SQL执行</h1><p>在查阅了相关文档后，我发现事情比我想象中的简单。在Django官方文档中的<a href="https://docs.djangoproject.com/en/2.2/faq/models/">FAQ: Databases and models</a>有这样的描述：</p><p><img src="/images/2020/04/19/document.webp" alt="document.webp"></p><p>如此一来方法就很清晰了：写一个中间件，将<code>connection.queries</code>里的内容插入到JSON响应中。中间件代码如下：</p><pre><code class="python"># coding:utf-8import jsonfrom django.conf import settingsfrom django.core.exceptions import MiddlewareNotUsedfrom django.db import connectionsfrom django.http.response import HttpResponseclass ProfileMiddleware(object):    ignore_sql = []    def __init__(self, get_response):        if not settings.DEBUG:            raise MiddlewareNotUsed  # 只在DEBUG模式下启用该中间件        self.get_response = get_response    def __call__(self, request):        response = self.get_response(request)  # type: HttpResponse        # 根据url参数和返回类型判断是否应该插入SQL执行信息        if request.GET.get(&quot;profile&quot;) == &quot;db&quot; and &quot;application/json&quot; in repr(response):            try:                data = json.loads(response.content)            except (TypeError, ValueError):                return response  # 解析json失败            # 插入sql执行信息            sql_queries = []            for alias in connections:                for query in connections[alias].queries:                    if query[&quot;sql&quot;] not in self.ignore_sql:                        query[&quot;using&quot;] = alias                        sql_queries.append(query)            if isinstance(data, dict):                data[&quot;_debug_sql&quot;] = sql_queries            elif isinstance(data, list):                data.append(&#123;&quot;_debug_sql&quot;: sql_queries&#125;)            response.content = json.dumps(data)        return response</code></pre><p>写个简单视图函数来验证一下：</p><pre><code class="python">#!/usr/bin/env python3from django.contrib.auth.models import Userfrom django.http import JsonResponsedef user_count(request):    data = &#123;&quot;count&quot;: User.objects.count()&#125;    return JsonResponse(data)</code></pre><pre><code class="shell">$ curl -s localhost:8000/user_count/\?profile=db | python3 -m json.tool&#123;    &quot;count&quot;: 0,    &quot;_debug_sql&quot;: [        &#123;            &quot;sql&quot;: &quot;SELECT COUNT(*) AS \&quot;__count\&quot; FROM \&quot;auth_user\&quot;&quot;,            &quot;time&quot;: &quot;0.000&quot;,            &quot;using&quot;: &quot;default&quot;        &#125;    ]&#125;</code></pre><h1 id="和cProfile中间件组合"><a href="#和cProfile中间件组合" class="headerlink" title="和cProfile中间件组合"></a>和cProfile中间件组合</h1><p>在<a href="/2019/03/11/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90/">以前的文章</a>中我也介绍了使用<code>cProfile中间件</code>来分析Django视图函数的Python代码执行情况，这里就顺便组合起来。</p><pre><code class="python"># coding:utf-8import cProfileimport jsonimport pstatsfrom io import StringIOfrom django.conf import settingsfrom django.core.exceptions import MiddlewareNotUsedfrom django.db import connectionsfrom django.http.response import HttpResponseclass ProfileMiddleware(object):    ignore_sql = []    def __init__(self, get_response):        if not settings.DEBUG:            raise MiddlewareNotUsed  # 只在DEBUG模式下启用该中间件        self.get_response = get_response    def __call__(self, request):        if request.GET.get(&quot;profile&quot;) == &quot;cprofile&quot;:            # 用cProfile的结果覆盖原始响应            profile = cProfile.Profile()            profile.enable()            self.get_response(request)            profile.disable()            ram_file = StringIO()            sort_by = &#39;tottime&#39;            stats = pstats.Stats(profile, stream=ram_file)            stats.strip_dirs().sort_stats(sort_by).print_stats()            return HttpResponse(ram_file.getvalue().encode(&#39;utf-8&#39;), &#39;text/plain&#39;)        response = self.get_response(request)  # type: HttpResponse        # 根据url参数和返回类型判断是否应该插入SQL执行信息        if request.GET.get(&quot;profile&quot;) == &quot;db&quot; and &quot;application/json&quot; in repr(response):            try:                data = json.loads(response.content)            except (TypeError, ValueError):                return response  # 解析json失败            # 插入sql执行信息            sql_queries = []            for alias in connections:                for query in connections[alias].queries:                    if query[&quot;sql&quot;] not in self.ignore_sql:                        query[&quot;using&quot;] = alias                        sql_queries.append(query)            data[&quot;_debug_sql&quot;] = sql_queries            response.content = json.dumps(data)        return response</code></pre><p>同样验证一下：</p><pre><code class="shell">$ curl -s localhost:8000/user_count/\?profile=cprofile         830 function calls (826 primitive calls) in 0.002 seconds   Ordered by: internal time   ncalls  tottime  percall  cumtime  percall filename:lineno(function)        1    0.000    0.000    0.000    0.000 base.py:379(execute)        1    0.000    0.000    0.000    0.000 &#123;built-in method _sqlite3.connect&#125;        4    0.000    0.000    0.000    0.000 base.py:53(list_aggregate)       20    0.000    0.000    0.000    0.000 functools.py:44(update_wrapper)      158    0.000    0.000    0.000    0.000 &#123;built-in method builtins.getattr&#125;# 省略</code></pre><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>不得不感叹Django真是一个功能完善、生态丰富的Web框架，这篇文章只是站在巨人的肩膀上取得了一些微不足道的成就。<code>Django Debug Toolbar</code>虽然也是通过<code>connection.queries</code>的方式获取了SQL执行情况（<a href="https://github.com/jazzband/django-debug-toolbar/blob/2.2/debug_toolbar/panels/sql/panel.py#L58">panel.py#L58</a>），但其所做的工作远不止如此，值得细看。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>Python</tag>
      
      <tag>性能分析</tag>
      
      <tag>Django</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Golang极简实现WebSocket承载socks5流量</title>
    <link href="/2020/02/20/Golang%E6%9E%81%E7%AE%80%E5%AE%9E%E7%8E%B0WebSocket%E6%89%BF%E8%BD%BDsocks5%E6%B5%81%E9%87%8F/"/>
    <url>/2020/02/20/Golang%E6%9E%81%E7%AE%80%E5%AE%9E%E7%8E%B0WebSocket%E6%89%BF%E8%BD%BDsocks5%E6%B5%81%E9%87%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h1><p>假设有一台远程服务器：<code>1.2.3.4</code>，上面运行着一个socks5代理：<code>127.0.0.1:1080</code>。登上这台远程服务器后，可以通过curl验证这个代理：</p><pre><code class="shell">$ curl --socks5 127.0.0.1:1080 https://httpbin.org/get&#123;  ...  &quot;origin&quot;: &quot;1.2.3.4&quot;,  ...&#125;</code></pre><p>本文的目标是通过WebSocket的方式让本地curl也可以使用这个socks5代理。</p><h1 id="服务器运行代码"><a href="#服务器运行代码" class="headerlink" title="服务器运行代码"></a>服务器运行代码</h1><p>这段代码运行于远程服务器，代码运行时会在<code>1.2.3.4:5000</code>上开启一个WebSocket服务。每接收到一个WebSocket请求，代码就会连接至本地的socks5代理，使用<code>io.Copy</code>进行数据转发工作。</p><pre><code class="go">package mainimport (    &quot;golang.org/x/net/websocket&quot;    &quot;io&quot;    &quot;log&quot;    &quot;net&quot;    &quot;net/http&quot;    &quot;sync&quot;)func ws2socks(ws *websocket.Conn) &#123;    defer ws.Close()    socks, err := net.Dial(&quot;tcp&quot;, &quot;127.0.0.1:1080&quot;)    if err != nil &#123;        log.Println(&quot;dial socks error:&quot;, err)        return    &#125;    defer socks.Close()    var wg sync.WaitGroup    ioCopy := func(dst io.Writer, src io.Reader) &#123;        defer wg.Done()        io.Copy(dst, src)    &#125;    wg.Add(2)    go ioCopy(socks, ws)    go ioCopy(ws, socks)    wg.Wait()&#125;func main() &#123;    http.Handle(&quot;/&quot;, websocket.Handler(ws2socks))    log.Fatal(http.ListenAndServe(&quot;1.2.3.4:5000&quot;, nil))&#125;</code></pre><h1 id="本地运行代码"><a href="#本地运行代码" class="headerlink" title="本地运行代码"></a>本地运行代码</h1><p>这段代码运行于本地，代码运行时会监听<code>127.0.0.1:8888</code>。每接收到一个TCP连接，代码就会连接至远程服务器的WebSocket服务，同样使用<code>io.Copy</code>进行数据转发工作。</p><pre><code class="go">package mainimport (    &quot;golang.org/x/net/websocket&quot;    &quot;io&quot;    &quot;log&quot;    &quot;net&quot;    &quot;sync&quot;)func socks2ws(socks *net.TCPConn) &#123;    defer socks.Close()    ws, err := websocket.Dial(&quot;ws://1.2.3.4:5000/&quot;, &quot;&quot;, &quot;ws://1.2.3.4:5000/&quot;)    if err != nil &#123;        log.Println(&quot;dial websocket error:&quot;, err)        return    &#125;    defer ws.Close()    var wg sync.WaitGroup    ioCopy := func(dst io.Writer, src io.Reader) &#123;        defer wg.Done()        io.Copy(dst, src)    &#125;    wg.Add(2)    go ioCopy(ws, socks)    go ioCopy(socks, ws)    wg.Wait()&#125;func main() &#123;    listener, err := net.Listen(&quot;tcp&quot;, &quot;127.0.0.1:8888&quot;)    if err != nil &#123;        log.Fatal(err)    &#125;    log.Println(&quot;listen tcp at:&quot;, &quot;127.0.0.1:8888&quot;)    for &#123;        conn, _ := listener.Accept()        go socks2ws(conn.(*net.TCPConn))    &#125;&#125;</code></pre><h1 id="实际运行"><a href="#实际运行" class="headerlink" title="实际运行"></a>实际运行</h1><p>在远程服务器、本地分别运行对应的代码后，就可以在本地用curl测试这个”本地socks5代理”了。</p><pre><code class="shell">$ curl --socks5 127.0.0.1:8888 https://httpbin.org/get&#123;  ...  &quot;origin&quot;: &quot;1.2.3.4&quot;,  ...&#125;</code></pre><h1 id="TLS加密"><a href="#TLS加密" class="headerlink" title="TLS加密"></a>TLS加密</h1><p>如果不使用TLS加密传输，WebSocket中的数据将以明文的方式暴露在互联网上。为了安全性考虑，使用TLS加密传输是很有必要的。</p><h2 id="服务器端配置"><a href="#服务器端配置" class="headerlink" title="服务器端配置"></a>服务器端配置</h2><p>关于域名和证书的文章有很多，这里就不赘述了。假设有域名<code>test.com</code>的解析指向了这台服务器<code>1.2.3.4</code>，已经生成自签名证书文件于<code>/etc/ssl/1.cert</code>和<code>/etc/ssl/2.key</code>。配置了Nginx之后（如下所示），WebSocket服务的地址就会从<code>ws://1.2.3.4:5000/</code>变为<code>wss://test.com/ws</code></p><pre><code class="conf">server &#123;    listen 443 ssl;    server_name test.com;    ssl_certificate     /etc/ssl/1.cert;    ssl_certificate_key /etc/ssl/2.key;    ssl_protocols       TLSv1 TLSv1.1 TLSv1.2;    ssl_ciphers         HIGH:!aNULL:!MD5;    root /var/www/html;    index index.html index.htm index.nginx-debian.html;    location / &#123;        try_files $uri $uri/ =404;    &#125;    location /ws &#123;        proxy_pass http://1.2.3.4:5000;        proxy_http_version 1.1;        proxy_set_header Upgrade $http_upgrade;        proxy_set_header Connection &quot;Upgrade&quot;;    &#125;&#125;</code></pre><h2 id="本地代码变动"><a href="#本地代码变动" class="headerlink" title="本地代码变动"></a>本地代码变动</h2><p>如果不是自签名证书，那么本地代码只需要更改WebSocket连接地址。但如果是自签名证书，本地代码最好主动接受认证文件（即上文提到的<code>1.cert</code>），因为跳过证书认证会带来额外的风险。于是，本地代码在向WebSocket服务请求之前需要先读取认证文件，如下所示：</p><pre><code class="go">package main...var wsConfig *websocket.Configfunc initConfig() &#123;    // load certificate file    content, err := ioutil.ReadFile(&quot;1.cert&quot;)    if err != nil &#123;        log.Fatal(err)    &#125;    roots := x509.NewCertPool()    if ok := roots.AppendCertsFromPEM(content); ok != true &#123;        log.Fatal(&quot;cert parse fail&quot;)    &#125;    // generate websocket config    wsConfig, err = websocket.NewConfig(&quot;wss://test.com/ws&quot;, &quot;wss://test.com/ws&quot;)    if err != nil &#123;        log.Fatal(err)    &#125;    wsConfig.TlsConfig = &amp;tls.Config&#123;RootCAs: roots&#125;&#125;func socks2ws(socks *net.TCPConn) &#123;    defer socks.Close()    ws, err := websocket.DialConfig(wsConfig)    ...&#125;func main() &#123;    initConfig()    ...&#125;</code></pre><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>思路很简单，实现也简单……就当是Golang的入门练习。总结一下就是，Golang真香。</p>]]></content>
    
    
    
    <tags>
      
      <tag>nginx</tag>
      
      <tag>websocket</tag>
      
      <tag>socks5</tag>
      
      <tag>Golang</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>APT、Docker、PyPI使用国内镜像加速</title>
    <link href="/2019/12/17/APT%E3%80%81Docker%E3%80%81PyPI%E4%BD%BF%E7%94%A8%E5%9B%BD%E5%86%85%E9%95%9C%E5%83%8F%E5%8A%A0%E9%80%9F/"/>
    <url>/2019/12/17/APT%E3%80%81Docker%E3%80%81PyPI%E4%BD%BF%E7%94%A8%E5%9B%BD%E5%86%85%E9%95%9C%E5%83%8F%E5%8A%A0%E9%80%9F/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2019-12-17.03.webp"></p><p>因为一些不可描述的原因，在中国绝大多数地区进行更新APT软件包列表（<code>apt-get update</code>）&amp;安装APT软件包（<code>apt-get isntall</code>）、拉取Docker镜像（<code>docker pull</code>）、安装PyPI包（<code>pip install</code>）等操作都是一件非常痛苦的事：网络连接速度太慢了。万幸的是我们有许多开源软件镜像站点，合理设置镜像站点能够极大提升工作和学习效率。这篇文章介绍的就是在上述三种场景下如何设置镜像站点。</p><h1 id="APT镜像加速"><a href="#APT镜像加速" class="headerlink" title="APT镜像加速"></a>APT镜像加速</h1><p><a href="https://mirror.tuna.tsinghua.edu.cn/help">TUNA镜像站（清华大学开源软件镜像站）</a>上介绍了绝大部分Linux发行版的镜像加速设置方法，这里只介绍Debian系发行版的APT镜像加速如何设置。<br>APT更新软件包列表的第一步是从本地源列表（<code>/etc/apt/sources.list</code>和<code>/etc/apt/sources.list.d/*.list</code>）中读取所有软件来源站点，所以我们只要更新本地源列表就可以实现镜像加速了（下面的源列表不包括源代码站点，如需完整站点可自行取消注释对应行）。</p><pre><code class="shell"># 备份已有源列表sudo cp /etc/apt/sources.list /etc/apt/sources.list.olddistrib=$(lsb_release -is | awk &#39;&#123;print tolower($1)&#125;&#39;)codename=$(lsb_release -cs)apt_src=&quot;https://mirrors.aliyun.com&quot;# ↑阿里云源 ↓TUNA源# apt_src=&quot;https://mirrors.tuna.tsinghua.edu.cn&quot;cat &gt;sources.list&lt;&lt;EOFdeb $apt_src/$distrib/ $codename main restricted universe multiversedeb $apt_src/$distrib/ $codename-security main restricted universe multiversedeb $apt_src/$distrib/ $codename-updates main restricted universe multiversedeb $apt_src/$distrib/ $codename-backports main restricted universe multiverse#deb-src $apt_src/$distrib/ $codename main restricted universe multiverse#deb-src $apt_src/$distrib/ $codename-security main restricted universe multiverse#deb-src $apt_src/$distrib/ $codename-updates main restricted universe multiverse#deb-src $apt_src/$distrib/ $codename-backports main restricted universe multiverseEOFsudo mv sources.list /etc/apt# 更新软件包列表sudo apt-get update</code></pre><h2 id="手动添加Ubuntu-PPA源"><a href="#手动添加Ubuntu-PPA源" class="headerlink" title="手动添加Ubuntu PPA源"></a>手动添加Ubuntu PPA源</h2><p>PPA（Personal Package Archives）作为APT源的补充能让Ubuntu用户在第一时间体验到最新发布的软件。但在笔者的实际体验中，默认的PPA源添加命令（<code>add-apt-repository</code>）往往需要极长的时间才能添加完成。经过一段时间摸索，笔者发现可以通过简单的方式手动添加Ubuntu PPA源，在此分享给读者。<br><img src="/images/2019-12-17.01.webp"></p><p>以上图（<a href="https://launchpad.net/~starws-box/+archive/ubuntu/deadbeef-player">deadbeef-player : starws</a>）为例，点开图中的”Technical details about this PPA”，在下拉列表中选择系统版本后，将下方的PPA源链接保存至本地源列表。</p><pre><code class="shell">echo &quot;deb http://ppa.launchpad.net/starws-box/deadbeef-player/ubuntu bionic main&quot; &gt; ppa-deadbeef.listsudo mv ppa-deadbeef.list /etc/apt/sources.list.d/</code></pre><p>然后再点击上图中<code>Signing key</code>下方的链接，在出现的新页面（如下图所示）中复制<code>sig</code>公钥文件的地址。最后用<code>apt-key</code>添加这个公钥，这个PPA源就添加完成了。</p><p><img src="/images/2019-12-17.02.webp"></p><pre><code class="shell">$ wget -qO - https://keyserver.ubuntu.com/pks/lookup?op=get&amp;search=0x446c82dc2dd154a1 | sudo apt-key add -OK$ sudo apt-get update &amp;&amp; sudo apt-get install deadbeef</code></pre><h1 id="安装Docker（Ubuntu-16-04-）"><a href="#安装Docker（Ubuntu-16-04-）" class="headerlink" title="安装Docker（Ubuntu 16.04+）"></a>安装Docker（Ubuntu 16.04+）</h1><p>Debian系统理论上可以兼容这些命令，不过笔者没有进行过测试。其它系统参见<a href="https://yeasy.gitbooks.io/docker_practice/install/">《Docker——从入门到实践》 安装Docker</a>。</p><pre><code class="shell"># Ubuntu 16.04需要先安装这个依赖包，Ubuntu 18.04不用# wget http://launchpadlibrarian.net/344879847/libseccomp2_2.3.1-2.1ubuntu2~16.04.1_amd64.deb# sudo dpkg -i libseccomp2_2.3.1-2.1ubuntu2~16.04.1_amd64.deb# rm libseccomp2_2.3.1-2.1ubuntu2~16.04.1_amd64.deb# 卸载旧版本Dockersudo apt-get remove docker docker-engine docker.io# 添加Docker-ce源并安装Docker-ceecho &quot;deb [arch=amd64] https://mirrors.aliyun.com/docker-ce/linux/$(lsb_release -is | awk &#39;&#123;print tolower($1)&#125;&#39;) $(lsb_release -cs) stable&quot; &gt; docker-ce.listsudo mv docker-ce.list /etc/apt/sources.list.d/curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | sudo apt-key add -sudo apt-get update &amp;&amp; sudo apt-get install docker-ce# 启动Docker服务并配置用户组sudo systemctl enable dockersudo systemctl start dockersudo groupadd dockersudo usermod -aG docker $USER# 退出当前终端并重新登录，测试Docker是否正常运行docker version</code></pre><h1 id="Docker镜像加速"><a href="#Docker镜像加速" class="headerlink" title="Docker镜像加速"></a>Docker镜像加速</h1><p>Docker在安装完成后默认从DockerHub源站拉取镜像，而将拉取目标站点从源站改为镜像站往往能获得更快的镜像拉取速度。详细流程参见<a href="https://yeasy.gitbooks.io/docker_practice/install/mirror.html">《Docker——从入门到实践》 镜像加速器</a>。</p><pre><code class="shell">cat &gt;daemon.json&lt;&lt;EOF&#123;  &quot;registry-mirrors&quot;: [    &quot;https://dockerhub.azk8s.cn&quot;,    &quot;https://hub-mirror.c.163.com&quot;  ]&#125;EOFsudo mv daemon.json /etc/docker/# 重启服务sudo systemctl daemon-reloadsudo systemctl restart docker</code></pre><p>此时可以通过<code>docker info</code>命令查看镜像加速设置是否生效。</p><pre><code class="shell">$ docker info -f &#39;&#123;&#123;json .RegistryConfig.Mirrors&#125;&#125;&#39;[&quot;https://dockerhub.azk8s.cn/&quot;,&quot;https://hub-mirror.c.163.com/&quot;]</code></pre><h1 id="PyPI镜像加速"><a href="#PyPI镜像加速" class="headerlink" title="PyPI镜像加速"></a>PyPI镜像加速</h1><p>详细说明参见<a href="https://mirror.tuna.tsinghua.edu.cn/help/pypi/">TUNA pypi 镜像使用帮助</a>。</p><pre><code class="shell">mirror=&quot;https://mirrors.aliyun.com/pypi/simple/&quot;# ↑阿里云源 ↓TUNA源# mirror=&quot;https://pypi.tuna.tsinghua.edu.cn/simple&quot;# 临时安装pip install -i $mirror requests# 设为默认，需要升级pip到最新版本（&gt;=10.0.0）pip install -i $mirror pip -Upip config set global.index-url $mirror</code></pre>]]></content>
    
    
    
    <tags>
      
      <tag>linux</tag>
      
      <tag>网络优化</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Docker快速部署ownCloud并配置HTTPS</title>
    <link href="/2019/12/16/Docker%E5%BF%AB%E9%80%9F%E9%83%A8%E7%BD%B2ownCloud%E5%B9%B6%E9%85%8D%E7%BD%AEHTTPS/"/>
    <url>/2019/12/16/Docker%E5%BF%AB%E9%80%9F%E9%83%A8%E7%BD%B2ownCloud%E5%B9%B6%E9%85%8D%E7%BD%AEHTTPS/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2019-12-16.01.webp"><br>ownCloud一直是市场上最受欢迎的个人云存储解决方案之一，有着使用广泛、客户端兼容性好、自由开源等优点。但ownCloud的部署涉及LAMP环境，部署过程较为繁琐。这篇文章介绍如何用docker-compose快速部署ownCloud，并配置Nginx转发+HTTPS提高服务器安全性。</p><h1 id="配置docker-compose"><a href="#配置docker-compose" class="headerlink" title="配置docker-compose"></a>配置docker-compose</h1><blockquote><p>以下部分内容来自<a href="https://doc.owncloud.com/server/latest/admin_manual/installation/docker/">ownCloud官方安装文档</a></p></blockquote><p>首先，下载官方docker-compose文件，并创环境变量文件用以指定ownCloud版本、账户密码和HTTP端口。</p><pre><code class="shell">$ mkdir owncloud-docker-server$ cd owncloud-docker-server$ wget https://raw.githubusercontent.com/owncloud/docs/master/modules/admin_manual/examples/installation/docker/docker-compose.yml$ cat &lt;&lt; EOF &gt; .envOWNCLOUD_VERSION=10.3OWNCLOUD_DOMAIN=localhostADMIN_USERNAME=adminADMIN_PASSWORD=adminHTTP_PORT=8080EOF</code></pre><p>从下载下来的<code>docker-compose.yml</code>文件里可以看出，ownCloud共有三个docker服务：<code>owncloud</code>、<code>db</code>、<code>redis</code>，这三个docker服务都将创建独立的docker数据卷，将各自的数据文件持久化到数据卷里。</p><pre><code class="yaml">version: &#39;2.1&#39;volumes:  files:    driver: local...services:  owncloud:...    volumes:      - files:/mnt/data...</code></pre><p>我个人更习惯将ownCloud的数据保存目录指定为某个具体文件夹，方便我进行管理。所以我对<code>docker-compose.yml</code>做如下改动：</p><pre><code class="yaml">version: &#39;2.1&#39;volumes:#  files:#    driver: local...services:  owncloud:...    volumes:#      - files:/mnt/data      - /path/to/my/data/owncloud:/mnt/data...</code></pre><h1 id="用docker-compose启动ownCloud"><a href="#用docker-compose启动ownCloud" class="headerlink" title="用docker-compose启动ownCloud"></a>用docker-compose启动ownCloud</h1><p>启动ownCloud的命令很简单，只需让docker-compose按照配置好的<code>docker-compose.yml</code>启动容器。第一次运行需要拉取镜像，只需耐心等待即可。</p><pre><code class="shell">$ docker-compose up -d</code></pre><p>命令执行完成后可以通过docker-compose命令查看拉取的镜像和服务运行状态：</p><pre><code class="shell">$ docker-compose images     Container           Repository        Tag       Image Id      Size-------------------------------------------------------------------------owncloud_db_1         webhippie/mariadb   latest   3f6237885724   626 MBowncloud_owncloud_1   owncloud/server     10.3     b7c82576c651   785 MBowncloud_redis_1      webhippie/redis     latest   42ab00c664c2   56.4 MB$ docker-compose ps       Name                      Command                  State               Ports--------------------------------------------------------------------------------------------owncloud_db_1         /usr/bin/entrypoint /bin/s ...   Up (healthy)   3306/tcpowncloud_owncloud_1   /usr/bin/entrypoint /usr/b ...   Up (healthy)   0.0.0.0:8080-&gt;8080/tcpowncloud_redis_1      /usr/bin/entrypoint /bin/s ...   Up (healthy)   6379/tcp</code></pre><p>此时也可以用curl来验证服务是否启动：</p><pre><code class="shell">$ curl 127.0.0.1:8080 -IHTTP/1.1 302 FoundDate: Mon, 16 Dec 2019 03:14:55 GMTServer: Apache...</code></pre><h1 id="配置Nginx转发-HTTPS"><a href="#配置Nginx转发-HTTPS" class="headerlink" title="配置Nginx转发+HTTPS"></a>配置Nginx转发+HTTPS</h1><p>Nginx+HTTPS的配置不算麻烦，只要在Nginx的配置文件（比如<code>/etc/nginx/sites-available/default</code>）中添加一个server项即可。在这里的配置中，我让Nginx在8000端口上启动HTTPS服务，并将所有请求以HTTP方式转发至8080端口上的ownCloud。</p><pre><code class="nginx">server &#123;    listen 8000 ssl;    server_name owncloud;    ssl_certificate     /etc/nginx/ssl/owncloud.crt;    ssl_certificate_key /etc/nginx/ssl/owncloud.key;    ssl_protocols       TLSv1 TLSv1.1 TLSv1.2;    ssl_ciphers         HIGH:!aNULL:!MD5;    client_max_body_size 0;    location / &#123;        proxy_set_header   Host             $host:8000;        proxy_redirect     http://          https://;        proxy_pass         http://127.0.0.1:8080/;    &#125;&#125;</code></pre><p>配置时有一些需要注意的地方。</p><ul><li><code>client_max_body_size</code>项，其默认的值为<code>1m</code>，也就是说Nginx最大只能接受1M大小的HTTP请求，这对于ownCloud的文件上传功能来说是不可接受的。在这里我将其设为<code>0</code>，让Nginx不限制HTTP请求的大小。理论上来说这个参数应该限定在文件上传的<code>url</code>以提高安全性，但这里就图个方便。</li><li><code>location</code>中的<code>proxy_set_header</code>项，将请求转发头的Host设置为Nginx的Host（$host:8000）而不是ownCloud的Host（$host:8080），避免ownCloud在响应重定向时返回8080端口地址。</li><li><code>location</code>中的<code>proxy_redirect</code>项，将所有重定向响应中的HTTP地址转换为HTTPS地址。</li></ul><p>当然，在配置了以上转发规则后，ownCloud的响应还是存在一些问题：网页上的注销链接地址没有加上HTTPS前缀，导致点击后出现400错误，需要手动将地址前缀改成<code>https://</code>才能顺利注销。这个就属于ownCloud本身的BUG了。</p><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>个人云存储的优势在公共云存储商家不断倒闭之后逐渐显现出来了。如果有使用个人云存储服务的需求，ownCloud无疑是一个值得尝试的选择。不管是局域网内部使用、公网IP宽带+DDNS、内网穿透，还是VPS（包括低价大带宽的NAT VPS），HAVE FUN and ENJOY。</p>]]></content>
    
    
    
    <tags>
      
      <tag>docker</tag>
      
      <tag>owncloud</tag>
      
      <tag>nginx</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>自用linux server性能测试</title>
    <link href="/2019/11/18/%E8%87%AA%E7%94%A8linux-server%E6%80%A7%E8%83%BD%E6%B5%8B%E8%AF%95/"/>
    <url>/2019/11/18/%E8%87%AA%E7%94%A8linux-server%E6%80%A7%E8%83%BD%E6%B5%8B%E8%AF%95/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>用工具测试下手头用着的linux server，三个vps平台、一台物理机，测试结果看看就好，不用太较真。</p><h1 id="测试结果汇总"><a href="#测试结果汇总" class="headerlink" title="测试结果汇总"></a>测试结果汇总</h1><table><thead><tr><th>服务商</th><th>Google Cloud</th><th>Google Cloud</th><th>阿里云</th><th>某NAT VPS商家</th><th>个人PC</th></tr></thead><tbody><tr><td>机器型号</td><td>f1-micro 1vCPU 0.6GB 10GB</td><td>g1-small 1vCPU 1.7GB 40GB</td><td>ecs.n4.small 1vCPU 2GiB 40GB</td><td>1vCPU 1GB 20GB</td><td>4CPU 8GB 480GB(nvme)</td></tr><tr><td>参考价格</td><td>$6&#x2F;mo</td><td>$20&#x2F;mo</td><td>￥98&#x2F;mo</td><td>￥26&#x2F;mo</td><td></td></tr><tr><td>系统</td><td>Ubuntu 18.04 LTS</td><td>Ubuntu 18.04 LTS</td><td>CentOS 7</td><td>Ubuntu 18.04 LTS</td><td>Ubuntu 18.04 LTS</td></tr><tr><td>CPU信息</td><td>Xeon(R) CPU @ 2.00GHz</td><td>Xeon(R) CPU @ 2.00GHz</td><td>E5-2682 v4 @ 2.50GHz</td><td>E3-12xx v2 @ 2.50GHz</td><td>i3-8100 CPU @ 3.60GHz</td></tr><tr><td>sysbench(cpu)</td><td>9469 (v1.0.11)</td><td>9477 (v1.0.11)</td><td>7909 (v1.0.17)</td><td>6157 (v1.0.11)</td><td>12799 (v1.0.11)</td></tr><tr><td>4K W&#x2F;R</td><td>6.5 MB&#x2F;s 8.1 MB&#x2F;s</td><td>6.6 MB&#x2F;s 7.8 MB&#x2F;s</td><td>8.3 MB&#x2F;s 8.7 MB&#x2F;s</td><td>1.2 MB&#x2F;s 1.2 MB&#x2F;s</td><td>245 MB&#x2F;s 80 MB&#x2F;s</td></tr><tr><td>1M W&#x2F;R</td><td>37.7 MB&#x2F;s 128 MB&#x2F;s</td><td>37.7 MB&#x2F;s 128 MB&#x2F;s</td><td>109 MB&#x2F;s 111 MB&#x2F;s</td><td>101 MB&#x2F;s 103MB&#x2F;s</td><td>1 GB&#x2F;s 1 GB&#x2F;s</td></tr><tr><td>RAM 4K</td><td>975 MB&#x2F;s 1.5 GB&#x2F;s</td><td>1.2 GB&#x2F;s 2.3 GB&#x2F;s</td><td>1.2 GB&#x2F;s 2.1 GB&#x2F;s</td><td>89.1 MB&#x2F;s 94.2 MB&#x2F;s</td><td>2.2 GB&#x2F;s 2.9 GB&#x2F;s</td></tr><tr><td>MariaDB ENCODE</td><td></td><td>1.457s (19.03.5)</td><td>1.772s (18.06.1)</td><td>2.135s (19.03.5)</td><td>1.126s (19.03.4)</td></tr><tr><td>公网带宽</td><td>&gt;100Mb</td><td>&gt;100Mb</td><td>1Mb</td><td>共享50Mb</td><td>家宽</td></tr></tbody></table><h1 id="测试项说明"><a href="#测试项说明" class="headerlink" title="测试项说明"></a>测试项说明</h1><h2 id="sysbench-cpu"><a href="#sysbench-cpu" class="headerlink" title="sysbench(cpu)"></a>sysbench(cpu)</h2><p>直接运行如下shell命令，并取运行输出中的<code>Threads fairness avg events</code>作为测试结果（表中括号内为sysbench软件版本），数值越大越好：</p><pre><code class="shell">sysbench --test=cpu --cpu-max-prime=10000 run</code></pre><h2 id="4K-W-x2F-R"><a href="#4K-W-x2F-R" class="headerlink" title="4K W&#x2F;R"></a>4K W&#x2F;R</h2><p>用如下dd命令以4K为块单位测试硬盘写读速度（测3次取平均值），数值越大越好：</p><pre><code class="shell">dd if=/dev/zero of=tmpfile bs=4096 count=32768 oflag=directdd of=/dev/null if=tmpfile bs=4096 count=32768 iflag=direct</code></pre><h2 id="1M-W-x2F-R"><a href="#1M-W-x2F-R" class="headerlink" title="1M W&#x2F;R"></a>1M W&#x2F;R</h2><p>用如下dd命令以1M为块单位测试硬盘写读速度（测3次取平均值），数值越大越好：</p><pre><code class="shell">dd if=/dev/zero of=tmpfile bs=1048576 count=1024 oflag=directdd of=/dev/null if=tmpfile bs=1048576 count=1024 iflag=direct</code></pre><h2 id="RAM-4K"><a href="#RAM-4K" class="headerlink" title="RAM 4K"></a>RAM 4K</h2><p>挂载出一块内存盘，用dd命令以4K为块单位测试内存写读速度（由于内存盘不支持direct io，第二次之后的测试结果有较大波动，故只取首次运行结果），数值越大越好：</p><pre><code class="shell">mkdir ~/ramsudo mount -t tmpfs -o size=512m ramdisk ~/ramsudo chown $USER:$USER ~/ramcd ~/ramdd if=/dev/zero of=tmpfile bs=4096 count=65536dd of=/dev/null if=tmpfile bs=4096 count=65536</code></pre><h2 id="MariaDB-ENCODING"><a href="#MariaDB-ENCODING" class="headerlink" title="MariaDB ENCODING"></a>MariaDB ENCODING</h2><p>用Docker运行一个MariaDB实例，运行之后使用ENCODING命令测试MariaDB运行速度（表中括号内为Docker版本），数值越小越好：</p><pre><code class="shell">mkdir ~/mariadb_voldocker run -d -v ~/mariadb_vol:/var/lib/mysql -e MYSQL_ROOT_PASSWORD=12345 --name mariadb_test --restart=always mariadb --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_cidocker exec -it mariadb_test mysql -uroot -p12345</code></pre><pre><code class="sql">SELECT BENCHMARK(10000000,ENCODE(&#39;hello&#39;,&#39;goodbye&#39;));</code></pre><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>便宜自有便宜的道理，NAT VPS有着大带宽（虽然是共享的）、低价格的优势，但换来的是老旧的硬件平台（2012年的Ivy Bridge + DDR3）和接近机械硬盘的4K读写速度。<br>阿里云、谷歌云等大型云平台的VPS虽然带宽或流量贵、价格也贵，但硬件平台往往是较新的（阿里云是2016年的Broadwell + DDR4，谷歌云则更新），硬盘性能也更有保障（不过谷歌云实在是贵啊……）。怎么取舍，还是要看个人需求。</p>]]></content>
    
    
    
    <tags>
      
      <tag>性能分析</tag>
      
      <tag>vps</tag>
      
      <tag>linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>修改Kong官方Docker镜像</title>
    <link href="/2019/05/30/%E4%BF%AE%E6%94%B9Kong%E5%AE%98%E6%96%B9Docker%E9%95%9C%E5%83%8F/"/>
    <url>/2019/05/30/%E4%BF%AE%E6%94%B9Kong%E5%AE%98%E6%96%B9Docker%E9%95%9C%E5%83%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2019-05-30.01.png"><br>开源项目<a href="https://konghq.com/">Kong</a>是当下最火的API Gateway项目之一，官方也在<a href="https://hub.docker.com/_/kong">Docker Hub</a>上提供了Kong的镜像。但我在使用官方镜像来部署Kong的过程中发现，我需要对官方镜像做一些改动来适应我的需求：</p><ul><li>在Docker启动时只需要给定镜像、启动命令即可完成部署（包括初次部署），方便迁移k8s；</li><li>方便Kong切换不同的配置文件；</li><li>将Kong所有的日志存放在指定目录，方便挂载存储。</li></ul><p>这篇文章记录的就是我改动的思路和过程。</p><blockquote><p>注：本文所使用的运行环境为<code>Ubuntu 18.04 LTS</code>+<code>Docker 18.09.6</code>，Kong使用的数据库均为<code>PostgreSQL</code>，使用的Kong镜像版本为<code>1.1.2-alpine</code></p></blockquote><h1 id="官方镜像分析"><a href="#官方镜像分析" class="headerlink" title="官方镜像分析"></a>官方镜像分析</h1><p>Kong的官方文档（<a href="https://docs.konghq.com/install/docker/">Docker Installation</a>）给出了一个简单的Kong部署教程，记录如下：</p><pre><code class="shell"># 创建docker网络docker network create kong-net# 创建数据库docker run -d --name kong-database \               --network=kong-net \               -p 5432:5432 \               -e &quot;POSTGRES_USER=kong&quot; \               -e &quot;POSTGRES_DB=kong&quot; \               postgres:9.6# 初始化数据库docker run --rm \     --network=kong-net \     -e &quot;KONG_DATABASE=postgres&quot; \     -e &quot;KONG_PG_HOST=kong-database&quot; \     kong:latest kong migrations bootstrap# 启动Kongdocker run -d --name kong \     --network=kong-net \     -e &quot;KONG_DATABASE=postgres&quot; \     -e &quot;KONG_PG_HOST=kong-database&quot; \     -e &quot;KONG_PROXY_ACCESS_LOG=/dev/stdout&quot; \     -e &quot;KONG_ADMIN_ACCESS_LOG=/dev/stdout&quot; \     -e &quot;KONG_PROXY_ERROR_LOG=/dev/stderr&quot; \     -e &quot;KONG_ADMIN_ERROR_LOG=/dev/stderr&quot; \     -e &quot;KONG_ADMIN_LISTEN=0.0.0.0:8001, 0.0.0.0:8444 ssl&quot; \     -p 8000:8000 \     -p 8443:8443 \     -p 8001:8001 \     -p 8444:8444 \     kong:latest</code></pre><p>从以上教程可以看出，我们可以通过为官方镜像设置环境变量来配置Kong；而且，该镜像还有一个默认的启动命令，可以直接启动Kong主进程。<br>然后，我们再来看看<code>kong:1.1.2-alpine</code>这个官方镜像的<a href="https://github.com/Kong/docker-kong/blob/bb4efafd0e2272be1c27b9aa2de60cd0022a3fad/alpine/Dockerfile">Dockerfile</a>：</p><pre><code class="Dockerfile">FROM alpine:3.6LABEL maintainer=&quot;Kong Core Team &lt;team-core@konghq.com&gt;&quot;ENV KONG_VERSION 1.1.2ENV KONG_SHA256 0d7509fa2ef653b4aba14a1a1fd20339bccb4f8d386429102c42b7af6d8b6bdbRUN adduser -Su 1337 kong \    ... # 省略    &amp;&amp; chmod -R g=u /usr/local/kongCOPY docker-entrypoint.sh /docker-entrypoint.shENTRYPOINT [&quot;/docker-entrypoint.sh&quot;]EXPOSE 8000 8443 8001 8444STOPSIGNAL SIGTERMCMD [&quot;kong&quot;, &quot;docker-start&quot;]</code></pre><p>从Dockerfile可以看出，官方镜像默认的启动命令为<code>ENTRYPOINT</code>+<code>CMD</code>；再结合前文官方教程里初始化数据库的命令，我们可以知道：</p><ul><li>初始化数据库的容器内启动命令为：<code>/docker-entrypoint.sh kong migrations bootstrap</code></li><li>启动Kong主进程的容器内启动命令为：<code>/docker-entrypoint.sh kong docker-start</code></li></ul><p>有了这些信息，我们就可以对官方镜像进行修改了。</p><h1 id="自定义Dockerfile、启动脚本"><a href="#自定义Dockerfile、启动脚本" class="headerlink" title="自定义Dockerfile、启动脚本"></a>自定义Dockerfile、启动脚本</h1><p>考虑到我有多套Kong配置分别用于不同环境（本地调试、测试、实际应用等），而这些配置中有许多重复的内容，所以我选择将这些重复的配置项放入Dockerfile中，不重复的配置项放入自定义启动脚本中。Dockerfile如下：</p><pre><code class="dockerfile">FROM kong:1.1.2-alpineLABEL maintainer=&quot;orange_wolf &lt;orange_wolf@163.com&gt;&quot;COPY . /gatewayWORKDIR /gateway# KONG_PREFIX指定工作目录，Kong的日志默认存储在 KONG_PREFIX/logs/ 目录下ENV KONG_PREFIX=/gateway KONG_ADMIN_LISTEN=0.0.0.0:8001 KONG_DATABASE=postgresEXPOSE 8000 8001# 自定义启动脚本ENTRYPOINT [&quot;./run-kong.sh&quot;]</code></pre><p>自定义启动脚本：</p><pre><code class="shell">#!/usr/bin/env shcase $&#123;1&#125; in&quot;local&quot;)    export KONG_PG_HOST=192.168.0.100    export KONG_PG_USER=xxxxxx    export KONG_PG_PASSWORD=xxxxxx    ... # 省略    ;;&quot;pro&quot;)    export KONG_PG_HOST=xxx.xxx.xxx.xxx    export KONG_PG_USER=xxxxxx    export KONG_PG_PASSWORD=xxxxxx    ... # 省略    ;;... # 省略esac/docker-entrypoint.sh kong migrations bootstrap  # 初始化数据库/docker-entrypoint.sh kong docker-start  # 启动Kong</code></pre><p>于是，我用自定义的启动脚本代替了原有的启动脚本，这样就能让我通过在启动容器时输入不同指令来载入不同Kong配置，也能让Kong在连接到空数据库时自动实现初始化动作（而不是直接退出）。而这种部署方式也更灵活，更容易从Docker迁移到k8s等其它平台。实际部署流程如下：</p><pre><code class="shell">docker build . -t my-kong-imagedocker run -d --name kong -p 8000:8000 -p 8001:8001 my-kong-image local  # 载入本地调试配置docker run -d --name kong -p 8000:8000 -p 8001:8001 my-kong-image test  # 载入测试配置docker run -d --name kong -p 8000:8000 -p 8001:8001 my-kong-image pro  # 载入实际应用配置</code></pre><h1 id="以root命令启动Nginx"><a href="#以root命令启动Nginx" class="headerlink" title="以root命令启动Nginx"></a>以root命令启动Nginx</h1><p>在自定义了Dockerfile和启动脚本后，似乎一切都能满足我的需求了。但不幸的时，以这个镜像启动容器时我遇到了如下错误：</p><pre><code>nginx: [alert] could not open error log file: open() &quot;/gateway/logs/error.log&quot; failed (13: Permission denied)</code></pre><p>很明显，nginx没有足够的权限将日志写入我指定的日志文件夹中。由于在不指定<code>KONG_PREFIX</code>环境变量时，nginx和Kong能正常将日志文件写入默认的<code>/usr/local/kong/logs/</code>目录下，所以我尝试在启动脚本<code>run-kong.sh</code>中通过<code>chmod</code>、<code>chown</code>命令，将<code>/gateway/logs/</code>目录的权限与<code>/usr/local/kong/logs/</code>目录的权限同步，但还是出现了一样的错误提示。<br>上文提到，官方镜像的启动脚本为<code>/docker-entrypoint.sh</code>。我找到了这份和官方Dockerfile放在一起的<a href="https://github.com/Kong/docker-kong/blob/bb4efafd0e2272be1c27b9aa2de60cd0022a3fad/alpine/docker-entrypoint.sh">启动脚本</a>，发现nginx是以<code>kong</code>用户的身份运行的：</p><pre><code class="shell">#!/bin/sh      ...  # 省略      exec su-exec kong /usr/local/openresty/nginx/sbin/nginx \        -p &quot;$PREFIX&quot; \        -c nginx.conf    fi  fifiexec &quot;$@&quot;</code></pre><p>既然是这样，那么我在自己的启动脚本内修改官方的启动脚本，让nginx以root权限运行就可以解决问题了。修改后的自定义启动脚本如下：</p><pre><code class="shell">...  # 省略sed -i &#39;s/su-exec kong/su-exec root/g&#39; /docker-entrypoint.sh  # 修改启动脚本，以root身份启动nginx/docker-entrypoint.sh kong migrations bootstrap  # 初始化数据库/docker-entrypoint.sh kong docker-start  # 启动Kong</code></pre><p>顺便一提，如果想在容器内让Kong的默认端口由<code>8000</code>变为<code>80</code>（<code>KONG_PROXY_LISTEN=0.0.0.0:80</code>）或其它低于<code>1024</code>的端口，同样需要让nginx以root身份运行。</p><h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>开源项目官方提供的Docker镜像，往往要比自己用<code>Alpine</code>、<code>CentOS</code>、<code>Debian</code>等基础镜像一步步安装项目构成的镜像来得方便、稳定、好用；但后者又往往拥有前者无可比拟的自由度。其实，通过分析并“<code>继承</code>”官方Dockerfile、编写自己的启动脚本，往往能够同时拥有前两种镜像的优点——既方便，又自由。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>API GATEWAY</tag>
      
      <tag>Docker</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Django视图函数捕捉异常</title>
    <link href="/2019/05/23/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%8D%95%E6%8D%89%E5%BC%82%E5%B8%B8/"/>
    <url>/2019/05/23/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%8D%95%E6%8D%89%E5%BC%82%E5%B8%B8/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2019-05-23.01.png"><br>不存在没有BUG的代码，也不存在不会出现异常的视图函数。当Django视图函数中的代码出现异常时，访问这个URL的用户就会收到状态码为<code>500 Internal Server Error</code>的HTTP 响应。如何捕捉视图函数里出现的异常，这是本篇文章要讨论的问题。</p><blockquote><p>注：本文所使用的运行环境为<code>uWSGI 2.0.18</code>+<code>Python3.6</code>+<code>Django 2.2</code></p></blockquote><h1 id="在uWSGI的日志中显示Traceback"><a href="#在uWSGI的日志中显示Traceback" class="headerlink" title="在uWSGI的日志中显示Traceback"></a>在uWSGI的日志中显示Traceback</h1><p>这种方法算是一种比较消极的异常捕捉方式——仅将<code>Traceback</code>异常信息记录在uWSGI的日志中，不做进一步处理。日志可以用于日后分析，但用户收到的仍然是<code>500 Internal Server Error</code>响应。而这种捕捉方式也是默认启用的，只要所使用的Django版本高于或等于<code>1.9</code>就行。例子如下：</p><pre><code class="python"># django_test/urls.pyfrom django.urls import pathfrom .views import indexurlpatterns = [    path(&#39;index&#39;, index),]# django_test/views.pyfrom django.http.response import HttpResponsedef index(request):    1 / 0    return HttpResponse(b&#39;hello world&#39;)</code></pre><p>配置好路由和视图函数并用<code>uWSGI</code>启动应用后，访问<code>/index</code>页面就会触发<code>ZeroDivisionError</code>异常，该异常的Traceback信息也会输出至<code>uWSGI</code>的日志中，效果如下：</p><pre><code class="shell">$ uwsgi --module django_test.wsgi --http 0.0.0.0:5000*** Starting uWSGI 2.0.18 (64bit) on [Thu May 23 16:27:42 2019] ***......spawned uWSGI worker 1 (and the only) (pid: 13888, cores: 1)Traceback (most recent call last):  ......  File &quot;./django_test/views.py&quot;, line 3, in index    1 / 0ZeroDivisionError: division by zero[pid: 13888|app: 0|req: 1/1] 127.0.0.1 () &#123;40 vars in 964 bytes&#125; [Thu May 23 08:27:44 2019] GET /index =&gt; generated 55641 bytes in 47 msecs (HTTP/1.1 500) 1 headers in 63 bytes (1 switches on core 0)</code></pre><h1 id="用函数装饰器捕捉异常"><a href="#用函数装饰器捕捉异常" class="headerlink" title="用函数装饰器捕捉异常"></a>用函数装饰器捕捉异常</h1><p>Django中的视图函数只是一个普通的函数（废话），这就意味着我们可以用一个能够捕捉异常的函数装饰器来包裹这个视图函数，例子如下：</p><pre><code class="python"># django_test/views.pyfrom django.http.response import HttpResponsedef catch_exc(func):    def wrapper(*args, **kwargs):        try:            return func(*args, **kwargs)        except Exception as ex:            # put your code here            # logger.error(traceback.format_exc())            # return Response or just raise            return HttpResponse(str(ex))    return wrapper@catch_excdef index(request):    1 / 0    return HttpResponse(b&#39;hello world&#39;)</code></pre><p>在例子第18行～第19行，我用<code>catch_exc</code>这个装饰器包裹了<code>index</code>这个视图函数。而在<code>catch_exc</code>这个装饰器内部，我用<code>except Exception</code>语句捕捉了<code>index</code>函数所有可能出现的异常。然后，我们就可以按照需要来进行各种操作了，比如：</p><ul><li>用<code>traceback.format_exc()</code>获取详细的<code>Traceback</code>信息；</li><li>将错误信息输出至日志；</li><li>通知某人；</li></ul><p>等等等等。对异常的处理完成后，我们既可以通过<code>raise</code>语句让用户仍然收到<code>500 Internal Server Error</code>响应，也可以像上面的例子中返回一个关于异常信息的<code>200 OK</code>响应，还可以渲染一个提示HTML页面返回给用户。总之，Everything is under control.</p><h1 id="用Django中间件捕捉异常"><a href="#用Django中间件捕捉异常" class="headerlink" title="用Django中间件捕捉异常"></a>用Django中间件捕捉异常</h1><p>函数装饰器很好用，但装饰器只能针对单个视图函数。有没有一种应用于所有视图函数之上的异常捕捉方法？有的，这就是Django的中间件。和我上一篇文章（<a href="/2019/03/11/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90/">使用中间件对视图函数进行性能分析</a>）不同的是，那篇文章里所使用的中间件hook为<code>process_request&amp;process_response</code>，而这里使用中间件hook为<code>process_exception</code>，因为前者无法捕捉异常。</p><pre><code class="python"># django_test/my_middleware.pyfrom django.http import HttpResponseclass CatchExcMiddleware(object):    def __init__(self, get_response):        self.get_response = get_response    def __call__(self, request):        return self.get_response(request)    def process_exception(self, request, exception):        # put your code here        # logging.error(traceback.format_exc())        # return Response or None        return HttpResponse(str(exception))# django_test/settings.py# ......MIDDLEWARE = [    # ......    &#39;django_test.my_middleware.CatchExcMiddleware&#39;,  # 启用中间件]# ......</code></pre><p>和上一节的装饰器类似，当视图函数出现异常时，Django就会遍历所有中间件的<code>process_exception</code>hook，包括例子里的。在这个<code>process_exception</code>方法里，我们同样可以完成上一节提到的各种操作，操作完成后可以通过<code>return None</code>语句让用户仍然收到<code>500 Internal Server Error</code>响应，也可以像上面的例子中返回一个关于异常信息的<code>200 OK</code>响应……这里就不再重复了。</p><h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>其实在非DEBUG模式（<code>settings.py</code>里<code>DEBUG = False</code>）下，Django可以将视图函数里出现的异常信息以电子邮件的形式发送给<code>ADMINS</code>里配置的邮箱地址。这个功能需要配置邮箱参数、LOGGING，这里就不详细展开了，有兴趣的话可以查看<a href="https://docs.djangoproject.com/en/2.2/howto/error-reporting/">Django2.2官方文档(Error reporting)</a>或搜索相关教程。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>Python</tag>
      
      <tag>Django</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Django视图函数性能分析</title>
    <link href="/2019/03/11/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90/"/>
    <url>/2019/03/11/Django%E8%A7%86%E5%9B%BE%E5%87%BD%E6%95%B0%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>虽然根据经验来看，许多简单Django应用的性能问题都来源于数据库IO（而这可以通过聚合查询等手段进行优化），但<code>premature optimization is the root of all evil</code>，定位性能问题的具体位置仍然是最应该先做的事。<br>和其它相对独立的Python代码不同，Django的视图函数代码与Django框架的耦合度很高，这给对代码的性能分析带来了一些困难。但幸运的是，Django丰富的生态环境中有足够多的手段能让我们很方便地完成这件事。</p><blockquote><p>注：本文所使用的运行环境为<code>Python 3.6</code>+<code>Django 2.1.7</code></p></blockquote><h1 id="Django-Debug-Toolbar"><a href="#Django-Debug-Toolbar" class="headerlink" title="Django Debug Toolbar"></a>Django Debug Toolbar</h1><p>Django Debug Toolbar可能是Django生态环境中最知名、最强大的页面调试工具了（<a href="https://django-debug-toolbar.readthedocs.io/en/latest/">官方文档连接</a>）。这个工具可以很方便地将：页面耗时、设置、请求&#x2F;响应、模板、SQL操作等一系列信息以侧边栏的形式展示在网页右侧，官方宣传图如下。<br><img src="/images/2019-03-11.01.png"><br>由于该工具能够展示页面耗时、SQL操作耗时等信息（特别是后者），所以该工具在视图函数性能分析方面也能起到非常关键的作用。<br>其最小化的部署方式如下：</p><ol><li><p>通过<code>pip</code>安装该工具</p><pre><code class="shell">pip install django-debug-toolbar# 通过源代码安装开发版# pip install -e git+https://github.com/jazzband/django-debug-toolbar.git#egg=django-debug-toolbar</code></pre></li><li><p>修改Django配置文件（默认为<code>settings.py</code>），包括修改<code>INSTALLED_APPS</code>、<code>MIDDLEWARE</code>项，新增<code>INTERNAL_IPS</code>项。</p><pre><code class="python">INSTALLED_APPS = [    # ...    &#39;django.contrib.staticfiles&#39;,  # 确保已配置静态文件    # ...    &#39;debug_toolbar&#39;,]STATIC_URL = &#39;/static/&#39;</code></pre><pre><code class="python">MIDDLEWARE = [    # ...    &#39;debug_toolbar.middleware.DebugToolbarMiddleware&#39;,    # ...]</code></pre><pre><code class="python">INTERNAL_IPS = [&#39;127.0.0.1&#39;]  # 只对本机访问启用</code></pre></li><li><p>配置Django URL规则</p><pre><code class="python">from django.conf import settingsfrom django.urls import include, pathurlpatterns = [    # ...]if settings.DEBUG:  # 只在DEBUG模式下启用    import debug_toolbar    urlpatterns.append(        path(&#39;__debug__/&#39;, include(debug_toolbar.urls))    )</code></pre></li></ol><p>此时打开任意一个页面即可看到对应效果，如下图所示：<br><img src="/images/2019-03-11.02.png"><br>另外，对于返回JSON、XML等数据的接口型视图函数来说，默认情况下是没办法使用Django Debug Tool的。不过在本地调试时，我们可以通过返回一个空的HTML页面（只需包含一个闭合的<code>body</code>标签）来达到类似的效果，具体操作如下：</p><pre><code class="python">from django.http.response import HttpResponse, JsonResponsedef view_func(request):    # ...    # return JsonResponse(&#123;&#39;data&#39;: &#39;...&#39;&#125;)  # 无法启用Django Debug Tool    return HttpResponse(&#39;&lt;body&gt;&lt;/body&gt;&#39;)  # 可以启用Django Debug Tool</code></pre><h1 id="cProfile-Django中间件"><a href="#cProfile-Django中间件" class="headerlink" title="cProfile+Django中间件"></a>cProfile+Django中间件</h1><p>虽然Django Debug Toolbar是一个非常实用的性能分析工具，但它对于视图函数内部的代码具体执行耗时依然无能为力。如果想要进行更深层次的代码性能分析，就要使用我<a href="/2019/03/08/Python%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E5%90%8E%E7%AF%87%EF%BC%89/">上一篇文章</a>介绍的<code>cProfile</code>这类工具了。但与那篇文章中提到的使用方法不同，这次我们必须先编写Django中间件才能对Django视图函数进行性能分析。</p><blockquote><p>注：本节内容灵感来自博文：<a href="https://rock-it.pl/how-to-profile-django-views/">How to profile Django views</a></p></blockquote><ol><li><p>编写中间件代码主体（假设位于<code>django_profile/my_middleware.py</code>）</p><pre><code class="python"># coding:utf-8import cProfileimport pstatsfrom io import StringIOfrom django.conf import settingsfrom django.core.exceptions import MiddlewareNotUsedfrom django.http.response import HttpResponseclass ProfileMiddleware(object):    def __init__(self, get_response):        if not settings.DEBUG:            raise MiddlewareNotUsed  # 只在DEBUG模式下启用该中间件        self.get_response = get_response    def __call__(self, request):        if &#39;profile&#39; in request.GET:  # 当请求URL中存在profile参数时进行性能分析            profile = cProfile.Profile()            profile.enable()            self.get_response(request)            profile.disable()            ram_file = StringIO()            sort_by = &#39;tottime&#39;            stats = pstats.Stats(profile, stream=ram_file)            stats.strip_dirs().sort_stats(sort_by).print_stats()            response = HttpResponse(ram_file.getvalue().encode(&#39;utf-8&#39;), &#39;text/plain&#39;)        else:            response = self.get_response(request)        return response</code></pre></li><li><p>在Django配置文件中启用该中间件</p><pre><code class="python">MIDDLEWARE = [    # ...    &#39;django_profile.my_middleware.ProfileMiddleware&#39;,    # ...]</code></pre></li></ol><p>该中间件大概的思路为：在Django真正调用视图函数（23行，<code>self.get_response(request)</code>）时，使用<code>cProfile</code>对该视图函数进行性能分析，并用分析结果打包成的<code>HttpResponse</code>取代视图函数的<code>Response</code>返回给Django（或者说用户）。当然，由于我加了一系列的限制，该流程只会在DEBUG模式下、且URL中包含<code>profile</code>参数时才会启用。<br>此时在任何一个页面URL（<code>HTTP Parameters</code>）中增加<code>profile</code>参数时，即可看到对应效果，如下图所示：<br><img src="/images/2019-03-11.03.png"></p><p>这里介绍的只是<code>cPorfile</code>中间件的一个简单用例。实际上<code>cProfile</code>与Django中间件均具有比较强的可定制性，<a href="https://github.com/safarijv/yet-another-django-profiler">yet-another-django-profiler</a>就是一个思路类似但更加复杂和完善的项目，有兴趣的话可以自行了解。另外，从理论上来说，<code>cProfile</code>可以和任意一个有类似Django中间件模式的Python Web框架搭配来达成类似的效果，这里就不扩展来说了。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Web开发</tag>
      
      <tag>Python</tag>
      
      <tag>性能分析</tag>
      
      <tag>Django</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Python代码性能分析（后篇）</title>
    <link href="/2019/03/08/Python%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E5%90%8E%E7%AF%87%EF%BC%89/"/>
    <url>/2019/03/08/Python%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E5%90%8E%E7%AF%87%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>在前一篇文章中，我介绍了测量执行Python代码所需要的总体时间的几种方法。而在实际的性能分析场景中，目标代码的逻辑往往比较复杂，光靠总体执行耗时并不能帮助我们快速定位性能瓶颈。这个时候就需要请出Python的标准库：<code>cProfile</code>（<a href="https://docs.python.org/3.6/library/profile.html">官方文档</a>）来对代码进行细致的性能分析了。<br>和前一篇文章一样，本文所使用的测试平台为<code>Ubuntu 18.04</code>+<code>Python 3.6</code>，并使用代码文件<code>slow_func.py</code>作为样例进行性能分析，内容如下：</p><pre><code class="python"># coding:utf-8import timeimport randomdef func1():    time.sleep(1)  # 等待一秒def func2():    for _ in range(2 ** 24):        random.random()  # 生成1600万个随机数if __name__ == &#39;__main__&#39;:    func1()    func2()</code></pre><h1 id="命令行使用cProfile"><a href="#命令行使用cProfile" class="headerlink" title="命令行使用cProfile"></a>命令行使用cProfile</h1><p>对于单独的Python代码文件来说，通过命令行使用cProfile无疑是最方便的选择。<br>执行如下shell命令：</p><pre><code class="shell">python3 -m cProfile -s tottime slow_func.py</code></pre><blockquote><p>首先用<code>python3</code>的<code>-m</code>选项调用<code>cProfile</code>模块，然后用<code>cProfile</code>的<code>-s</code>选项让输出结果按<code>tottime</code>进行排序，最后执行<code>slow_func.py</code>文件。<br>完整调用格式为：<code>python -m cProfile [-o output_file] [-s sort_order] myscript.py</code></p></blockquote><p>得到如下结果：</p><pre><code class="shell">         16778563 function calls (16778520 primitive calls) in 3.176 seconds   Ordered by: internal time   ncalls  tottime  percall  cumtime  percall filename:lineno(function)        1    1.456    1.456    2.172    2.172 slow_func.py:8(func2)        1    1.001    1.001    1.001    1.001 &#123;built-in method time.sleep&#125; 16777216    0.716    0.000    0.716    0.000 &#123;method &#39;random&#39; of &#39;_random.Random&#39; objects&#125;        1    0.001    0.001    0.001    0.001 &#123;built-in method _imp.create_dynamic&#125;        3    0.000    0.000    0.000    0.000 &#123;built-in method marshal.loads&#125;        # ...省略后续100多行</code></pre><ul><li>输出的第1行表明，脚本文件执行中存在1600多万次的函数调用，共耗费3.268秒。<ul><li>而根据前一篇文章的测试结果，直接执行该脚本文件只需要2秒左右的时间，那多出来的1秒多花在了哪里？这是因为<code>cProfile</code>需要对每一次函数调用进行监控和记录，由于该文件存在较多的函数调用，所以总执行耗时也就增长了许多了。</li></ul></li><li>第3行表明，下表内容按照<code>internal time</code>（内部执行时间，<code>tottime</code>）排序，这是由执行命令中的<code>-s tottime</code>参数决定的。</li><li>第5行为分析结果表的表头，依次为<code>ncalls</code>：调用次数、<code>tottime</code>：内部执行耗时、<code>percall</code>：内部执行耗时&#x2F;调用次数、<code>cumtime</code>：累计执行耗时、<code>percall</code>：累计执行耗时&#x2F;调用次数，以及最后的<code>文件名+行号+函数名称</code>。<ul><li><code>tottime</code>和<code>cumtime</code>的区别在于，<code>tottime</code>不包括子函数执行所花费的时间，而<code>cumtime</code>是包括的。</li></ul></li><li>第6行表明，<code>slow_func.py</code>中第8行的<code>func2</code>函数共执行了1次，内部耗时1.456秒，累计耗时2.172秒。</li><li>第7行表明，Python内置的<code>sleep</code>函数共执行了一次，耗时1.001秒。</li><li>第8行表明，Python内置的<code>random</code>函数共执行了1600多万次，耗时0.716秒。<ul><li>由于<code>random</code>函数是被<code>func2</code>函数调用的，所以这0.716秒和<code>func2</code>函数的内部执行耗时1.456秒，共同组成了<code>func2</code>函数的累计执行耗时：2.172秒。</li></ul></li><li>由于是用<code>cProfile</code>分析整个脚本文件，所以许多Python自身所需的函数调用也被展示在了结果里，所以分析结果表才会有100多行的规模。这个问题可以在下一小节中解决。</li></ul><h1 id="代码里使用cProfile"><a href="#代码里使用cProfile" class="headerlink" title="代码里使用cProfile"></a>代码里使用cProfile</h1><p>从本质上来说，通过命令行使用<code>cProfile</code>相当于在代码里使用<code>cProfile</code>的一个简化操作。而在命令行里分析代码有着明显的局限性：目标代码必须独立成文件、输出格式固定等等。所以，在代码里使用<code>cProfile</code>往往是一个更优的选择。<br>执行如下Python代码：</p><pre><code class="python">import cProfileimport pstatsfrom io import StringIOfrom slow_func import func1, func2profile = cProfile.Profile()profile.enable()  # 分析开始func1()func2()profile.disable()  # 分析结束ram_file = StringIO()sort_by = &#39;tottime&#39;stats = pstats.Stats(profile, stream=ram_file)  # 读取结果stats.strip_dirs().sort_stats(sort_by).print_stats()  # 按格式输出至ram_fileprint(ram_file.getvalue())</code></pre><blockquote><p>代码的核心逻辑是使用<code>cProfile</code>模块的<code>Profile</code>类对代码块进行性能分析，分析完成后使用<code>pstats</code>模块的<code>Stats</code>类将分析结果按一定格式写入至内存文件，最后输出该文件里写入的内容。<br>实际上这只是一个较为简单的样例，<code>pstats</code>模块还可以获得函数之间的调用关系、将结果持久化、显示文件路径等等，更完整的说明可以参考<a href="https://docs.python.org/3.6/library/profile.html">官方文档</a>。</p></blockquote><p>得到如下结果：</p><pre><code class="shell">         16777220 function calls in 3.225 seconds   Ordered by: internal time   ncalls  tottime  percall  cumtime  percall filename:lineno(function)        1    1.517    1.517    2.224    2.224 slow_func.py:8(func2)        1    1.001    1.001    1.001    1.001 &#123;built-in method time.sleep&#125; 16777216    0.707    0.000    0.707    0.000 &#123;method &#39;random&#39; of &#39;_random.Random&#39; objects&#125;        1    0.000    0.000    1.001    1.001 slow_func.py:5(func1)        1    0.000    0.000    0.000    0.000 &#123;method &#39;disable&#39; of &#39;_lsprof.Profiler&#39; objects&#125;</code></pre><h1 id="cProfile结果可视化"><a href="#cProfile结果可视化" class="headerlink" title="cProfile结果可视化"></a>cProfile结果可视化</h1><p>一般来说，通过以上两个例子就可以获得完善的性能分析报告了。但通过一些可视化工具对<code>cProfile</code> 的报告进行二次处理，我们可以更清晰地观察函数之间的调用关系、更轻松地找出性能瓶颈，算是一个不错的辅助手段。在这里只介绍一种可视化工具：JetBrain PyCharm自带的<code>Profile</code>工具。</p><ul><li><p>点击Pycharm中<code>Run</code>菜单里的<code>Profile &#39;xxx&#39;项目</code>，即可对当前运行执行方案使用<code>cProfile</code>进行性能分析，如下图：<br><img src="/images/2019-03-08.01.png"></p></li><li><p>结果如下图所示。其中，<code>Time</code>对应<code>cProfile</code>中的<code>cumtime</code>，即累计执行耗时；<code>Own Time</code>对应<code>cProfile</code>中的<code>tottime</code>，即内部执行耗时。<br><img src="/images/2019-03-08.02.png"></p><p><img src="/images/2019-03-08.03.png"></p></li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>性能分析</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Python代码性能分析（前篇）</title>
    <link href="/2019/03/06/Python%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E5%89%8D%E7%AF%87%EF%BC%89/"/>
    <url>/2019/03/06/Python%E4%BB%A3%E7%A0%81%E6%80%A7%E8%83%BD%E5%88%86%E6%9E%90%EF%BC%88%E5%89%8D%E7%AF%87%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>我们怎样知道执行某个Python文件、某个函数、某段代码所耗费的总体时间？这是这篇文章尝试去回答的问题。<br>作为样例，本文使用<code>slow_func.py</code>来进行性能分析，内容如下：</p><pre><code class="python"># coding:utf-8import timeimport randomdef func1():    time.sleep(1)  # 等待一秒def func2():    for _ in range(2 ** 24):        random.random()  # 生成1600万个随机数if __name__ == &#39;__main__&#39;:    func1()    func2()</code></pre><p>函数<code>func1</code>和<code>func2</code>的区别在于：CPU在执行<code>func1</code>时基本处在闲置状态，在执行<code>func2()</code>时基本处于忙碌状态。这点会在之后的测试中有所体现。<br>在笔者的测试平台（<code>Ubuntu 18.04</code>+<code>Python 3.6</code>）上，两个函数所耗费的时间均在<code>1s</code>左右。</p><h1 id="time命令"><a href="#time命令" class="headerlink" title="time命令"></a>time命令</h1><p>类UNIX平台提供了<code>time</code>命令以统计执行执行命令所花费的时间。当然，这是一个通用型的工具，而不局限于Python。<br>执行如下shell命令：</p><pre><code class="shell">time python3 slow_func.py</code></pre><p>获得如下结果：</p><pre><code class="shell">real0m1.960s  # 命令执行时间user0m0.946s  # 用户态CPU时间sys 0m0.008s</code></pre><p>根据前两行结果中我们可以得知，<code>slow_func.py</code>从开始到结束共消耗了2秒左右的时间，但实际消耗的用户态CPU时间只有1秒左右。这是因为CPU在执行<code>func1()</code>时处于等待状态（<code>sleep</code>），这段时间里是不消耗CPU时间的。</p><h1 id="time库"><a href="#time库" class="headerlink" title="time库"></a>time库</h1><p>Python提供了标准库<code>time</code>来进行关于时间的操作，我们可以通过这个库来测量代码执行所耗费的时间。<br>执行如下Python代码：</p><pre><code class="python">import timefrom slow_func import func1, func2start1, start2 = time.perf_counter(), time.process_time()func1()func2()print(&#39;perf_counter: &#123;:.4f&#125;s&#39;.format(time.perf_counter() - start1))print(&#39;process_time: &#123;:.4f&#125;s&#39;.format(time.process_time() - start2))</code></pre><p>获得如下结果：</p><pre><code class="shell">perf_counter: 2.1201sprocess_time: 1.1119s</code></pre><p><code>time.perf_counter()</code>的时间差是代码开始与代码结束两个时间点的时间差，而<code>time.process_time()</code>的时间差是消耗的CPU时间长度，所以得出了不同的结果，这与先前的<code>time</code>命令的原因和结果相类似。</p><h1 id="time库-上下文管理器"><a href="#time库-上下文管理器" class="headerlink" title="time库+上下文管理器"></a>time库+上下文管理器</h1><p>上面提到的用time库来测量代码耗时用起来很方便，但如果经常要用到的话写起来也很繁琐。这时我们可以写一个自定义的上下文管理器来避免重复代码。<br>执行如下Python代码：</p><pre><code class="python">import timefrom contextlib import contextmanager@contextmanagerdef time_block(label):  # 代码块计时上下文管理器    # 进入上下文    start = time.perf_counter()    try:        yield  # 执行代码块    finally:        # 执行完成后输出代码块耗时        used = time.perf_counter() - start        print(&#39;&#123;&#125;: &#123;:.4f&#125;s&#39;.format(label, used))# 用法with time_block(&#39;sleep&#39;):    time.sleep(1)</code></pre><p>获得如下结果：</p><pre><code class="shell">sleep: 1.0011s</code></pre><h1 id="time库-函数装饰器"><a href="#time库-函数装饰器" class="headerlink" title="time库+函数装饰器"></a>time库+函数装饰器</h1><p>上下文管理器针对的是代码块，如果只想统计函数执行所消耗的时间，用函数装饰器更为方便和快捷。<br>执行如下Python代码：</p><pre><code class="python">import timefrom functools import wrapsdef time_wrap(func):  # 函数计时装饰器    @wraps(func)    def wrapper(*args, **kwargs):        start = time.perf_counter()        r = func(*args, **kwargs)        used = time.perf_counter() - start        print(&#39;&#123;f.__module__&#125;.&#123;f.__name__&#125;: &#123;t:.4f&#125;s&#39;.format(f=func, t=used))        return r    return wrapper@time_wrap  # 函数定义时使用装饰器def slow_func():    time.sleep(1)# 执行函数时自动调用装饰器slow_func()</code></pre><p>获得如下结果：</p><pre><code class="shell">__main__.slow_func: 1.0008s</code></pre><h1 id="timeit库"><a href="#timeit库" class="headerlink" title="timeit库"></a>timeit库</h1><p>当需要多次重复测量Python代时以获取精确的耗时结果时，我们可以通过循环控制配合上文提到的方法来实现，也可以通过一个更便捷的、适合重复测试的标准库：<code>timeit</code>来实现。<br>执行如下代码：</p><pre><code class="python">import timeitsetup = &#39;from slow_func import func1&#39;used = timeit.timeit(&#39;func1()&#39;, setup=setup, number=5)print(&#39;&#123;:.4f&#125;&#39;.format(used))</code></pre><p>获得如下结果：</p><pre><code class="shell">5.0039</code></pre><p><code>timeit</code>库默认使用的计时器为<code>time.perf_counter()</code>，如果想换成测量CPU耗时的计时器，只需要附加上<code>timer</code>参数即可：</p><pre><code class="python">...import timetimer = time.process_timeused = timeit.timeit(&#39;func1()&#39;, timer=timer, setup=setup, number=5)  # 附加timer参数...</code></pre>]]></content>
    
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>性能分析</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Nginx&amp;uWSGI静态文件性能对比</title>
    <link href="/2019/02/20/Nginx-uWSGI%E9%9D%99%E6%80%81%E6%96%87%E4%BB%B6%E6%80%A7%E8%83%BD%E5%AF%B9%E6%AF%94/"/>
    <url>/2019/02/20/Nginx-uWSGI%E9%9D%99%E6%80%81%E6%96%87%E4%BB%B6%E6%80%A7%E8%83%BD%E5%AF%B9%E6%AF%94/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p><img src="/images/2019-02-20.01.png"><br>在实际部署Python Web应用时，我们往往会采用类似<code>Nginx-&gt;uWSGI/Gunicorn-&gt;Python</code>的三层架构。使用Nginx而不是直接使用uWSGI来处理HTTP请求的理由主要有以下几点：</p><ul><li>Nginx更安全；</li><li>需要实现负载均衡&#x2F;URL转发；</li><li>Nginx处理静态文件更快，缓存头更完善。</li></ul><p>但在某些诸如简单Web应用、外部统一网关、单机单容器、内网环境等部署场景，Nginx的优势并不一定那么明显。本文着重分析Nginx、uWSGI的静态文件性能问题，辅助判断是否需要使用Nginx。</p><h1 id="性能测试流程"><a href="#性能测试流程" class="headerlink" title="性能测试流程"></a>性能测试流程</h1><ol><li>创建测试文件<br>  生成大小为4k~512k的随机字符串文件作为测试样本</li></ol><pre><code class="shell">openssl rand -hex $(bc &lt;&lt;&lt; 2^11-1) &gt; 4k.hexopenssl rand -hex $(bc &lt;&lt;&lt; 2^12-1) &gt; 8k.hex...openssl rand -hex $(bc &lt;&lt;&lt; 2^18-1) &gt; 512k.hex</code></pre><ol start="2"><li>启动测试容器</li></ol><pre><code class="shell">docker run -d -v $(pwd):/static --name pytest python:3.6-jessie tail -f /dev/nulldocker exec -it pytest bash</code></pre><ol start="3"><li>配置并启动Nginx和uWSGI</li></ol><pre><code class="shell">apt update &amp;&amp; apt install -y nginx apache2-utilspip install uwsgicat &gt;/etc/nginx/conf.d/default.conf&lt;&lt;EOFserver &#123;    listen 81 default_server;    location / &#123;        root /static;    &#125;&#125;EOF# 启动nginx和uWSGIservice nginx startuwsgi --master --http 0.0.0.0:82 --static-map /=/static</code></pre><ol start="4"><li>开始测试<br>  使用<code>Apache Benchmark</code>对Nginx和uWSGI的静态文件进行测试，并发数为100，共请求20000次。提取测试结果中的关键参数：<code>Requests per second</code>。</li></ol><pre><code class="shell">cd /staticfor fn in *.hex; do  echo -e &quot;\n$fn&quot;  echo &quot;nginx (keep alive)&quot;  ab -q -k -c 100 -n 20000 127.0.0.1:81/$fn | grep -Po &#39;Requests per second:[ \d]+&#39;  echo &quot;nginx&quot;  ab -q -c 100 -n 20000 127.0.0.1:81/$fn | grep -Po &#39;Requests per second:[ \d]+&#39;  echo &quot;uwsgi&quot;  ab -q -c 100 -n 20000 127.0.0.1:82/$fn | grep -Po &#39;Requests per second:[ \d]+&#39;done</code></pre><h1 id="测试结果"><a href="#测试结果" class="headerlink" title="测试结果"></a>测试结果</h1><p><img src="/images/2019-02-20.02.png"></p><p>其中<code>nginx (keep-alive)</code>项为理论最好情况，实际应用中很难达到。uWSGI在处理静态文件时不支持<code>keep-alive</code>模式。</p><p>假设将未使用<code>keep-alive</code>的Nginx性能作为基准，可得出下表：</p><table><thead><tr><th align="center">文件大小</th><th align="center">nginx(keep-alive)</th><th align="center">nginx</th><th align="center">uwsgi</th></tr></thead><tbody><tr><td align="center">4k</td><td align="center">157%</td><td align="center">100%</td><td align="center">43%</td></tr><tr><td align="center">8k</td><td align="center">204%</td><td align="center">100%</td><td align="center">42%</td></tr><tr><td align="center">16k</td><td align="center">202%</td><td align="center">100%</td><td align="center">34%</td></tr><tr><td align="center">32k</td><td align="center">207%</td><td align="center">100%</td><td align="center">26%</td></tr><tr><td align="center">64k</td><td align="center">173%</td><td align="center">100%</td><td align="center">20%</td></tr><tr><td align="center">128k</td><td align="center">160%</td><td align="center">100%</td><td align="center">14%</td></tr><tr><td align="center">256k</td><td align="center">151%</td><td align="center">100%</td><td align="center">11%</td></tr><tr><td align="center">512k</td><td align="center">125%</td><td align="center">100%</td><td align="center">10%</td></tr></tbody></table><p>由此可以看出，uWSGI在处理小体积静态文件上的效率能达到Nginx的三、四成，但当静态文件体积增加到64k及以上时，其效率就远比不上Nginx了。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Nginx</tag>
      
      <tag>uWSGI</tag>
      
      <tag>性能测试</tag>
      
      <tag>Web开发</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
